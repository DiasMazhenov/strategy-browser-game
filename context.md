# Контекст проекта — «Империи Рассвета» (Empires Dawn)

Мини-стратегия в реальном времени в духе **Age of Empires**: изометрия, сбор ресурсов,
эпохи, юниты, здания, набеги волн, дипломатия с ИИ-соперником. Один HTML-файл на выходе
(весь JS/CSS вшит Vite-плагином `singlefile`), рядом только папка `voices/` с mp3.

**Язык игры и общения — русский. Арт — пиксель-арт (не реализм).**

---

## Текущая версия

**1.0.022** — источник истины: константа `GAME_VERSION` в `src/App.tsx`.

## Реплики при выходе юнита из здания (1.0.022)
- `SoundBank.playPhrase(phrase)` — проигрывает КОНКРЕТНУЮ фразу по ключу: сперва казахская
  запись расы игрока (KZ_CLIP_URLS, случайный вариант), затем откат на русскую.
- В `updateBuildings` при завершении обучения (выход юнита из здания, owner==='player'):
  крестьянин → случайно `playPhrase('чего изволите')` или `playPhrase('слушаю')` (казахские
  «Чего изволите - каз» / «слушаю - каз»); воин из казармы/конюшни/кузницы/ГЦ →
  `playPhrase('готов')` (казахское «Я готов - каз»). Звучит вместе с `sound.train()`.

## Казахские голоса рабочих (1.0.021)
Веб-загрузка пользователя добавила 9 mp3 в `public/voices/kaz-voises/worker-kaz/` (все NFC,
суффикс ` - kaz.mp3`): «За работу», «Добываю», «Берёмся за дело», «Сделаю в лучшем виде»,
«Сейчас сделаю», «Отобьёмся», «Чего изволите», «слушаю» ×2 варианта. Подключены в `KZ_CLIPS`
(путь с подпапкой `worker-kaz/` — сегментный URL-код уже поддерживал вложенность, кодируется
только имя файла). Крестьянин: select=[слушаю, чего изволите], move=[сейчас сделаю, сделаю в
лучшем виде], attack=[отобьемся], gather=[за работу, добываю, беремся за дело]. События:
gather звучит при отправке на работу (voiceSel('gather')), move — при перемещении.

## Рельеф — AI-арт гор и холмов (gpt image) (1.0.020)

## Рельеф — AI-арт гор и холмов (gpt image) (1.0.020)
По фидбеку («не похоже на вершины, повторяются; нарисуй красивые части гор/холмов через gpt image»):
- Сгенерированы 4 AI-спрайта рельефа (`src/assets/sprites/terrain/`, raw/ в .gitignore):
  `peak_snow.png` (235×128, заснеженная вершина), `peak_rock.png` (166×118, скальный массив),
  `hill_grass.png` (124×63, травяной курган), `hill_rock.png` (128×68, каменистый холм).
  Конвейер `scripts/process-terrain.cjs` (как у зданий): magenta-кейнг flood-fill → крупнейшая
  компонента → кроп → NN даунскейл (пики масштабируются по ВЫСОТЕ для высоких пиков, холмы по
  ширине) → чистка пурпурной каймы (5 проходов по краю + глобальная вырезка яркой магенты +
  деспилл G). Якорь посадки — низ-центр.
- engine: `TERRAIN_FEATURES[4]` (img+scale: snow 1.55, rock 1.25, grass 0.85, rock 0.95).
  В рендере вместо процедурных пиков размещаются спрайты: на локальных максимумах рельефа,
  прорежены хэшем координат и разнесены минимальной дистанцией (`nearPlaced`, rad в клетках:
  snow 5, rock 3, hill 3) — редкие одиночные вершины/курганы, а не частокол. Снежные пики на
  высочайших поясах (lvl5), скальные на lvl4, холмы в биоме hill lvl2+. Рисуются через
  `drawTerrainFeature` (тень-эллипс + спрайт низ-центром на опорную точку поднятой террасы),
  кладутся в общий drawList и сортируются по глубине. Горный/холмовой тайлы остаются плоскими
  каменистыми/травяными площадками террас.

## Рельеф — плавная лесенка, плоская вода, редкие вершины (1.0.019)

## Рельеф — плавная лесенка, плоская вода, редкие вершины (1.0.019)
Переделано по фидбеку (в 1.0.018 пики рисовались на КАЖДОЙ плитке → частокол):
- **Плавный подъём.** Высота строится не от дёрганого биом-шума, а от ГЛАДКОГО
  крупномасштабного `fbm(seed, cell=900, oct=3)`. Сырая ступень: вода/берег/базы = 0,
  иначе `round(clamp((rv-0.46)/0.34,0,1)*5)`. Затем `reliefGrid()` делает два прохода
  (слева-сверху и справа-снизу): высота = min(сырая, расстояние до любой низины/воды) ⇒
  соседние плитки отличаются МАКСИМУМ на 1 ступень (проверено скриптом
  scripts/relief-probe.cjs: max drop = 1, ~27% поднятых). Никаких резких обрывов.
- **Вода плоская.** water/deep/sand всегда ступень 0 (вода на одном уровне).
- **Вершины не повторяются.** С горного тайла (`getMountainTile`) УБРАН пик — теперь это
  плоская каменистая площадка террасы. Большие снежные вершины (`Game.drawPeak`) рисуются
  ОТДЕЛЬНО и РЕДКО: только на локальных максимумах горного пояса (ступень ≥4), прореженных
  детерминированно (хэш координат ≥150/255). Итог — хребет, а не частокол.
- engine: сетка `hAt` строится раз за кадр на видимую область (с запасом +S), `upAt(wx,wy)`
  даёт iso-px подъёма; используется в цикле тайлов, гранях (drawCliff), и для подъёма объектов
  (декор/ресурсы/реликвии/юниты). Вершины кладутся в общий drawList и сортируются по глубине.

## Рельеф — ступенчатый подъём плиток по вертикали (1.0.018)
- Раньше холмы/горы были просто другой текстурой на плоскости. Теперь плитки реально
  ПОДНИМАЮТСЯ ЛЕСЕНКОЙ по вертикали: у каждой точки ландшафта есть целочисленная ступень
  высоты, плитка рисуется выше на `ступень × RELIEF_STEP` (10 iso-px), а между уровнями
  рисуются две вертикальные грани-«уступа» (тёмный бок) к соседям спереди.
- `terrain.ts`: `heightAt(wx,wz)` — ступени из `elev` шума: с 0.57, `round((e-0.57)*15)`
  кап 5 (холмы — низкие ступени, горы до ~50px); safe-зоны (базы) всегда 0. Результат
  кэшируется в `hCache` (Map, чистится при >60000). Высота чисто визуальная — мир/движение/
  коллизии/picking остаются ПЛОСКИМИ (никакой реальной 3D-высоты).
- engine.ts: константа `RELIEF_STEP=10`; хелпер `terrainUp(wx,wy)` (iso-px подъёма в точке,
  с привязкой к сетке TILE_STEP). В цикле тайлов верхняя площадка рисуется со сдвигом `-up`,
  затем `drawCliff` рисует правую (ребро R→B, цвет cR) и левую (L→B, cL) грани с перепадом до
  соседей `x+S`/`y+S`, горизонтальными «пластами» и тёмной подошвой. Палитра граней — по биому
  (`cliffColors`: гора серо-каменная, холм/пустыня/песок охристые, лес/трава/поле зелёные,
  вода синие). Объекты на рельефе приподнимаются на тот же `terrainUp`: декор, ресурсные ноды,
  реликвии, юниты (в drawList и y-sort). Здания/базы на высоте 0 — не трогаются.

## Казахская озвучка — фикс NFD-фолбэка для подпапки (1.0.017)
- У файлов «Клянёмся честью/2» буква «ё» на диске в Unicode-форме **NFD** («е»+◌̈), а строка в коде —
  NFC: dev/статик-сервер отдавал index.html (SPA-фолбэк), реплики не запускались. NFD-фолбэк в
  `playUrl` существует, но ломал путь: `encodeURIComponent` кодировал слеш подпапки
  `kaz-voises/` в `%2F`. Теперь NFD-URL собирается ПОСЕГМЕНТНО (`path.split('/').map(encodeURIComponent).join('/')`),
  слеши пути сохраняются. Проверено: NFD-URL отдаёт audio/mpeg 45 КБ.
- Подтверждено: варианты фраз (`Вперёд`/`Вперёд2`, `За короля`/`2`, `Клянёмся честью`/`2`) лежат
  списками в `KZ_CLIPS`, при каждом реплайке выбирается случайный `kz[(Math.random()*len)|0]`.
  Все 9 файлов отдаются сервером (200).

## Казахская раса — саманные стены, ворота и угловой бастион (1.0.016)
- Доделана восточная фортификация игрока (ранее стены/ворота/угол оставались каменными
  европейскими): gpt image сделал `kz_wall` (длинный саманный сегмент с зубцами и бирюзовой
  плиточной полосой), `kz_gate` (айван-портал в бирюзовой плитке с двумя башенками, стены
  продолжаются до краёв кадра), `kz_wall_corner` (квадратный зубчатый саманный бастион с
  плоской крышей). Враги/нейтралы — на старых каменных.
- Обработка в том же `scripts/process-kz-buildings.cjs`: для стены/ворот кадр ПРИНУДИТЕЛЬНО
  приводится к 186×150 / 152×150 (как штатные wall.png/gate.png) — сегмент edge-to-edge и нужная
  высота (AI рисует диагональ длиннее, поэтому вертикаль подтягиваем, чтобы наклон лёг на
  изо-клетку и соседние сегменты тайлились); угловой бастион — по ширине фундамента (130px),
  грузится как extra-sprite (baseW=naturalWidth). Якоря kz_wall/kz_gate/kz_wall_corner в SPR_ANCHORS.
- engine.ts: wall/gate добавлены в `KZ_SPRITE_URLS`; новый `KZ_CORNER_SPRITE` (makeExtraSprite);
  хелпер `wallSpriteFor(b, isCorner)` выбирает саманный/каменный комплект по `b.owner==='player'`.
  Путь рендера стен (`drawWallSprite`, зеркало по оси x, тень, стройка/флеш) общий.

## Казахская раса — озвучка приказов живыми mp3 (1.0.015)

## Казахская раса — озвучка приказов живыми mp3 (1.0.015)
- Пользователь залил 9 казахских голосовых реплик в `public/voices/kaz-voises/` (коммит
  «Add files via upload»): «Я готов», «Вперёд» (×2), «В атаку», «К бою», «За короля» (×2),
  «Клянёмся честью» (×2). Они подключены к расе игрока.
- `audio.ts`: `KZ_CLIPS` (канон. ключ → варианты файлов в подпапке `kaz-voises/`), `KZ_CLIP_URLS`
  (URL с `encodeURIComponent` имени), `KZ_PHRASES` (по юнитам/событиям). Метод `voice()` сначала
  берёт казахскую реплику (при нескольких вариантах — случайный), а если для команды/юнита её нет
  (часть рабочих реплик крестьянина/монаха — gather/move) — откатывается на русскую запись из
  `PHRASES`. Все вызовы голоса идут от юнитов игрока (`voiceSel` фильтрует owner==='player',
  клик по своему юниту тоже player), поэтому раса-игрок говорит по-казахски, враги — без голоса.

## Казахская раса — здания в восточной средневековой архитектуре (1.0.014)
- Здания игрока (казахская раса) перегенерированы через **gpt image** по 5 фото-референсам пользователя
  (саманные купольные города Средней Азии — Отырар/Туркестан/Сауран): охристый adobe/саман, купола,
  цилиндрические башни-минареты, большие стрельчатые арки-айваны, резной терракотовый кирпич,
  бирюзовая глазурованная плитка. **Дома (house) — ЮРТЫ** (киіз үй): белый войлок, чёрные
  орнаментальные ленты (қошқар-мүйіз), красная дверь, шанырак на куполе, дымок.
- 8 спрайтов в `src/assets/sprites/kz/`: `kz_towncenter` (крепость с бирюзовым куполом и двумя
  айванами), `kz_house` (юрта), `kz_barracks` (казарма-форт с башнями и тамгой), `kz_tower`
  (минарет с бирюзовым куполом), `kz_farm` (юрта+грядки/овцы/пшеница), `kz_stable` (конюшня с
  айван-арками и лошадьми), `kz_market` (караван-сарай/базар с куполом и навесами),
  `kz_blacksmith` (саманная кузня с куполом, горном и дымящей трубой). Стены/ворота/чудо — общие.
- Пайплайн `scripts/process-kz-buildings.cjs` (raw в .gitignore): декод PNG → magenta-кеинг
  (сначала ГЛОБАЛЬНО вырезается насыщенная magenta/purple-заливка `isHotMag`, застрявшая ВНУТРИ
  силуэта — залитый фоном двор казармы, треугольники ромба-подложки у башни; затем флудфилл от краёв
  по isMag/isPurp, ловит и тёмно-пурпурную подложку) → удаление толстой тёмной тени-диска
  морфологией (эрозия r=2 + флудфилл от кромки кадра + дилатация — тонкий чёрный контур и орнамент
  НЕ трогаются) → крупнейшая компонента → кроп → обмер изо-фундамента → nearest-neighbor до
  ЦЕЛЕВОЙ ширины фундамента (TARGET_BASEW, как у штатных зданий; после удаления пурпура истинная
  ширина может быть меньше — это ок, рантайм сам сажает фундамент на ромб клетки) → 3 прохода
  эрозии пурпурной каймы (внутренние фиолетовые детали — ковры/товары — не трогаются) → удаление
  мелких оторванных компонент-пыли <8px. Якоря печатаются в консоль и внесены вручную в SPR_ANCHORS
  (ключи `kz_*`). Скрипт-обработчик версионно-тулза (коммитится), raw — в .gitignore.
- NB: на arena-ветке пользователь через веб добавил казахские голосовые mp3 в
  `public/voices/kaz-voises/` (коммит «Add files via upload») — подключены к расе в 1.0.015 (см. выше).
- engine.ts: импорты 8 kz-PNG; `KZ_SPRITE_URLS` + `KZ_BLD_SPRITES` (грузятся `loadBldSprite` с
  якорями `kz_<key>`, флеш-канвас урона как у штатных); `placeBld(b, S)` теперь берёт восточный
  спрайт когда `b.owner === 'player'` и kz-замена есть, иначе штатный (враги/нейтралы — старые
  здания). Стены/ворота (wallLike) и чудо не переключаются.

## Казахская раса — анимация ходьбы (1.0.013)
- Раньше у расы был 1 статичный кадр на направление (только процедурный боб) — ноги не шли.
  Теперь полноценные **циклы шага**: сгенерированы листы двух фаз ходьбы и нарезаны сплиттером.
- Боковой шаг (2 фазы `_wa`/`_wb`, чередуются по `sin(anim)`, отражается по face) — у ВСЕХ 7
  подвижных юнитов: villager, swordsman, archer, spearman, knight, cavalry, monk (конница — галоп).
  Крестьянин дополнительно имеет перёд-шаг (`_fwa`/`_fwb`) и спина-шаг (`_wba`/`_wbb`) — он чаще всех
  снуёт к ресурсам. У остальных «на камеру/от камеры» — статичные перёд/спина (воин не размножается
  массово, как крестьяне). catapult статичен.
- Файлы: `kz_<key>_wa/_wb.png` (бок шаг), `kz_villager_fwa/fwb/wba/wbb.png`; raw-листы `*_walk_raw.png`
  (бок, режим `walk`), `*_fwalk_raw.png`/`*_bwalk_raw.png` (режимы `fwalk`/`bwalk`) в .gitignore.
  Сплиттер `split-kz-sheet.cjs` расширен режимами walk/fwalk/bwalk + чистка чёрной линии-«земли» у низа.
- pixelart.ts: таблицы `KZ_WALK_SIDE`/`KZ_WALK_FRONT`/`KZ_WALK_BACK` (хелпер W2), в drawUnitSprite для
  расы в движении берётся фаза шага по `kzStep = sin(anim)<0?1:0`; покой/бой — статичный бок; kzFB
  использует walk-цикл если есть, иначе статичный перёд/спина. ВАЖНО: импорт PNG — это URL-строка,
  поэтому кадры расы оборачиваются в `mk()` (как штатные UNIT_IMAGES). Якоря kz_*_wa/wb/fwa/... — в
  UNIT_ANCHORS. Итого **40 PNG** расы в `src/assets/sprites/units/kz/`.

## Казахская раса игрока — направленные кадры (1.0.012)

## Казахская раса игрока — направленные кадры (1.0.012)
- Догенерированы листы «слева=спина, справа=перёд» (`raw/<key>_sheet_raw.png`) для archer, spearman,
  monk, knight, cavalry и нарезаны `split-kz-sheet.cjs` в `kz_<key>_f.png`/`_b.png` (targetH: пешие/монах
  150, knight 128). Теперь НАПРАВЛЕННАЯ ХОДЬБА (вверх — спина, вниз — перёд, вбок — side с отражением)
  у всех подвижных юнитов расы: villager, swordsman, archer, spearman, knight, cavalry, monk.
- В `KZ_FRONT_CYCLE`/`KZ_BACK_CYCLE` (pixelart.ts) добавлены все 7 ключей; якоря kz_*_f/_b — в UNIT_ANCHORS.
- Итого **22 кадра** расы в `src/assets/sprites/units/kz/`: 8 боковых базовых + по 2 направленных
  (перёд/спина) у 7 юнитов; catapult статичен (боковой кадр, разворот не нужен — осадная машина).
- Конница: knight — конь крупом (b)/мордой (f) с всадником сзади/спереди; cavalry — то же для лёгкого
  лучника. В drawUnitSprite kzFB использует эти циклы для fmode 1/2, иначе боковой кадр с отражением.

## Казахская раса игрока (1.0.011)
- **Отдельная раса для игрока** — кочевые казахские батыры по референсам пользователя (меховые шапки
  тымак/борик, ламеллярная кожаная бронь с латунными заклёпками и орнаментом, меховые наплечники,
  круглые кочевые щиты с золотым узором, сабли-клыч, рекурвные луки, тёмно-красные/охристые туники,
  кожаные сапоги). Старые «европейские» спрайты ОСТАЮТСЯ врагам (enemy), нейтральным племенам и зверям.
- **Файлы**: готовые кадры `src/assets/sprites/units/kz/kz_*.png` (12 шт.), исходники-магенты в
  `src/assets/sprites/units/kz/raw/` (в .gitignore, тяжёлые). Конвейер: `scripts/process-kz-sprites.cjs`
  (magenta-кеинг flood-fill → крупнейшая компонента → кроп → nearest-neighbor до целевой высоты →
  чистка пурпурной каймы → якорь ax=центр нижней полосы, ay=низ), `scripts/split-kz-sheet.cjs`
  (режет лист «слева=спина, справа=перёд» на _b/_f по связным компонентам).
- **Интеграция в pixelart.ts**: импорты kz*, `KZ_BASE` (боковой кадр по ключу юнита),
  `KZ_FRONT_CYCLE`/`KZ_BACK_CYCLE` (направленные кадры; сейчас только swordsman и villager).
  В `drawUnitSprite`: `isKz = u.owner==='player' && KZ_BASE[u.key]`. База/якорь — kz_<key>; для расы
  выключены штатные 4-кадровые циклы пехоты (`hasDirWalk`), кадры рубки/работы/лука (working/slashing/
  loosing/aiming) — раса рисует единый боковой кадр с процедурным подскоком/креном, а «на камеру»/
  «от камеры» (fmode 1/2) использует kz-циклы перёд/спина где они есть (иначе бок). `upright` и `mountFB`
  расширена флагом isKz/kzFB. Якоря kz_* добавлены в UNIT_ANCHORS (sprite-art.ts).
- Высоты кадров как у штатных: пешие/монах 150, knight 128, catapult 81; UNIT_TARGET_H берётся по ключу.
- **Пока без направленных кадров** (используют боковой кадр с отражением): archer, spearman, knight,
  cavalry, catapult, monk — кадры перёд/спина догенерируются следующим шагом через split-kz-sheet
  (лист raw/<key>_sheet_raw.png → <key>_b/_f), затем добавить в KZ_FRONT/BACK_CYCLE и UNIT_ANCHORS.
- **Здания расы** пока остаются прежними (казахские постройки — отдельная возможная задача).

## Бесконечная процедурная карта и нейтральные племена (1.0.010)

## Бесконечная процедурная карта и нейтральные племена (1.0.010)
- **Практически бесконечный мир**: WORLD в config.ts = **48000×48000** (мягкий предел клампа, игрок
  его не достигает). Стартовые базы вынесены в константы **HOME=(24000,26200)** (игрок) и
  **RIVAL=(27200,23400)** (ИИ-соперник) — близко к центру огромного мира. Координаты 470,2330 убраны
  изо всех мест (centerOn/H/ageUp burst/launchWave fallback/dirt рендер) и заменены на HOME/RIVAL.
- **src/game/terrain.ts переписан на бесконечный координатный шум**: целочисленный хэш решётки
  `hash2(ix,iz,seed)` (работает и для отрицательных координат) → value-noise `valueNoise`/`fbm`
  БЕЗ нормировки на размер карты (старый шум был конечен). Поля: elev (fbm 720, 5 октав), moist
  (620), temp (1500), river (две узкие долины-«гребня» v≈0.5/v≈0.55). Классы `TerrainClass`:
  deep/water/sand/field/grass/forest/desert/hill/mountain. `classAt`, `isLand` (суша, не горы/вода),
  `isBuildable` (ровная суша: grass/field/forest/sand), `chunkRand(cx,cz)` — детерминированный ГПСЧ
  чанка (контент одинаков при повторном визите, восстановлении). addSafe(r=360 у обеих баз).
- **Биомы (пороги в rawClass)**: горы e>0.78, холмы e>0.68; вода — низины e<0.13 или долины рек
  (r>0.62), глубокая e<0.07/r>0.85; песок-берег e<0.18; пустыня (temp>0.64 && moist<0.44); лес
  (moist>0.66); поля (e>0.58 && moist<0.52). Замер баланса (5 сидов): суша ~82%, вода ~18%
  (deep ~7 + water ~10-11), лес ~8-10%, поля ~7%, холмы ~7-8%, пустыня ~3-5%, горы ~1%.
- **Новые тайлы в iso.ts** (процедурные на холсте, в стиле существующих, кэш в Map): `getDeepWaterTile`
  (тёмно-синий ромб с рябью), `getSandTile` (песок с крапинками), `getDesertTile` (охра с дюнами-дугами),
  `getFieldTile` (зелёно-жёлтые грядки с бороздами), `getForestFloorTile` (тёмный лесной пол),
  `getMountainTile` (скальные пики треугольниками со снежными шапками и тенью обрыва — высота только
  шейдингом ВНУТРИ ромба, боковых граней нет, чтобы не ломать depth-сортировку). Рендер земли — switch
  по classAt; хэш варианта тайла считаем из мировых координат (может быть отрицательным: `& 0xFFFF`).
- **Ленивая чанковая генерация контента**: `Game.CHUNK=1600`, `chunksGen:Set<string>`, `ensureChunks(wx,wy,R)`
  (круг чанков в радиусе R от точки), `genChunk(cx,cz)` — по chunkRand: лесные кластеры деревьев
  (густо в 'forest', редкие рощи в 'grass'), золото-жилы (горы/холмы/пустыни), ягоды (поля/трава),
  стаи животных (wolf .28 [2-3], deer .34 [3-4], sheep .26 [3-5], cow .18 [2-3]), декор на суше,
  редкие реликвии (6%). Чанки у стартовых баз (distHome < C*0.9) зарезервированы — стартовый контент
  ставится вручную в genWorld. Дозагрузка в update: каждые 0.4с ensureChunks вокруг камеры R=2.
  genWorld теперь ставит только базы/стартовых юнитов/ресурсы и пред-генерит чанки вокруг баз.
- **Нейтральные племена**: в genChunk с шансом ~10% в чанках дальше C*3.2 от баз вызывается
  `spawnTribeCamp(x,y,rng)` — башня (`addBld('tower','neutral')`, флаг `b.tribe=true`) + 2-4 воина
  (spearman/swordsman/archer, owner='neutral', флаги `u.tribe=true, u.aggro=false, homeX/Y=лагерь`)
  + клад золота. Дипломатия: `hostile()` считает neutral враждебным, но авто-целеполагание
  (`acquireEnemy`) и стрельба башен ПРОПУСКАЮТ воинов/постройки племени пока `!aggro` — племя пассивно.
  При ударе по воину (`damageUnit`) или постройке (`damageBld`) племени вызывается
  `provokeTribe(x,y,by)`: все воины племени в радиусе ~360 получают `aggro=true` и идут на обидчика
  (attackmove). Башня племени стреляет только когда `tribeAggro(b.x,b.y)`. Воины племени в updateUnits
  идут через `updateSoldier` (не через animal/wolf), без цели возвращаются к лагерю (homeX/Y, поводок
  120px). Волки/скот племя не трогают (пропуск owner==='neutral'). Миникарта красит племя охрой #e0b050.
- Миникарта переведена на **региональную** модель (мир бесконечный): показывает RANGE=4200 world-ед.
  вокруг камеры (центр карты = камера), террейн сэмплируется сеткой 150 в этой области, объекты
  фильтруются по inRange, рамка вьюпорта в центре; `minimapJump` прыгает относительно камеры.

## Карта и террейн (1.0.009)
- **WORLD** в config.ts увеличен в ~2 раза по площади (коэф. √2): было 3600×2800 → стало **5090×3960**.
  Базы: игрок P=(470,2330), соперник E=(3140,560). Эти же координаты продублированы в рендере земли
  (dirt под базой) и в центре камеры (`centerOn`, клавиша H, burst при age-up, launchWave fallback).
- **src/game/terrain.ts** — детерминированный шумовой террейн (mulberry32 + value-noise): озёра (моисть в
  низинах), 2 извилистые реки (sin-меандры вдоль мировой Y), холмы (elevation > 0.80). Сид — случайный
  на новую игру. Класс тайла: `terrain.classAt(wx,wy)` → 'water'|'hill'|'grass'; `isLand()` — для спавна.
  Зоны баз добавляются через `terrain.addSafe(x,y,r)` (суша, без рек/холмов).
- Тайлы в iso.ts: `getWaterTile(variant)` (синий ромб с бликами-рябью), `getHillTile(variant)` (каменистая
  площадка, светлая верхняя кромка-гребень + тёмный обрыв у нижней). Выбор тайла в рендере земли
  engine.ts по классу террейна (центр клетки wx+16,wy+16). Вода/холмы — **чисто визуальные**, на
  проходимость не влияют.
- Миникарта рисует воду (#3b82f6) и холмы (#8a8266) сеткой 90 world-единиц поверх зелёной заливки.
- **Дикие животные — случайный разброс**: helper `spawnPack(kind, packs, [min,max])` ставит стада/стаи в
  случайных точках суши (randLand, ищет точку на суше вне 420px от баз): волки 12 стай ×2-3, овцы 8 ×3-5,
  коровы 6 ×2-3, олени 8 ×3-4. Больше никаких фиксированных массивов packs/herds.
- Ресурсы: стартовые у баз — как раньше (дуга леса, золото/ягоды); остальные — леса вдоль линии между
  базами + 22 случайные рощи, золотой «спор» в центре + 16 случайных точек золото/ягоды на суше;
  декор (трава/цветы) 420→760 и только на суше; вражеских лагерей-башен 2→3, реликвий в разброс 3→4.
- Баланс террейна (замер на сидах): вода ~7-13% (озёра 4-11% + реки ~3-4%), холмы ~6-10%, остальное суша.
Правило версий: при каждом обновлении поднимаем ТРЕТЬЮ цифру на 1 —
`1.0.001 → 1.0.002 → 1.0.003 …`. Показывается в главном меню в левом нижнем углу
(мелкий полупрозрачный белый текст). В `package.json` поле `version` остаётся валидным
semver `1.0.0`, а реальная версия игры продублирована в `_gameVersion`.

---

## Технологический стек и сборка

- **Vite + React + TypeScript + Tailwind**, рендер игры — на `<canvas>` 2D (без движка).
- UI (меню, HUD, док, модалки) — React-компоненты в `src/App.tsx`; игровая логика — классы.
- Сборка: `npm run build` → `dist/index.html` (~2.5 КБ) + `dist/assets/`: CSS (81 КБ),
  чанки первого рендера `index`/`vendor-react`/`vendor-ui` (~0.39 МБ, gzip ~0.12 МБ),
  чанк движка `engine` (0.35 МБ, gzip ~0.12 МБ) — грузится ЛЕНИВО, со стартом партии
  (1.0.128), ~240 спрайтов файлами (~10 МБ) + `dist/voices/`.
  Одним HTML (14 МБ) — `npm run build:singlefile` (`SINGLE_FILE=1`), см. раздел 1.0.126.
- Проверка типов: `./node_modules/.bin/tsc --noEmit` (после `npm install` — node_modules не персистятся между сессиями).
- Деплой — **любой статический/shared хостинг**: залить содержимое `dist/` в `public_html/` (или аналог).
  Бэкенда/БД/Node на сервере НЕ нужно. Сохранения — в `localStorage`.
  Единственная внешняя зависимость — шрифт Playfair Display с Google Fonts (при офлайне — системный фолбэк).

### Команды
```bash
npm install            # если tsc/vite не найдены
./node_modules/.bin/tsc --noEmit   # проверка типов
npm run build          # прод-сборка в dist/ (чанками)
npm run build:singlefile  # то же, но одним HTML (SINGLE_FILE=1)
npx http-server dist -p 8080 -c-1  # локальный предпросмотр
```

---

## Структура кода

| Файл | Назначение |
|---|---|
| `src/App.tsx` | Весь React-UI: меню, HUD, нижний док (войска/стройка), карточки выделения, пауза, настройки, дерево технологий, экран итогов. Здесь `GAME_VERSION`. |
| `src/game/engine.ts` | Класс `Game` — главный движок: мир, камера, ввод, рендер изо, юниты/здания, ИИ, волны, дипломатия, экономика, сохранения. ~3400 строк. |
| `src/game/config.ts` | `BUILDING_DEFS`, `UNIT_DEFS`, `AGES`, `DIFF`, `TECHS`, `WORLD`, настройки. |
| `src/game/iso.ts` | Изометрия: `toIso/fromIso`, `TILE_STEP=32`, ромб 64×32, тайлы травы/земли. |
| `src/game/pixelart.ts` | Процедурная отрисовка юнитов (`drawPixelUnit`), стройки, кольца выделения, тени. |
| `src/game/sprite-art.ts` | `SPR_ANCHORS` (якоря посадки спрайтов зданий) и `UNIT_ANCHORS` (кадры юнитов). |
| `src/game/audio.ts` | Класс `SoundBank`: синтез SFX (WebAudio) + голоса/боевой звук из mp3. |
| `src/assets/sprites/` | PNG-спрайты зданий и `units/` (юниты + кадры `_w` ходьбы). Прозрачные, h≈150px. |
| `public/voices/` | mp3-фразы юнитов (имена файлов = фразы) и `battle-sword-fight.mp3`. |

### Ключевые константы и ограничения (НЕ ломать — см. CONTRIBUTING.md)
- `TILE_STEP = 32`, изо-ромб клетки 64×32.
- Лимит частиц **650** (пыль из-под ног — отдельный лимит <620).
- Куллинг объектов через `inView()`; тайлы кэшируются (`Map`).
- Спрайты лёгкие: после даунскейла высота ~100–150px; ассеты ужаты.
- **TTS/Web Speech запрещены полностью** — озвучка только готовыми mp3 из `public/voices/`.

---

## Арт-пайплайн спрайтов

1. Генерация через инструмент изображений (gpt image) на **белом фоне**, пиксель-арт, изо 2:1.
2. Сырой файл → `raw/` (в .gitignore).
3. Пост-обработка скриптом `scripts/proc.cjs` (в .gitignore, нативный node+zlib):
   удаление белого фона flood-fill от краёв → обрезка по альфе → box-downscale до h=150
   → слабая чистка бахромы → энкод PNG.
   ```bash
   node scripts/proc.cjs raw/вход.png src/assets/sprites/выход.png 150
   ```
4. Прописать импорт в `engine.ts`, при необходимости якорь в `sprite-art.ts`.
5. Проверка тайлинга стен — оффскрин-композит `scripts/tile-test.cjs` (в .gitignore).

---

## Стены и ворота (важно)

- Спрайты: `wall.png` (**чистая каменная кладка, БЕЗ дверей/арок**), `gate.png` (ворота с дверью),
  `wall_corner.png` (угловой бастион-столб).
- **Протяжка**: клавиши **B** (стена) / **V** (ворота) → зажать и тянуть; сегменты ставятся
  вдоль доминирующей изо-оси с привязкой к клеткам (`TILE_STEP`).
- Один спрайт на объект; **вторая диагональ рисуется зеркально** по X (`ctx.scale(-1,1)`)
  в `drawWallSprite()` — отдельные `wall_b/gate_b` НЕ нужны (были, удалены как неверно ориентированные).
- **Угол**: `isWallCorner(b)` детектит соседа и вдоль мировой X, и вдоль Y (в пределах клетки) →
  на этой клетке рисуется `wall_corner.png` (зубчатый квадратный столб, масштаб ~0.9, без флипа).
- **Ворота**: свои юниты проходят, враги — стопорятся (коллизии в `updateUnits`).
- Замена участка стены на ворота: выделить стену → кнопка «🚪 Ворота» (`buildGateOnWall()`).
- Снос строения: выделить своё здание → «⛏ Снести» или **Delete/Backspace** (`demolish()`,
  возврат части ресурсов; ГЦ снести нельзя).

---

## Анимации крестьянина (кадры работы и направление ходьбы)

Спрайты в `src/assets/sprites/units/`, якоря — в `UNIT_ANCHORS` (`sprite-art.ts`),
выбор кадра — в `drawUnitSprite` (`pixelart.ts`), состояние — в `updateVillager` (`engine.ts`).

**Кадры работы** (вид сбоку, как базовый `villager.png`; тот же персонаж): пары [замах, удар],
ключ кадра = имя файла (якорь в UNIT_ANCHORS по этому имени):
- `vchop1/vchop2` — рубка леса топором;
- `vmine1/vmine2` — кирка (золото/руда; также стройка — удары инструментом);
- `vgather1/vgather2` — сбор фруктов/урожая (тянется к ветке / кладёт яблоки в корзину;
  используется и для фермы).

Механика: у юнита есть `wkind: 'chop'|'mine'|'gather'` и `wphase: 0..1` (фаза цикла ~0.55с).
`wkind` ставится в `updateVillager` по типу ресурса (`n.kind`: wood→chop, gold→mine, food→gather;
стройка→mine; ферма→gather). В `drawUnitSprite` при `state==='gather'|'build'` и отсутствии шага
кадр выбирается по `wkind`/`wphase` (замах при `wphase<0.62`, удар — после), с лёгким приседом и
выпадом в сторону ресурса (flip по `face`).

**Направленная ходьба в изометрии** (требование: вверх — спина, вниз — спереди), 4 кадра на цикл:
- сбоку: `vsw1..vsw3` (+vsw2 как 4-я фаза) в массиве `VILL_WALK_SIDE`; отражается по `face`;
- спереди (на камеру/«вниз» по экрану): `vfw1..vfw4` (`VILL_WALK_FRONT`);
- со спины (от камеры/«вверх» по экрану): `vbw1..vbw4` (`VILL_WALK_BACK`; vbw3/vbw4 — зеркало vbw1/vbw2,
  полученное скриптом, т.к. у вида сзади смена ноги = зеркало).
- фазы цикла: 1 контакт, 2 пронос внизу, 3 контакт, 4 вынос/пронос.
Механика: в цикле апдейта юнита мировая дельта перемещения переводится в изо-экранную
(`toIso`: sx=dwx−dwy вправо, sy=(dwx+dwy)/2 вниз к камере), сглаживается (`mvx/mvy`), и
`fmode`: sy доминирует → `1` (спереди, вниз) или `2` (спина, вверх); иначе `0` (сбоку).
В `drawUnitSprite` при шаге крестьянина кадр берётся из 4-фазного цикла:
`idx = floor(((anim/2π)%1)*4)`; фаза шага и вертикальный подскок идут от `u.anim`, на готовых
кадрах бобовой анимации крен не накладывается (`upright = working || (villager && move)`).
Якоря ходьбы выровнены по центру корпуса (шляпа/туловище), а не ступней — иначе проходные кадры
с вынесенной ногой давали горизонтальный дрейф.
Поле на юните: `fmode?: 0|1|2`. Старые `villager.png`/`villager_w.png` (с топором) для ходьбы больше
не используются; покой крестьянина — боевой кадр `villager`.
Открытая доработка: боковой контакт-3 (vsw3) почти совпал с vsw1 (та же нога вперёд); при
возможности перегенерировать настоящий зеркальный контакт с другой ногой, а не отражать (отражение
бокового кадра разворачивает лицо).

### Ополченец (swordsman) — рубка и направленная ходьба
- Бой: базовый `swordsman.png` (меч поднят — замах/готовность) и `sslash.png` (рубка сверху-вниз с
  траекторией и выпадом). В `drawUnitSprite` кадр `SW_SLASH` показывается, когда юнит на месте и
  `atkAnim > 0.45` (фаза удара); иначе базовый кадр. Выпад — общий `atkAnim*lunge*face`.
- Ходьба 4 фазы по изо-направлению:
  - сбоку `SW_WALK_SIDE` = [`swordsman_w`, `swordsman_walk2`, `ssw3` (новый контакт), `swordsman_walk2`];
    отражается по `face`. Старые `swordsman_w`/`swordsman_walk2` (меч опущен, маршируют) переиспользованы.
  - спереди (на камеру) `SW_WALK_FRONT` = `sfw1..sfw4`; со спины (от камеры) `SW_WALK_BACK` = `sbw1..sbw4`.
- `fmode` теперь считается в движке для ВСЕХ юнитов на ходу (не только крестьянина), но кадры
  фронт/тыл заведены только у villager и swordsman; остальные используют боковые кадры по `face`.
- Якоря фронт/тыл — по центру шлема/головы (стабильная вертикаль), а не ступней (щит/меч уводят центр).

### Лучник (archer) — стрельба и направленная ходьба
- Бой: `aaim.png` (лук натянут, стрела на тетиве — прицеливание) и `arelease.png` (тетива отпущена, стрела
  срывается). В движке у юнита есть `aiming?: boolean`: в `updateSoldier` он выставляется, когда цель в зоне
  выстрела (ветки tu/tb у лучника, кроме катапульты). В `drawUnitSprite`: `arelease` при `atkAnim>0.5`
  (момент выстрела), иначе `aaim` пока `aiming`; при выстреле наносится урон сразу (снаряд летит от спрайта).
- Ходьба 4 фазы: спереди (на камеру) `afw1..afw4`; со спины (от камеры) `abw1..abw4` (abw3/abw4 — зеркало
  abw1/abw2, т.к. вид сзади при смене ноги симметричен); сбоку — переиспользованы существующие 2 кадра марша
  `archer`/`archer_w` (чередуются). Новые боковые кадры-дубли (asw3/asw4) генератор вернул идентичными базовым
  и были удалены.
- Якоря всех кадров лучника — по центру спрайта (W/2), как у существующих archer (56.5)/archer_w (54),
  чтобы не было сдвига между позами (лук/колчан уводят центр масс).
- `fmode`/направленная ходьба теперь поддерживается у villager, swordsman, archer; остальные воины — бок.

### Копейщик (spearman) — направленная ходьба
- Бой: отдельного кадра укола нет — базовый `spearman.png` уже боевая стойка с горизонтальным копьём;
  выпад/укол передаётся штатным лунгом `atkAnim*lunge*face`. Пробный кадр `spthrust` совпал с базой и удалён.
- На марше копьё держится ВЕРТИКАЛЬНО (новые кадры): сбоку `spsw1..spsw3`(+spsw2), спереди `spfw1..spfw4`,
  со спины `spbw1..spbw4` (spbw3/4 — зеркало spbw1/2). В бою копейщик переходит на горизонтальное копьё.
- **Важно:** штатные `spearman`/`spearman_w` НЕ h=150, а 78/104 (низкие широкие кадры, `UNIT_TARGET_H=50`).
  Новые марширующие кадры обработаны до **h=104** (как `spearman_w`), `ay=103`; иначе фигура вышла бы мельче.
- Якоря новых кадров — по медиане центра туловища (зона 36–78% высоты): копьё вертикально уходит вверх и
  сбивает центр по голове, а древко/щит — по бокам; медиана торса даёт стабильную вертикаль без дрейфа.

### Конница (рыцарь knight / всадник cavalry) — конь тоже разворачивается
Всадник+конь — один спрайт. Боковые кадры (галоп в профиль с отражением по `face`) остаются штатными:
knight/knight_w, cavalry/cavalry_w. Дополнительно сделан разворот коня:
- на камеру (`fmode=1`, «вниз»): рыцарь `kfw1/kfw2`, всадник `cfw1/cfw2` — конь мордой/грудью к зрителю,
  рыцарь пикой на камеру, всадник с саблей; 2 фазы галопа разложены на 4 ячейки цикла;
- от камеры (`fmode=2`, «вверх»): `kbw1/kbw2`, `cbw1/cbw2` — конь крупом/хвостом, спина всадника.
- В `drawUnitSprite` ветка `isMounted && move && fmode 1|2` берёт `KN_WALK_FRONT/BACK` или `CV_WALK_*`,
  `flip=1`, боковой крен не накладывается (`mountFB`), вертикальный подскок галопа — амплитуда 6.
- Высота кадров: рыцарь h=128 (как `knight`/`knight_w`), всадник h=134 (как `cavalry_w`); цель рендера 60.
- **Генерация:** вид спереди/сзади НЕ получается, если передавать боковой спрайт-конь референсом (модель
  держит боковую композицию). Нужен жёсткий текстовый промпт без сильного бокового референса: «warhorse seen
  from directly in FRONT/BEHIND — face/muzzle or rump/tail toward viewer, NOT a side profile».
- Боковой галоп конницы — по-прежнему 2 кадра (штатные). Катапульта направленного разворота не имеет.

### Монах и волк — разворот на/от камеры (1.0.008)
Та же схема, что у конницы, добавлена для **монаха** (пешком, шаг) и **волка** (галоп):
- монах `mfw1/mfw2` (лицо, борода, посох с крестом) / `mbw1/mbw2` (тонзура, спина рясы) — h=150, рендер 44;
- волк `wfw1/wfw2` (морда на камеру, 2 фазы галопа) / `wbw1` (широкий круп с растопыренными задними
  ногами и хвостом); `wbw2 = wbw1` (зад симметричен, вторая фаза не понадобилась) — h=91, рендер 34.
- В `drawUnitSprite` ветка разворота обобщена: `fbUnit = isMounted || isMonk || isWolf`, таблица циклов
  `FB[key] = {f,b}`; flip=1, без бокового крена; подскок — монах мягкий (шаг, 3), волк галопный (6).
- **Волк — нейтрал** (`owner==='neutral'`), он идёт мимо общего расчёта fmode. Направление ему задано
  прямо в neutral-ветке `updateUnits` (engine.ts) по дельте перемещения: `fmode=1` на камеру, `2` от,
  `0` бок. Остальные животные (овца/корова/олень) fmode не получают.
- **Обработка:** монах — тонкая вертикальная фигура со штрихами рясы; box-average даунскейл process()
  рвёт её в вертикальные полосы (доля непрозрачного <0.35 в колонках → прозрачность). Для монаха сделан
  nearest-neighbor даунскейл (скретч `scripts/_mfix.cjs`-логика): солидные пиксели без муара.
  Волк широкий/плотный — обычный process() box-average.
- Тыл волка генерить с пометкой «BROAD wide rump, hind legs spread WIDE, fill frame horizontally», иначе
  модель даёт узкую вертикальную щепку (круп ~38px против анфаса ~77px).

Известный нюанс (можно доработать): переносимый груз (вязанка дров/еда/золото на плече)
рисуется только в процедурной ветке `drawHumanoid`, а у спрайтовых крестьян не показывается —
это поведение эпохи спрайтов, к анимациям работы не относится.

---

## Звук

- Голосовые реплики — mp3 из `public/voices/`, карта фраз в `audio.ts` (`PHRASES`/`CLIP_FILES`).
  Казахская озвучка расы — `public/voices/kaz-voises/` (`KZ_CLIPS`/`KZ_PHRASES`, 1.0.015),
  при отсутствии файла фраза молча пропускается (и откат на русскую запись).
  Имена файлов кириллические, берутся через `encodeURIComponent` (NFD-фолбэк в `playUrl`).
- **Боевой звук** `battle-sword-fight.mp3` (`SoundBank.battleClash()`): играет в рукопашной
  (`strike()`), старт со **случайной секунды** трека (5–40с), короткий фрагмент 2–4с,
  не больше одного одновременно, гейт ~0.9с.

---

## Git / рабочий процесс

- Рабочая ветка сессии: **у каждой сессии своя**, вида `arena/<id>-strategy-browser-game`
  (первая была `arena/01a06db0-…`, текущая — `arena/01a080b2-…`) — коммитим сюда,
  по ней Arena отслеживает сессию. Актуальное имя брать из `git branch --show-current`,
  а не копировать из этого файла.
- **По приказу владельца проекта актуальное состояние ВСЕГДА зеркалируется и в `main`.**
  После коммита в сессионную ветку пушить тот же HEAD и в main:
  ```bash
  git push origin "HEAD:refs/heads/$(git branch --show-current)"
  git push --force-with-lease origin HEAD:refs/heads/main   # или origin HEAD:main
  ```
  `main` на GitHub был одиночным коммитом-загрузкой через веб (orphan-история), поэтому
  первая синхронизация — force-push (истории не связаны). Дальше можно fast-forward,
  но сессионная ветка — источник истины, при расхождении её и приводим к main.
- Коммиты — conventional commits на русском (`feat:`, `fix:` …).

### Уведомления о сдаче задачи (ntfy.sh) — как правильно пушить

**Ничего дополнительно делать не нужно: уведомление отправляется САМО на каждый пуш.**
Достаточно обычной пары команд выше — GitHub сам дёрнет вебхук.

Тема: **`ArenaAI-khanate-db24`** → приложение ntfy или https://ntfy.sh/ArenaAI-khanate-db24
(с 2026-09-10, раньше была `ArenaAI` — почему сменили, см. «Диагностика» ниже).

Как это устроено и почему именно так:

- Отправляет **webhook репозитория** (Settings → Webhooks), а НЕ GitHub Actions и не `curl`
  из песочницы. Доставку выполняют серверы GitHub, поэтому сетевые ограничения агента
  роли не играют.
- Приоритет зависит от ветки, чтобы телефон не звенел на каждый промежуточный пуш:

  | что запушено | приоритет | как приходит |
  |---|---|---|
  | `main` | 4 | со звуком — **это и есть сдача задачи** |
  | любая ветка, первая строка коммита начинается с `1.0.` (релиз пункта плана) | 3 | со звуком |
  | остальные коммиты в `arena/…` | 1 | тихо, без звонка |

  Отсюда два правила:
  1. **Коммит, закрывающий пункт плана, называется `1.0.NNN: что сделано`** — именно
     префикс версии делает уведомление громким. Промежуточные коммиты («wip», «правки»)
     — без префикса, они приходят тихо.
  2. **Обязательно обновлять ОБЕ ветки: текущую сессионную и `main` — ПРЯМЫМ ПУШЕМ,
     без PR** (изменено в 1.0.126; старая формулировка была «main через PR»).
     После каждого релиз-коммита: `git push origin <сессионная-ветка>`, затем
     `git push origin main`. PR не открывать: они плодят review-ветки, которые
     потом расходятся с сессионной, и дублируют уведомления. Входящие PR
     закрываются автоматически (workflow в `ci/no-pr-workflow.yml`).
     Сессионная ветка перед релизным коммитом должна догонять `main`
     (`git merge --ff-only origin/main`), иначе её база устареет — см. 1.0.126.
     Если пуш в `main` невозможен (права, конфликт) — сказать об этом пользователю
     в том же ходе, а не молча оставлять `main` позади.
- Текст уведомления собирает **сам ntfy** из JSON вебхука через Go-шаблон (`tpl=yes`):
  заголовок «Задача сдана — Казахское Ханство», тело — тема коммита, автор и ветка,
  клик открывает diff (`.compare`). Без шаблона ntfy показывал бы сырой пейлоад GitHub
  на 7.7 КБ вложением-«кашей».
- Следствие для формата коммитов: **первая строка сообщения — это заголовок пуша в телефоне**,
  поэтому она должна быть самодостаточной («что сделано»), а не «правки» или «фикс».

Диагностика, если уведомление не пришло (всё через `gh`, хук `676340808`):

```bash
# были ли доставки и что ответил ntfy (нужен HTTP 200)
gh api repos/DiasMazhenov/strategy-browser-game/hooks/676340808/deliveries \
  --jq '.[]|"\(.event) → HTTP \(.status_code) \(.status)  \(.delivered_at)"' | head

# что именно увидел телефон (заголовок/приоритет/текст)
gh api repos/DiasMazhenov/strategy-browser-game/hooks/676340808/deliveries/<ID> \
  --jq '.response.payload'

# проверочная доставка без коммита
gh api repos/DiasMazhenov/strategy-browser-game/hooks/676340808/pings -X POST
```

Пересоздать/поменять URL хука (тема, тексты, приоритеты) — URL собирать **скриптом с
`urlencode`**, руками не писать: в нём кириллица и фигурные скобки шаблона.

```python
from urllib.parse import urlencode
topic = 'ArenaAI-khanate-db24'
title = 'Задача сдана — Казахское Ханство'
message = ('{{with .head_commit}}{{.message}}\n\nАвтор: {{.author.name}}{{end}}\n'
           'Ветка: {{trimPrefix "refs/heads/" (printf "%v" .ref)}}')
priority = ('{{if eq (printf "%v" .ref) "refs/heads/main"}}4'
            '{{else if hasPrefix "1.0." (printf "%v" .head_commit.message)}}3{{else}}1{{end}}')
q = urlencode({'tpl': 'yes', 't': title, 'm': message, 'p': priority,
               'click': '{{printf "%v" .compare}}',
               'tags': 'white_check_mark,video_game'})
print(f'https://ntfy.sh/{topic}?' + q)
```

```bash
gh api repos/DiasMazhenov/strategy-browser-game/hooks/676340808 -X PATCH \
  -f "config[url]=$URL" -f "config[content_type]=json"
```

**Диагностика «не приходят на телефон» (2026-09-10, итог): цепь GitHub→ntfy исправна,
виновата подписка телефона; тема сменена на уникальную `ArenaAI-khanate-db24`.**

Проверено по шагам:

1. Все доставки вебхука — HTTP 200 (десятки пушей за два дня, `status: OK`). GitHub
   доставляет, ntfy принимает: в `.response.payload` каждой доставки — JSON публикации
   с `id` (например `nMXX3Knmk0Ab`), т.е. ntfy реально опубликовал сообщение в тему.
   Вывод: с серверной стороны всё работает, «не приходит» — проблема на телефоне
   (не та тема в подписке — тема **регистрозависима**, подписка слетела, или приложение
   убито оптимизацией батареи).
2. Расшифровка `config.url` (`urllib.parse.unquote_plus`): тема `ArenaAI`, `tpl=yes`,
   шаблон `p` = `main→4` / `hasPrefix "1.0." →3` / остальное `→1`, `tags`, `click` — всё на месте.
3. Ping (`…/pings -X POST`) без коммита — GitHub 200, ntfy 200: `topic: ArenaAI`,
   `title` и `tags` отрендерились, `message` = «Ветка: <nil>» и `priority: 1` — это
   **норма для ping** (в ping-пейлоаде нет `.ref` и `.head_commit`, срабатывает else-ветка).
4. Так как обе стороны отвечали 200, тема сменена на уникальную
   **`ArenaAI-khanate-db24`** (PATCH тем же скриптом `urlencode`, поменялся только путь
   темы) — уникальная тема исключает чужие записи/опечатки в подписке. Повторный ping —
   GitHub 200, ntfy 200, `topic: ArenaAI-khanate-db24`, id `yZyPRuSyBb1g`.
   **Пользователю: подписаться на https://ntfy.sh/ArenaAI-khanate-db24 (точно в этом
   регистре), включить instant delivery / разрешить приложению работать в фоне;
   старую подписку `ArenaAI` удалить.**
5. **Не merge'ить PR до конца работы:** merge PR из сессионной ветки в `main` закрывает
   сессию Arena — поэтому merge делаем САМЫМ ПОСЛЕДНИМ действием задачи, после него
   агенту в сессию писать больше нельзя.

Попутно найдено (не блокирует доставку, пока оставлено как есть): **ntfy при `tpl=yes`
рендерит только `message`, `title` и `priority`** (проверено по исходникам ntfy,
`server/server_template.go` → `renderTemplateFromParams`), поле `click` шаблоном НЕ
обрабатывается — в телефон уходит сырая строка `{{printf "%v" .compare}}`, тап по
уведомлению открывает битый URL. Если захочется починить — просто убрать параметр
`click` из URL хука.

**Грабли (проверено, не повторять):**

- **jq калечит ID доставок** (float64): печатает `…31600` вместо `…31648`, и GET по
  такому ID даёт 404. Точный ID брать через Python (`json.load`) или `--jq '.[0].id'`
  с последующим исправлением — надёжнее Python. Ответ ntfy в доставке появляется
  не мгновенно: если `response` пуст — подождать пару секунд и перечитать.

- **`curl` на ntfy.sh из песочницы агента не работает** — исходящий трафик по белому списку:
  200 отдают только `github.com`, `api.github.com` и `registry.npmjs.org`; `ntfy.sh`,
  `example.com`, `google.com` — обрыв TLS (`SSL_ERROR_SYSCALL`), и по IPv4, и по IPv6,
  и по HTTP. Дело не в ntfy, а в allowlist.
- **Пуш файла в `.github/workflows/` отклоняется**: `refusing to allow a GitHub App to create
  or update workflow … without workflows permission`. Через Contents API — 403
  `Resource not accessible by integration`. Поэтому вариант с GitHub Actions отпал;
  локальная `.github/` добавлена в `.gitignore`, чтобы случайно не сломать пуш.
  Заготовка workflow лежит в `ci/notify-workflow.yml` — она НЕ нужна, пригодится
  только если захочется слать уведомление после прогона тестов, а не по факту пуша.
- **Права на вебхуки у токена есть** (`hooks` POST/PATCH проходят) — этим и пользуемся.
- `jq -rn --arg t "$T" '"…" + (@uri $t)'` — **синтаксическая ошибка**: `@uri` так не
  применяется к переменной. URL собираем на Python (`urlencode`), а не на `jq`.
- Тело JSON для ntfy (когда шлём не вебхуком, а сами) — только через `jq -n --arg`,
  и **никогда** заголовками `X-Title`: кириллица в заголовке уезжает в RFC 2047 кракозябрами.
- **Версионируемая dev-тулза** (лежит в `scripts/`, коммитится): `process-art.cjs` (обработка
  спрайтов), `build-sprites.cjs`, `extract-sprites.cjs`, `iso-check.cjs`,
  `pixelart-preview.cjs`, `walk-sheet.cjs`, `walk-strip.cjs`.
- **Не коммитить** скретч (в `.gitignore`): `raw/`, `plan.md`, `tile-test.png`,
  `scripts/proc.cjs` (одноразовый обработчик), `scripts/tile-test.cjs` (одноразовый композит),
  `dist/`, `node_modules/`.
  Внимание: в `.gitignore` нет inline-комментариев после паттерна (git их не поддерживает —
  комментарий только с отдельной строки, иначе ломается паттерн).
- Мёртвые ассеты не возвращать: `wall_b.png`/`gate_b.png` (неверно ориентированные диагональные
  спрайты) — вторая диагональ рисуется зеркально, эти файлы не нужны.
- История сессии: база `6e67394` → перевод на русский `113493f` → спрайты/стены `5cc7abb` →
  снос/ворота/угол/боевой звук `09b4e3a` → версия в меню `ac6e956` → context.md+1.0.001 `57b8006` → …

---

## Бэклог / открытые идеи
- Угол сейчас автоопределяется по соседним сегментам; поворот **в одной протяжке** одной линией (залом) — не сделан.
- Ранее предлагалось: случайные события (чума/урожай/караван), ачивки и баннеры, погодные эффекты.
- Опционально: self-host шрифта для полностью офлайн-версии (сейчас Google Fonts).

---

## 1.0.023 — изометрическая ГЕКСАГОНАЛЬНАЯ карта
- Карта переведена с ромбов (квадратная решётка 64×32, TILE_STEP=32) на **изо-гексы**
  (flat-top, 2:1): аксиальная решётка (q,r), центр гекса в изо-экране `ix=26q, iy=HQY·q+HY·r`
  (`HQX=26, HY=16√3≈27.71, HQY≈13.86`). Вершины гекса — Вороной-ячейка решётки:
  `HEX_PTS = [(-9.31,-HQY),(9.31,-HQY),(16.69,0),(9.31,HQY),(-9.31,HQY),(-16.69,0)]`
  (каждое ребро общее с одним из 6 соседей; площадь гекса = площади ячейки решётки 720.5).
- **iso.ts**: `hexCenter/screenToHex/hexRound (cube)/hexPath`, `getHexTile(kind,variant)` —
  канвас 46×38: плотная база `BASE[kind]` по hexPath → AI-текстура (96² из
  `assets/sprites/terrain/hex/*.png`, джиттер ±6/±4, clip по гексу, кэш Map по
  `kind:(v%4):ready`, сброс ':0' по onload) → тинт `TINT` (dgrass/forest/mountain/hill)
  → процедурная фактура леса/скал/холмов → свет верхних граней/тень нижних.
  Старые getGrassTile/…/diamondPath удалены.
- **terrain.ts**: `reliefGridHex(wx0,wy0,wx1,wy1,pad=8)` — аксиальная сетка, rawRelief по
  центрам гексов, Chamfer distance-transform по 6 соседям в 2 прохода (перепад соседей ≤1,
  проверено численно); отдаёт `{q0,q1,r0,r1,worldAt,at,hAtWorld}` (hAtWorld — cube rounding
  мировой точки → ступень для объектов/юнитов/декора).
- **engine.ts**: земля рисуется циклом r(внешний)→q(внутренний) (painter's order), класс
  биома в центре гекса, drawImage со сдвигом -up; `drawCliffsHex` — обрывы по ТРЁМ передним
  рёбрам: BL→BR (сосед r+1, самая тёмная), R→BR (q+1), L→BL (q−1,r+1) — пласты каждые 4px
  + тёмная подошва. Туман войны и призраки построек — тоже hexPath. Вершины/курганы
  (TERRAIN_FEATURES) — локальные максимумы по 6 соседям (snow lvl≥5 / rock lvl≥4 /
  hill lvl≥2). Игровая логика осталась в непрерывных мировых координатах — гекс только визуал.
- **Текстуры**: 10 gpt-image исходников 1024² в `raw/hex_*.png` (в .gitignore) →
  `scripts/process-hex.cjs` (box-average даунскейл) → лёгкие 96² PNG в
  `assets/sprites/terrain/hex/`: grass, dirt, water, deep, sand, desert, field, forest,
  mountain, hill. Плотная подложка базового цвета под текстурой ⇒ швов/щелей нет.
- TILE_STEP=32 оставлен ТОЛЬКО как шаг привязки стен (логика стен не тронута).

---

## 1.0.024 — канонический изо-шестиугольник (вершина вверх, вертикальные бока)
- Прошлая форма гекса была сплющенным ПЛОСКИМ гексом с горизонтальными гранями (плечи ~62°),
  она не лежала на изо-осях. Переделано на канонический изо-гекс (Unity/Godot isometric hex):
  верхняя и нижняя ВЕРШИНЫ, плечи строго по изо-диагоналям 2:1 (уклон 1/2, те же ромбы, что у
  зданий), боковые грани ВЕРТИКАЛЬНЫЕ (как боковины изо-кубов).
- Параметры: полу-ширина w=22, половина боковой грани b=9, вынос вершины A=b+w/2=20 (условие
  изо-уклона плеч). Вершины HEX_PTS: T(0,-20), UR(22,-9), R(22,9), B(0,20), L(-22,9), UL(-22,-9).
- Базис решётки: q=(44,0) [сосед через вертикальную грань], r=(22,29) [сосед вниз-право].
  hexCenter(q,r)=(44q+22r, 29r); screenToHex обратно; worldAt — обратное изо. Численно
  проверено: 6 общих рёбер с соседями, площадь гекса = площади ячейки решётки (1276), roundtrip.
- Обрывы: видны ДВЕ передние плечевые грани (R→B к соседу q+1,r+1; B→L к соседу q,r+1),
  уклон 1/2; вертикальные бока при спуске вырождены, верхние плечи обращены от камеры.
- Все 10 биомов получили gpt-image текстуры 96px (догенерированы forest/mountain/hill),
  process-hex.cjs прогнан; тинт поверх текстур ослаблен (читается AI-арт); процедурная
  фактура осталась только у dgrass. Канвас тайла 52×48.

---

## 1.0.025 — изо-гекс как ТОЧНАЯ 2:1-проекция плоского шестиугольника (широкая плитка 2:1)
- Гекс 1.0.024 был вертикально-симметричным «кристаллом» с почти квадратными пропорциями
  (44×40), он не читался как лежащий на земле пол. Исправлено: берётся обычный regular
  flat-top шестиугольник со стороной HS=20 в МИРЕ и проецируется через toIso (ix=wx-wy,
  iy=(wx+wy)/2) — автоматически получается широкая низкая изо-плитка 54.6×27.3 (сплющивание
  ровно 2:1, как у ромбов зданий). Проекция сохраняет плоскую мозаику.
- Мировой центр ячейки (стандартная flat-top раскладка redblob): wx=1.5·HS·q,
  wy=√3·HS·(q/2+r). HEX_PTS = проекция 6 углов мира: (20,10),(-7.3,13.7),(-27.3,3.7),
  (-20,-10),(7.3,-13.7),(27.3,-3.7). hexCenterWorld/hexCenter/screenToHex/hexRound
  перестроены; world→гекс через cube-round по flat-top формулам.
- Painter's order: три передних (нижних) соседа (q+1,r),(q,r+1),(q+1,r-1) имеют глубину
  s=2q+r ровно на +2 — земля рисуется проходом по s снизу вверх, внутри по q. Проверено:
  6 общих рёбер, s монотонна к передним соседям, screenY передних >0.
- Обрывы по ТРЁМ передним рёбрам: 0→1 (переднее, тёмное cB, сосед q+1,r), 1→2 (левое,
  cL, сосед q,r+1), 5→0 (правое, cR, сосед q+1,r-1). Светлый кант по задним рёбрам 2-3,
  3-4, 4-5; тень по передним.
- Канвас тайла 69×42; террейн reliefGridHex переведён на flat-top базис (worldAt/w2h),
  distance-transform и local-max по 6 соседям не зависят от ориентации.

---

## 1.0.026 — фикс рендера изо-гексов: двойной учёт камеры (земля и туман)
- БАГ 1.0.025 (на скриншоте: не видно текстур земли, туман «сотами» не на месте, тёмный
  куб налезал на ГЦ): `hexCenter(q,r)` возвращает АБСОЛЮТНЫЕ изо-координаты (мировой
  масштаб, десятки тысяч px у камеры), а цикл земли трактовал их как screen-relative —
  добавлял камеру второй раз (`ix = hx + camIX`) и куллил результат относительно нуля по
  экранному прямоугольнику. Абсолютные hx у камеры ~порядка camIX, плюс camIX → всегда
  за пределами halfW/halfH → отбраковывались ВСЕ тайлы (видна только заливка фона).
- ПРАВИЛО: внутри трансформа `translate(-camIsoX,-camIsoY)` рисуем в АБСОЛЮТНЫХ изо-
  координатах (как `toIso` мировых точек юнитов/зданий) — камеру НЕ добавляем; для
  куллинга вычитаем camIso (`sx = hx - camIX`) и сравниваем с halfW/halfH.
- Цикл земли render() переписан: диапазон q/r берётся из рельефной сетки
  (`relief.q0/q1/r0/r1`, она уже покрывает видимую МИРОВУЮ область), мировая точка —
  `relief.worldAt(q,r)`, центр — `hexCenter(q,r)` (абсолют), drawImage в
  `(hx-TCX, hy-TCY-up)`, обрывы drawCliffsHex(hx,hy,…). Куллинг по экранному смещению.
- drawFog() переписан на тот же базис: видимая МИРОВАЯ область вокруг камеры (margin от
  vw/vh/zoom) → углы через world→iso (`ix=wx-wy, iy=(wx+wy)/2`) → screenToHex → целочисл.
  границы q/r; внутри — мировые центры `hexCenterWorld` для индекса тумана (fogCell-сетка
  по миру) и АБСОЛЮТНЫЕ `hexCenter` для hexPath. Раньше туман читал карту по точкам у
  начала координат (wx=hx/2+hy от ОТНОСИТЕЛЬНЫХ hx) и рисовался со вторым camIX → «соты»
  в случайных местах и куб на базе. Теперь гекс тумана точно ложится на тайл земли.
- Цикл редких AI-объектов рельефа (вершины/курганы) тоже переведён на абсолютные
  координаты (hexCenter вместо camIX+fhx), иначе декор-пики не появлялись.
- Проверено численно: ближайший к камере гекс на экране со смещением ~пол-гекса (камера
  редко точно в центре); у всех 3 передних соседей ds=+1..+2 и dy>0 (рисуются поверх),
  у задних — отрицательные; painter's order по s=2q+r корректен. tsc --noEmit чисто,
  build ОК.

---

## 1.0.027 — пост-фикс аудит изо-гекса (документация координат, проверка геометрии)
- Обновлён устаревший комментарий над циклом земли (оставался от 1.0.024, со ссылкой на
  старый базис q=(44,0)): теперь описан корректный абсолютный базис и правило камеры.
- Численно подтверждено соответствие 3 передних рёбер обрыва соседям (world-углы гекса
  относительно центров соседей): ребро 0→1 ↔ сосед (q+1,r), 1→2 ↔ (q,r+1), 5→0 ↔
  (q+1,r-1) — drawCliffsHex рисует вертикальные грани на правильных рёбрах; тёмное
  переднее ребро 0→1 корректно.
- Аудит остального рендера: inView/рельефные отступы (margin 120 мир + pad 8), depth-sort
  объектов, призраки стен/зданий, миникарта — все используют toIso(world) в абсолютных
  координатах; двойного учёта камеры больше нет ни в одном месте. Без функциональных правок.

---

## 1.0.028 — партия из 4 фиксов: казахская озвучка, ворота, обход препятствий, свет террейна
- **Озвучка (казахская раса игрока).** Рабочие реплики шли через `SoundBank.say()`,
  который ищет ТОЛЬКО русские CLIP_URLS (казахская ветка реализована в `playPhrase()`:
  KZ_CLIP_URLS → откат на русский). Поэтому «За работу»/«Сделаю в лучшем виде» всегда
  играли русские mp3. В engine.ts все рабочие вызовы переведены на
  `sound.playPhrase('за работу')` (ресурс/ремонт/стройка) и
  `playPhrase('сделаю в лучшем виде')` (раппорт о постройке). `say()` оставлен для
  сюжетных русских реплик; новые mp3 не создавались (казахские уже есть в
  public/voices/kaz-voises/worker-kaz/).
- **Ворота/стены (ориентация).** Спрайт ворот «встроен» в стену, идущую по ДРУГОЙ
  изо-диагонали, чем спрайт сегмента стены (боковые пролёты ворот перпендикулярны
  длинной оси стены). В `drawWallSprite` зеркалирование по X теперь разное для ворот
  и стены: `flipX = isGate ? b.axis!=='x' : b.axis==='x'`. Раньше ворота зеркалились
  по тому же условию, что и стена → проёмы не стыковались, кладка шла «не слитно».
- **Обход препятствий (A*).** Юниты, упёршиеся в стену/здание, стояли на месте.
  Добавлены `computePath()` (A* по сетке CELL=26; стены/ворота-блокиры — круг r=15,
  готовые здания — AABB size/2+8; ворота своего владельца проходимы; старт/цель не
  блокируются, при занятости ищется ближайшая свободная клетка; не режем диагональные
  углы сквозь стену; лимит области 26000 клеток и guard 6000) и `moveTowardPath()`
  (если прямая линия до цели перекрыта — прокладываем путь и идём по waypoint'ам;
  детекция застревания stuckT>0.7 → пере-прокладка; антиспам noPathT 0.5с; если путь
  не найден — прём прямо как раньше). ВСЕ перемещения крестьян/воинов/монаха
  (move/attackmove/gather/return/build/patrol/ремонт/точки сбора/гарнизон) переведены
  с moveToward на moveTowardPath. Животные/волки (updateAnimal/updateWolf) и сбор
  ресурсов врагом в enemyAI оставлены на прямом moveToward — им обход не нужен.
  Состояние пути — поля Unit.path/pathGoal/stuckT/lastX/lastY/noPathT.
- **Осветление террейна (iso.ts getHexTile).** Земля была тёмной, юниты сливались с
  фоном: BASE-цвета биомов подняты по светлоте (grass #5a8c42→#6fa84f, dirt #8a7048→
  #9c8157, forest #3f6b2f→#52843c и т.д.; вода чуть ярче); тёмные тинты (dgrass/forest/
  mountain/hill) заменены на МЯГКИЕ ОСВЕТЛЯЮЩИЕ светлые вуали; тёмные вкрапления
  dgrass заменены на светлые; задний светлый кант рёбер усилен (0.22→0.30), передняя
  тень-гребень ослаблена (чёрный 0.28→rgba(30,40,20) 0.16). AI-текстуры (квадратные
  PNG 96×96) полностью перекрывают гекс, поэтому осветление через подложку+тинт+канты
  даёт равномерно более светлую землю без швов.
- Версия 1.0.028 (package.json `_gameVersion`, src/App.tsx GAME_VERSION). tsc --noEmit
  чисто, vite build ОК.

---

## 1.0.029 — перерисовка стен/ворот (gpt image) и светлые текстуры земли (gpt image)
- **Стены и ворота перерисованы с нуля (gpt image).** Корень бага стыковки: старый
  спрайт ворот имел встроенные обрывки стены по ПЕРПЕНДИКУЛЯРНОЙ изо-диагонали, чем
  спрайт сегмента стены (стена шла снизу-слева→вверх-вправо, пролёты ворот —
  вверх-слева→вниз-вправо) — это не чинилось зеркалом. Новые ассеты генерировались
  как ЕДИНЫЙ арт: kz_wall (длинная стена с торцом, бирюзовая полоса, зубцы) и
  kz_gate (через images-ссылку на стену — та же ось/высота/стиль; две круглые башни
  с зубцами + тимуридская арка к зрителю; стена продолжается по ОБЕ стороны без
  излома). Оба идут по диагонали снизу-слева→вверх-вправо.
- Обработка: новый `scripts/process-wall-gate.cjs` — декод, вырезка magenta-фона
  (в т.ч. ТЁМНОЙ тени под аркой: R и B высокие и близкие, оба заметно выше G),
  крупнейшая связная компонента, автокроп, box-average даунскейл с усреднением цвета
  ТОЛЬКО по непрозрачным пикселям (иначе magenta-бахрома тонирует кромку). Размеры:
  kz_wall 335×200 (baseW 335, ax 168, ay 200), kz_gate 418×250 (baseW 418, ax 209,
  ay 250). Анкеры обновлены в sprite-art.ts.
- **Зеркалирование возвращено к единому условию** `flipX = b.axis==='x'` для стены И
  ворот (хак 1.0.028 «противоположной оси» убран — он был костылём под перпендикулярный
  старый спрайт). Ворота центрируются на клетке, соседние сегменты стены нахлёстываются
  на их боковые пролёты → слитная линия на обеих осях.
- **Текстуры земли перерисованы (gpt image):** grass (ярко-светлая трава), dirt (светлая
  бежевая земля вместо тёмно-шоколадной — это площадка вокруг базы, где стоят юниты),
  field (светло-золотисто-зелёное поле), forest (светлый лиственный пол с кронами),
  desert (светлый золотой). Песок не сгенерировался (лимит) — sand/mountain/hill/water/
  deep пока осветлены программно (lift к белому, k≈0.66–0.82); перерисовать gpt image
  следующим заходом. Обработка — scripts/process-hex.cjs (raw/hex_*.png → hex/*.png 96²).
- На фоне светлой земли тинт BASE в iso.ts уже светлый (1.0.028) — тёмного «шоколада»
  больше нет, юниты читаются на фоне.

---

## 1.0.030 — полный набор светлых текстур земли (gpt image) + угловой бастион в стене новой стены
- Догенерированы gpt image оставшиеся текстуры (песок срывался в 1.0.029): hex_sand
  (светлый пляжный песок), hex_mountain (светлый серо-бежевый камень), hex_hill
  (светлый травянисто-оливковый холм), hex_water (яркая бирюзовая вода, чуть
  приглушена/сдвинута к синему скриптом чтобы не была неоном), hex_deep (открытая
  синяя вода, тоже приглушена). Теперь ВСЕ 10 биомов — новый светлый gpt-арт, ни одна
  текстура не осталась осветлённой программно (как было с 5 в 1.0.029). Обработка —
  scripts/process-hex.cjs.
- Угловой бастион kz_wall_corner перерисован gpt image (через референс стены) в едином
  стиле: квадратная угловая башня с зубцами, бирюзовая полоса, саманный кирпич, та же
  изо-ось и освещение, что у новой стены (старый бастион отличался по стилю).
  Обрабатывается тем же scripts/process-wall-gate.cjs (добавлен в TARGET_H, высота 200):
  181×200, baseW 181, ax 91, ay 200 — анкер обновлён в sprite-art.ts.
- Итог по стенам: стена (335×200), ворота (418×250) и бастион (181×200) — единый набор
  с одной изо-осью (снизу-слева→вверх-вправо); ворота имеют встроенные пролёты по той же
  диагонали → кладка слитная, зеркалирование по axis==='x' (из 1.0.029) работает для всех.
- Тинт/BASE iso.ts остаётся светлым (1.0.028) поверх ещё более светлых текстур — земля
  читается, юниты не сливаются. tsc --noEmit чисто, vite build ОК.

---

## 1.0.031 — крупные пропорции стен/ворот + текстуры травы/земли по референсу игрока
- **Проблема пропорций (забор «мелкий»).** Стена рисуется с масштабом scale=(2*S*fit)/baseW —
  длинный спрайт (большая baseW), ужатый в ширину ОДНОЙ клетки (2*S), давал крошечную
  высоту (28*200/335≈17px — ниже юнита, тонкий частокол). Исправлено двумя рычагами:
  1) спрайт стены перерисован gpt image как КОРОТКИЙ ВЫСОКИЙ сегмент (3 крупных зубца,
  baseW 249 при высоте 200, соотношение 1.25 вместо длинного пролёта); ворота —
  компактная высокая башня-ворота с двумя башнями/аркой и короткими обрубками стены
  (baseW 440, высота 250). 2) размеры в config.ts: wall 28→44, gate 34→66.
  Итог: высота сегмента стены ≈71px (выше юнита), ворота-башни ≈75px, угловой бастион
  ≈98px — стена выглядит массивной фортификацией. Соседние сегменты (шаг TILE_STEP=32)
  перекрываются (целевая ширина 2*S=88 > смещения вдоль линии ~36 экр.px) — линия
  сплошная, без щелей. Ворота визуально перекрывают соседние сегменты (обрубки), стык
  слитный. placementValid для смежных стен при шаге 32 проходит (need=S-16=28 < 32);
  блокировка/pathfinding (радиус 15 у стен) не менялись.
- **Анкеры sprite-art.ts** обновлены под реальные PNG (важно: масштаб = 2*S/baseW, при
  неверной baseW спрайт мельчал): kz_wall baseW 249 ax 125 ay 200; kz_gate baseW 440
  ax 220 ay 250.
- **Текстуры по референсу игрока:** трава — насыщенный естественный зелёный (плотный
  газон, не неон/не бледный), земля — тёплый бежево-коричневый утоптанный суглинок с
  мелкими крапинками. Перерисованы gpt image (hex_grass, hex_dirt) → process-hex.cjs.
  Учтены пропорции гекса: текстура квадратная 96², обрезается по широкому низкому
  flat-top гексу (шире, чем выше) — детализация под мелкий кроп.
- Угловой бастион (181×200 из 1.0.030) совместим: при S=44 даёт ~98px башню.
- tsc --noEmit чисто, vite build ОК.

## 1.0.032 — батч из 7 фич
1. **Высота гор/холмов до 30 ступеней** (terrain.rawRelief): горы 14..30 (smoothstep по e>0.76, fbm cell 1100, 2 октавы), холмы 4..16, равнины 0..3; вода/берег/базы плоские. reliefGridHex давит перепад ≤1 по 6 соседям. **RELIEF_STEP в engine.ts 10→5** (пик 30×5=150px, не взлетает; холмы 20–80px).
2. **Больше равноудалённых баз племён**: `planTribeCamps()` в genWorld — детерминированная аксиальная (гексагональная) сетка лагерей шагом 1500 по всему миру с джиттером по seed, отсев по краям/близости к стартам/суше. Старый случайный спавн в genChunk убран.
3. **Расширенная дипломатия** (новые методы + поля в App-кнопках панели мира):
   - `openTradeRoute()` — торговый договор (нужен Рынок, 60🪙): +6 золота игроку / +4 соседу каждые 8с, неприязнь −1.2; рвётся в войну.
   - `signNAP()` — пакт о ненападении (120🪙): ИИ не объявляет войну 120с (условие wantsWar требует napT<=0).
   - `condemnNeighbor()` — осуждение: casusBelli режется до ≤0.5 (будущая атака ИИ вероломна → низкий боевой дух), неприязнь +12.
   - `demandTribute()` — дань: требует сила ≥1.25× соседа; сразу золото + периодическая выплата (tributeT), копит обиду.
   Поля: tradeRoute/tradeT, napT, condemned, tributeT/tributeGold; сериализуются в dip.
4. **Камера к выделению**: engine.focusSelection() + кнопка MiniBtn «К юниту» (Crosshair) рядом с «Центр».
5. **Мини-карта переписана** (была «регион вокруг камеры», неюзабельна): теперь МИРОВАЯ (весь WORLD 0..w/h), кэшированный фон террейна (offscreen canvas, перерисовка при ресайзе), туман неисследованного, точки ресурсов/реликвий/зданий/юнитов с учётом видимости, метки баз, рамка вьюпорта; клик/драг → `minimapJump` транслирует долю → мировую координату.
6. **Звук сражения только против юнитов**: strike() — battleClash() вызывается если !vsAnimal (neutral && !tribe = зверь). Охота на волков/скот звучит иначе.
7. **Рыбалка (как AoE)**: узел kind 'fish' (r=18, несёт food, синие частицы/рябь, drawIsoFish в iso.ts); спавн косяков 1–3 на МЕЛКОЙ воде у берега (в genChunk shoreWater — вода с сушей в радиусе ~90, чтобы рабочий достал с суши); порядок сбора/right-click работает через issueSmart→orderGather (carry fish→food), депозит как еда.
- tsc --noEmit чисто, vite build ОК (8.74MB singlefile).

## 1.0.033 — фикс сильных просадок FPS (после 1.0.032)
Причины лагов и правки:
1. **Туман мини-карты рисовался КАЖДЫЙ КАДР** по всем ячейкам мира (fogGW×fogGH = 750×750 ≈ 562k итераций/fillRect за кадр). → Туман вынесен в оффскрин-слой `minimapFog`, перерисовка раз в ~30 кадров.
2. **Лагерей племён было ~300+** (шаг сетки 1500 на мире 48000) → сотни башен и ~1000+ юнитов сразу, сепарация O(n²) по всем. → Шаг 1500→8000, диапазон узлов ±12→±4: теперь ~30–45 равноудалённых лагерей (≈135 воинов + ~40 башен).
3. **O(n²) логика нейтралов по всему миру** каждый кадр (поиск врагов воинами племён, стрельба нейтральных башен, сепарация, коллизии). → Добавлен `activeZone(wx,wy,rad)`: нейтрал спит, если далеко от камеры (rad*1.7) И от всех не-нейтральных юнитов/зданий. Спящие пропускают апдейт/коллизии/скан целей и не давят в сепарации (грубый отсев пар по осям). Игрок/соперник не спят никогда; бой на территории лагеря будит его.
4. Фон террейна мини-карты сэмплится в половинном разрешении (0.5×) — разовый хитч при ресайзе/старте в ~4× дешевле.
- tsc чисто, vite build ОК.

## 1.0.034 — ворота в единой изо-проекции со стеной
Проблема: спрайт ворот был ФРОНТАЛЬНЫМ (арка анфас, две башни по бокам, обрубки стены торчали горизонтально влево/вправо), а стена — в изо-диагонали (снизу-слева → вверх-вправо). При посадке в изо-клетку обрубки не продолжали диагональ стены → «каша» на стыке.
Правки:
- kz_gate_raw перегенерён gpt image С ИСПОЛЬЗОВАНИЕМ kz_wall.png как референса (images=[kz_wall.png]) — ворота в ТОЧНО той же изо-проекции: единая диагональная стена снизу-слева→вверх-вправо, арка-проём врезана в эту линию, зубцы на гребне вдоль той же оси, без фронтальных башен и без горизонтальных обрубков.
- process-wall-gate.cjs: добавлен детектор isFringe (полупрозрачная magenta-бахрома на кромке: R,B высокие, G заметно ниже; не задевает песок [G высокий] и бирюзу [R низкий]).
- Новый размер ворот 311×250 (было 440×250): sprite-art.ts kz_gate ax 220→156, baseW/w 440→311, ay 250. Ворота массивнее стены (масштаб по baseW даёт высоту ~89px против ~71px стены), их более длинная диагональ перекрывает соседние сегменты — линия сплошная.
- tsc чисто, vite build ОК (8.69MB).

## 1.0.035 — ворота в точных пропорциях стены
После 1.0.034 проекция ворот совпала со стеной, но ворота были КРУПНЕЕ/ВЫШЕ сегмента (size 66 vs wall 44, квадратные зубцы вместо округлых) — пользователь: «ворота должны точно соответствовать размерам стен».
Правки:
- kz_gate_raw перегенерён по kz_wall.png с жёстким требованием ИДЕНТИЧНЫХ пропорций: тот же сегмент стены (округлые зубцы-3 шт., высота, толщина, длина, бирюзовая полоса на той же высоте), в центре — одна маленькая стрельчатая арка-дверь для прохода. Без башен/лишних зубцов. Результат 311×250 (wall 249×200 — соотношение сторон совпадает).
- config.ts: gate size 66→44. При scale=2*S/baseW стена (44/249) и ворота (44/311) дают идентичный экранный размер 88×70.7 px — ворота больше не выделяются по габариту. HP/стоимость ворот (600/45) сохранены.
- tsc чисто, vite build ОК (8.69MB).

## 1.0.036 — ворота перекрывают стену + широкий проход
Проблема: примыкающий сегмент стены (длинный спрайт 88px, перехлёстывает соседние клетки),
рисуясь по depth-sort ПОЗЖЕ ворот, заходил на клетку ворот и закрывал проём. Рисовать ворота
поверх стен нельзя — сквозь арку тогда виден сплошной спрайт стены.
Правки:
- kz_gate_raw перегенерён по kz_wall.png: тот же сегмент стены, но в центре ШИРОКИЙ сквозной
  арочный проход (иван-портал в среднеазиатском стиле), видимый насквозь, обрамлён бирюзовым
  орнаментом; фланги — короткие крылья стены с обычными зубцами. Размер 311×250 (как стена).
- engine.beginWallGateClip(b,ix,iy): для каждого сегмента СТЕНЫ ищет соседние ворота того же
  владельца вдоль линии (toIso, дистанция <60) и накладывает canvas-clip на полуплоскость
  стороны стены, отсекая перехлёст спрайта, заходящий на клетку ворот (граница — середина
  между центрами). Применяется вокруг drawWallSprite для не-угловых сегментов. Результат:
  ворота с собственными крыльями перекрывают линию стены, проём открыт, враги блокятся
  радиусом ворот, свои проходят (gatePass), свои юниты свободно идут через широкую арку.
- tsc чисто, vite build ОК (8.69MB).

## 1.0.037 — анимации казахского крестьянина: 4-кадровая ходьба по изо-направлениям + работа
Раньше у kz-крестьянина (раса игрока) было лишь 2 кадра шага на направление и вообще не было
кадров работы (ветка work у расы была отключена — рисовался статичный боковой кадр).
Арт gpt image по эталону kz_villager.png (меховой тымак, терракотовый чапан с золотым
орнаментом, зелёная рубаха, сапоги) — 6 полос на magenta-фоне, нарезаны в кадры.
Новые спрайты (src/assets/sprites/units/kz/, якоря в sprite-art.ts):
- ходьба 4 кадра × 3 изо-направления: kz_villager_sw1..4 (сбоку, отражается по face),
  kz_villager_fw1..4 (АНФАС — идёт вниз к камере, видно лицо, нога шагает к зрителю),
  kz_villager_bw1..4 (ТЫЛ — идёт вверх от камеры, видно спину/затылок).
  fmode уже выставлялся движком (1=перёд при mvy>0, 2=спина при mvy<0, 0=бок) — теперь
  казах использует полноценный 4-кадровый цикл вместо 2.
- работа [замах, удар]: kz_villager_chop1/2 (топор, рубка леса), kz_villager_mine1/2
  (кирка, золото/руда/стройка), kz_villager_gather1/2 (руки вверх — сбор фруктов/урожая).
  wkind/wphase уже выставлялись движком (chop/mine/gather, цикл 0..1, удар при >0.62).
Пайплайн: scripts/split-kz-strip.cjs (новый) — режет полосу любой раскладки (ряд/сетка 2×2/
столбик) по связным компонентам после вырезки magenta, порядок чтения, общий масштаб по
медиане роста тела (тело всегда ~150px), ноги прижаты к низу, чистка magenta-каймы и
внутренних артефактов, якорь-центр (AXMODE=center) для стабильной посадки.
pixelart.ts: добавлены KZ_V_WALK_SIDE/FRONT/BACK (4 кадра) и KZ_VILL_WORK (3 пары);
ветки kzWorking и kzVill в drawUnitSprite выбирают кадр по fmode/wkind/wphase; поза
(подскок/наклон/выпад) переиспользует логику штатного крестьянина.
- tsc чисто, vite build ОК (~9.1MB).

## 1.0.038 — убрана сетка гексов в затемнённых/туманных областях
Жалоба: на возвышениях сетка тайлов, видимая сквозь полупрозрачное затемнение
(туман/неосвещённые клетки), не совпадает — приподнятые на ступень тайлы несут свои
запечённые рёбра-гребни, и они «разъезжаются» с соседями по высоте.
Причина: обводка рёбер (светлый гребень + тень-обрыв) запекалась в КАЖДЫЙ тайл
(getHexTile, ridge()). В тумане (alpha 0.42/0.82) она просвечивала и на elevation-
ступенях читалась как кривая сетка.
Правка:
- iso.getHexTile(kind, variant, grid=true): ключ кэша +'grid'; при grid=false рёбра
  не рисуются (бесшовная поверхность). Кэш-набор при дозагрузке текстуры — по
  сегменту ready (k.split(':')[2]==='0') вместо endsWith(':0').
- engine: в цикле террейна lit = !fogOfWar || fogAt(wx,wy).vis; в туман/затемнение
  рисуем тайл без сетки (getHexTile(kind,hv,lit)), обрывы drawCliffsHex тоже только
  на lit (их грани тоже несут «сетку» вверх по ступеням). Рельеф по-прежнему читается
  формой тайлов/обрывами в освещённой зоне; в тумане — ровная поверхность без сетки.
- tsc чисто, vite build ОК (~9.1MB).

## 1.0.039 — рабочие: быстрый возврат, авто-работа, самооборона и охота
1) МЕДЛЕННЫЙ ВОЗВРАТ. В state 'return' цель moveTowardPath пересчитывалась каждый кадр
   как tc.x+rand(-4,4),tc.y+rand(...) → goalMoved (>48 иногда ложно) и A*/направление
   «дрожали», рабочий топтался/еле шёл у ТЦ. Исправлено: sendToDrop задаёт стабильную
   точку сдачи у кромки здания (со стороны прихода, радиус size/2+10), return идёт на неё
   без покадрового rand, arrive=70. Депозит в радиусе 95 от центра ТЦ.
2) АВТО-РАБОТА. Свободный/простаивающий крестьянин и рабочий, чей узел иссяк, сразу идёт
   к ближайшему ресурсу (nearestResource — узел ЛЮБОГО типа с остатком; радиус 1500–1600,
   чтобы не убегать за полкарты без приказа). После сдачи груза — обратно к узлу, а если
   тот кончился — к ближайшему ресурсу (orderGather).
3) САМООБОРОНА И ОХОТА.
   - acquireEnemy: крестьяне авто-берут угрозу только в малом радиусе (волк ~150, воин/
     племя ~120) — отбиваются рядом с работой, не гоняются по карте; дичь/скот в авто-бою
     не трогаются.
   - villagerCombat: преследует и бьёт боевую цель (волк/воин) в обороне; дичь — только
     по явному приказу (флаг hunt). orderAttack для крестьян теперь = attackmove +
     targetU + hunt (раньше просто 'move' без удара). ПКМ по дичи = охота, мясо/еда
     начисляются в killUnit (корова 90 / олень 55 / овца 45).
   - damageUnit: крестьянин, которого ударили (воин/волк), ставит targetU и переходит в
     attackmove — даёт сдачи обидчику.
   - после боя: с грузом — return на сдачу, иначе авто-работа; hunt/targetU сбрасываются.
   - updateAnimal: олень пугается охотящегося крестьянина (hunt) и убегает (скорость
     бегства 200→135 — догоняется группой/одиночкой); скот (овца/корова) не пугается.
- tsc чисто, vite build ОК (~9.1MB).

## 1.0.040 — рыбалка (AoE), юнит-разведчик, слежение камеры, снап построек к гексам, фиксы
1) СЕТКА В ЗАТЕМНЕНИИ (возвышения). Туман рисовался в базовой плоскости тайла, а приподнятый
   на ступень рельефа (up) торчил над тёмным гексом — сетка/гребни на верхушке оставались
   видны и «не совпадали». Теперь drawFog поднимает каждый тёмный гекс на ту же высоту
   (reliefGridHex + RELIEF_STEP), что и тайл земли — туман ложится на ВЕРХ соты.
2) СНАП К ГЕКСАМ. Здания (не стены) при постройке привязываются к центру ближайшего гекса
   (iso.snapToHexWorld/hexRound); предпросмотр-фундамент рисуется на ЦЕНТРАЛЬНОЙ соте +
   соседях (hexNeighbors) по размеру спрайта (buildingHexRing): ГЦ/чудо ~2 кольца, прочее 1.
   Стены остаются на TILE_STEP (кладка вдоль оси). Привязка и в tryPlace, и в placementValid.
3) ЮНИТ-РАЗВЕДЧИК (config 'scout'): быстрый (speed 196), дёшев (60🍖/10🪙), обзор 480 (туман),
   может обороняться (atk 6). Тренируется в ГЦ (кнопка «Разведчик 🧭», горячая клавиша 9).
   updateScout: бьёт явную цель/агрессора в радиусе 150 (оборона), иначе АВТО-разведка —
   scoutGoal выбирает ближайшую неисследованную (туманную) точку на кольцах 260–680, обходит
   глубокую воду; когда всё открыто — идёт к неосмотренным лагерям племён. Спрайт — кадры
   лучника расы (KZ_*: scout=archer), UNIT_TARGET_H.scout=46.
4) КНОПКА АВТО-СЛЕЖЕНИЯ. toggleFollow/folTick + HUD camFollow: камера плавно следует за
   центром группы выделенных юнитов; ручное движение WASD/скролл/край экрана отключает режим.
   Кнопка «Следить» (Video-иконка) в блоке камеры; активна подсветкой.
5) РЫБАЛКА (AoE). Рабочий подходит к косяку рыбы (fish-узел на воде) и удит С БЕРЕГА —
   радиус подхода n.r+46, arrive не загоняет в воду; на месте проигрывается кадр сбора
   (удочка). Косяки уже анимированы (drawIsoFish: тень, круги ряби, 3 прыгающие рыбки по
   неисчерпанности). Рыба чаще у берега (genChunk shoreWater).
6) ФИКС ВОЗВРАТА (регрессия 1.0.039). В 'return' точка сдачи ставилась сбоку у кромки ТЦ, а
   arrive=70 срабатывал «прибыл» ДО входа в зону депозита (dropR) → рабочий вставал и не
   разгружался. Теперь цель — ЦЕНТР ТЦ (стабильно, arrive 12), депозит по радиусу size/2+14
   от центра; после сдачи — обратно к узлу или к ближайшему ресурсу.
- tsc чисто, vite build ОК (~9.1MB).

## 1.0.041 — дипломатия Civilization, приказы разведчика, спрайт разведчика, фикс возврата
1) ФИКС ВОЗВРАТА РАБОЧИХ (снова «медленно/застревают»). Причина: крестьяне толкаются
   коллизией у УГЛА ТЦ и до центра не доходят, а депозит круговой (dist до центра < dropR)
   срабатывал лишь ближе к граням; шедший по диагонали кружил снаружи. Теперь в 'return':
   депозит по расстоянию до БЛИЖНЕЙ ГРАНИ квадрата ТЦ (Chebyshev по size/2, запас +22) ИЛИ
   по центру (на всякий); цель движения — ближняя точка У ГРАНИЦЫ на радиусе dropR−6 со
   стороны подхода (стабильна между кадрами), arrive 8 совпадает с радиусом сдачи →
   разгрузка сразу на входе, без топтания у коллизии.
2) ДИПЛОМАТИЯ КАК В CIVILIZATION. Убрана центральная плашка с кнопками/индикаторами Вы/Враг;
   вместо неё компактная кнопка «Дипломатия» (Landmark) → модалка со списком народов и
   портретами правителей (gpt image, src/assets/portraits/*.png 256×256). Новый модуль
   src/game/nations.ts: NATIONS (id/name/ruler/title/portrait/color/kind/greet/choices):
   - rival «Славянское княжество», князь Ратибор (rival_ruler.png);
   - pecheneg «Печенеги», хан Куря (pecheneg_khan.png);
   - oghuz «Огузы», ябгу Тоган (oghuz_yabgu.png);
   - khwarezm «Хорезм», шах Ануш-Тегин (khwarezm_shah.png).
   Соперник остаётся owner='enemy'; племена (tribe) получают nationId на лагерь —
   tribeNationAt() детерминирован по позиции (циклично 3 народа). Портрет в карточке.
3) ЗНАКОМСТВА (4). rivalMet/tribeMet/tribeRel(neutral/friend/hostile). До контакта народы
   друг о друге не знают: враг скрыт туманом (как было), набеги/война/неприязнь ЗАМОРОЖЕНЫ
   до rivalMet (diplomacyUpdate гейт, grievance держится ≤8, casusBelli=0). Контакт —
   updateContacts (раз в 0.8с): вражеский ГЦ/юнит или лагерь племени в fogAt().vis →
   знакомство → очередь приветствий (greetQueue, по одному; greetShown без повторов).
   Приветствие — модалка RulerGreeting (портрет + реплика правителя + выбор действия как
   в Civ: тёплый приём/дары 75/дерзость сопернику; поприветствовать/дары 40/угроза племени).
   Явное нападение на соперника = форс-контакт без приветствия (onPlayerAggression).
4) РАЗВЕДЧИК: ПРИКАЗЫ + СВОЙ АРТ. После спавна НЕ убегает (нет mission — стоит у ГЦ).
   Кнопки в карточке выбранного разведчика: Исследовать (explore, туман кольца 260–680),
   Искать базы (bases, ближайший лагерь >520), Наладить связь (diplomacy, к ближайшему
   НЕ встреченному народу; контакт → приветствие правителя), Внедриться кротом
   (infiltrate, к ближайшей вражеской базе: крадётся на дистанцию ~230, 8с внедрения →
   база раскрыта, обзор 720 вокруг, периодические «донесения» +6🪙). scoutOrder(mission);
   u.mission/mtx/mty/mNation/infT/infDone/infBaseId. По ручному move разведчик по прибытии
   возвращается к заданию. Оборона: acquireScoutEnemy (дружественные племена не трогает).
5) СПРАЙТ РАЗВЕДЧИКА (gpt image). Свой лёгкий бегун: синий чапан с оранжевой тесьмой,
   бурка, меховой тымак, сабля в ножнах, сума. 13 PNG в kz/: kz_scout (статичный бок) +
   kz_scout_sw1..4 (бок, ходьба), kz_scout_fw1..4 (анфас), kz_scout_bw1..4 (тыл).
   Нарезка: бок — split-kz-strip.cjs (компоненты); анфас/тыл склеены тёмной линией-
   базлайном → новый scripts/split-strip-cols.cjs (сегментация по magenta-колонкам,
   baseline и огрызки <24px отброшены). Якоря в sprite-art.ts (kz_scout*, ay~150).
   pixelart.ts: KZ_BASE.scout=kzScout (вместо лучника), циклы KZ_SCOUT_WALK_SIDE/FRONT/
   BACK (W4), ветка kzCycleUnit (общая для крестьянина и разведчика) в drawUnitSprite.
   Из archer-наборов scout убран. Портреты уменьшены scripts/resize-portraits.cjs
   (кроп в квадрат с упором на лицо для вертикальных, bilinear → 256px).
   ВНИМАНИЕ: oghuz_yabgu.png и khwarezm_shah.png — ВРЕМЕННЫЕ заглушки (копии
   pecheneg/rival), не успели догенерироваться (лимит 10 картинок/ход) — заменить.
- tsc чисто, vite build ОК (~10.5MB).

## 1.0.042 — портреты огузов/хорезма + мелкие фиксы
- Догенерированы настоящие портреты oghuz_yabgu.png (ябгу Тоган, белый кыпчакский
  калпак с золотым узором, синий халат) и khwarezm_shah.png (шах Ануш-Тегин, белый
  тюрбан с рубиновым эгретом, бирюзовый халат) — заменили временные заглушки 1.0.041.
- Крот (infiltrate) больше не выбирает дружественное племя целью (nearestEnemyBase
  пропускает tribe-лагеря с rel==='friend').
- Убран мёртвый пустой цикл в updateContacts.

## 1.0.043 — Абылай-хан (правитель игрока) + 4 новых народа
- Портрет игрока abylai_khan.png (по референсу: остроконечный красный калпак с
  золотым узором, чёрная борода, красный халат с меховым воротником, золотой
  медальон, посох с навершием). PLAYER_NATION в nations.ts получил ruler/title/
  portrait: «Абылай-хан / Великий хан». Баннер хана в шапке DiplomacyModal.
- 4 новых правителя (gpt image, 256px) и народа: джунгары (Галдан, хунтайджи,
  dzungar_leader.png, воин в латах/шлеме, дары-дань 60), Коканд (Эрдэнэ-бий,
  зелёный халат, 50), Бухара (Мухаммад Рахим, эмир с чётками, 50), Российская
  империя (Екатерина II, горностай/андреевская лента, 60).
- engine.tribeNationAt теперь берёт TRIBE_IDS из nations.ts (было 3 хардкод-
  племени → стало 7 кочевых/оседлых народов на карте, детерминир. хэш).
- Цена даров племени берётся из NationDef.choices (gift.gold), а не хардкод 40:
  dipAction, greetingChoice, кнопка модалки и NationHud.gift — везде динамически.
- dipAction: act 'threat' из приветствия/кнопок маппится на 'attack'.

## 1.0.044 — экран переговоров с правителем в стиле Civilization
- Новый полноэкранный компонент LeaderAudience (App.tsx): слева большой портрет
  правителя в золотой раме (на мобильных — баннер сверху), справа тёмная панель:
  имя народа + цветной индикатор, «Титул Имя», бейдж НАСТРОЕНИЯ (новый контакт/
  нейтралитет/дружба/вражда/война, цвет), реплика правителя в пергаментном блоке,
  вертикальный список действий и кнопка «🤝 Попрощаться» внизу — как экран лидера
  в Civ V.
- Два режима одного экрана: первый контакт (greetingChoice: warm/gift/threat) и
  переговоры из списка дипломатии (openAudience). У князя Ратибора: мир во время
  войны; дары/торговля/пакт/осуждение/дань/война в мире. У племён: дары/дружба,
  при вражде — «задобрить дарами», угроза. Недоступные действия дизейблятся,
  нехватка золота показывается «нужно N🪙» на кнопке.
- Движок: audienceId + openAudience/closeAudience, AudienceHud в снапшоте
  (id/имя/титул/портрет/цвет/kind/rel/atWar/gold), greet добавлен в NationHud.
- DiplomacyModal упрощена: список народов → одна кнопка «💬 Переговоры с
  правителем», все действия уехали на экран аудитенции (onAct убран, добавлен
  onAudience).

## 1.0.045 — рыбалка-спрайты, погружение в воду, большие непроходимые горы, иммунитет разведчика
1. Рыбалка: спрайты kz_villager_fish1/2.png (казах в красном чапане, меховой
   шапке, стоит в профиль с удочкой, лёгкая анимация удочки). Новый wkind 'fish'
   (engine+pixelart типы), маппинг в KZ_VILL_WORK; рабочий на рыбе использует fish
   вместо gather (для не-казаха fallback gather).
2. Погружение в воду: drawPixelUnit получает water (0/1/2) от класса террейна
   ('water'→1 четверть, 'deep'→2 половина). Тело ниже ватерлинии обрезается
   ctx.clip, рисуется полупрозрачный эллипс воды + блик ряби. Ватерлиния на
   iy+8−subD от высоты UNIT_TARGET_H.
3. Горы: перегенерированы peak_snow (358x196) и peak_rock (175x172) крупнее и
   выше (process-terrain TARGET_H 128/118→196/172, MAXH 200→240); scale в движке
   snow 1.55→1.7, rock 1.25→1.45. Непроходимы: terrainBlocked() (класс mountain),
   moveToward скользит вдоль преграды, lineBlocked и free() pathfinding учитывают
   горы (цель на горе → ближайшая свободная клетка), scoutExploreGoal пропускает
   mountain; волки бродят в обход блокировки (sign-движение), чтобы не застревать.
4. Разведчик неуязвим для волков: волки не выбирают его добычей (updateWolf
   skip e.key==='scout'), scoutHostile/acquireScoutEnemy игнорируют волков.

## 1.0.046 — запрет стройки на горах/воде + замедление в воде
- placementValid теперь проверяет террейн: ни фундамент, ни стена не ставятся на
  классах mountain/water/deep (сетка точек вокруг центра).
- moveToward: вброд по воде скорость ×0.75, по глубокой ×0.55 (mid-cell класс).

## 1.0.047 — пастухи/загоны, улучшения башен, больше рыбалки
1. Скотоводство: новое здание 'pen' (Загон для скота, size 110, 160🪵, H-клавиша) —
   kz_pen.png изгородь 5×5 с овцами/коровой. Выбираешь рабочего → ПКМ по загону →
   он становится ПАСТУХОМ (assignShepherd): полцикла (26с) верхом на коне с кнутом
   (kz_shepherd_1/2.png, спрайт всадника в красном чапане/меховой шапке) выезжает в
   поле, гонит свободных овец/коров к загону (скот движется в сторону загона);
   вторые полцикла собирает скот у загона и даёт еду (~3/цикл с множителем).
   Пастух снимается с должности любым другим приказом (orderGather/перемещение).
   Спрайт: herder-флаг на villager, в pixelart ветка kz_shepherd (H=54, mounted-гейтинг).
2. Улучшения башен (tower): b.upg {range,dmg,archers}. Кнопки в карточке башни:
   📐 Дальность (3 ур, +18% дальности за уровень), 🏹 Урон (3 ур, +35% урона и +80 HP),
   🎯 Лучники (2 ур, +доп. залп веером и +25% темп). Цены 90/110/150×(1+lvl*0.8) 🪙.
   Применяется в defense-цикле (rangeUp/dmgUp/archers, salvo 1+archers веером).
   Сохраняется в serialize/load (blds upg). Снапшот sel.towerUpg.
3. Рыбалка: вероятность прибрежных чанков 0.55→0.8, 2 точки на чанк, 2–4 косяка,
   спавн и на 'deep' рядом с сушей (reach рыбака n.r+64).

## 1.0.048 — фикс: рабочий отказывался становиться пастухом
Причина: клик по НЕдостроенному загону уходил в ветку «строить», а пастух в state
'gather' срывался на авто-добычу ресурсов (idle-автоподбор nearestResource).
- issueSmart ветка pen теперь покрывает стройку: недостроенный загон → рабочие
  достраивают (build), готовый → назначение пастуха; берётся первый СВОБОДНЫЙ
  (не-пастух) рабочий.
- Должность пастуха устойчива: в состояниях gather/idle/return updateVillager
  всегда запускает updateShepherd (нет ухода на ресурсы); ручной move/attackmove
  временно прерывает, по прибытии (idle→gather) пастух возвращается к стаду.

## 1.0.049 — кнопка «Пасти скот» у рабочего (надёжное назначение пастуха)
- Новый метод herdOrder(): выбранные рабочие назначаются пастухами к ближайшим
  готовым загонам (1 пастух на загон; если загона нет — подсказка построить).
- В карточке выбранного рабочего блок «🐑 Пастух» → кнопка «🐎 Пасти скот».
  Кликом больше не нужно попадать по загону.
- assignShepherd: скорость рабочего→175 (верхом на коне), сброс цели/buildId;
  releaseShepherd снимает должность и возвращает скорость 118 (вызывается при
  потере загона); orderGather тоже снимает пастуха.

## 1.0.050 — пастух реально скачет на пастбище, скот пригоняется
- Пастух после назначения теперь ВСЕГДА движется: фаза ВЫПАСА (22с) — скачет на
  фиксированную точку пастбища в поле у загона (landNear — суша, не вода/гора,
  каждую новую фазу точка свежая); фаза ЗАГОНА (18с) — едет к воротам, скот внутри
  даёт еду (2+головы за цикл).
- Скот (овцы/коровы) при выпасе МЕТИТСЯ точкой притяжения a.herdX/herdY; updateAnimal
  при наличии метки ведёт животное к ней (не пугается пастуха, не блуждает к спавну),
  снимает метку когда пастух далеко. Радиусы сбора 420 (пастбище) / 1600 (загон).
- assignShepherd: скорость 175 (верхом), сброс buildId/целей; releaseShepherd
  возвращает скорость 118.

## 1.0.051 — пастух немедленно скачет на поле (фикс стояния на базе)
- Движение пастуха переведено с pathfinding (moveTowardPath) на прямое moveToward —
  всадник скачет напрямик по степи, исключены сбои прокладки пути/скольжения.
- assignShepherd сразу выбирает ДАЛЬНЕЕ пастбище (340 от загона, landNear) и ставит
  флоатер «🐎 Пастбище» в точке назначения + «скачет на поле» — видно куда едет.

## 1.0.052 — дальнее ровное пастбище со стадом
- findPasture(): кольца 1700..2700 мир.ед. (≈50-75 гексов) от базы игрока,
  ровное поле (террейн не вода/гора/лес, ступень рельефа ≤1 в радиусе 60).
- stockPasture(): спавнит 5 овец + 5 коров на пастбище, их якорь (wx/wy) = пастбище.
- Загон хранит pastureX/Y + pastureStocked; при назначении пастуха стадо создаётся
  один раз, метка «🐑 Пастбище» в поле, баннер. Цикл: выпас на пастбище (lureR 600),
  загон (lureR 2600 — скот идёт домой). Движение пастуха прямое moveToward.

## 1.0.053 — стадо кружит по кругу, пастух подгоняет сзади
Выпас: каждая голова стада движется по своей орбите вокруг точки пастбища —
свой угол-смещение в дуге (−0.95..0.95) и радиус (78..156), точка медленно
вращается (flockAng = time*0.22) → скот идёт по кругу дугой, разбросанно,
не в куче, всё время ВПЕРЕДИ. Пастух держится позади стада (угол головы −1.35,
радиус 175) и подгоняет его. Загон: скот бежит к центру загона вперёд, пастух
заходит со стороны поля (противоположной загону от центра стада) и толкает к нему.

## 1.0.054 — повышенный обзор пастуха (стадо всегда видно)
- updateFog: пастух (villager с herder) раскрывает радиус 560 вместо 150 — его
  стадо на дальнем пастбище всегда в поле зрения (у разведчика 480/720).
- Круговой выпас (1.0.053): стадо идёт по орбитам вокруг точки пастбища (свой
  угол/радиус на голову), скот впереди, пастух орбитит сзади (радиус 175).
  Скот просыпается и кружит, пока пастух в пределах 700 (activeZone), и не
  сбрасывает цель (release только когда пастух дальше 700 И скот дошёл).

## 1.0.055 — пастух всегда позади стада и рядом
Выпас: позиция пастуха привязана к ФАКТИЧЕСКОМУ центру стада — он держится в
60 ед. позади (против направления бега стада по кругу) + 26 ед. снаружи дуги,
arrive=18, поэтому не обгоняет и не убегает вперёд, всегда рядом (~86 ед.).
Радиусы орбит стада 85..155. Раньше шёл по независимой орбите (радиус 175) и
проскакивал вперёд.

## 1.0.056 — машина состояний выпаса: 4.5 мин, реальный перегон в загон
- Выпас на пастбище увеличен до 4.5 минуты (GRAZE_T=270).
- Цикл через herdState: graze (круговой выпас, пастух позади центра стада) →
  home (перегон: скот бежит к загону со скоростью 135, пастух гонит с тыла; фаза
  длится пока ≥80% стада не дойдёт, таймаут 150с) → pen (25с постой, еда капает)
  → back (стадо возвращается на пастбище, пастух следом).
- Скот помечен pastureId=pen.id при спавне (stockPasture), herd-фильтр строго
  по принадлежности к загону; в updateAnimal цель к загону не сбрасывается, скот
  к загону бежит быстрее. Нейтралы не сталкиваются со зданиями → заходят в загон.
- Пастух строго позади: graze — тыл центра стада по касательной (arrive 18),
  home/back — со стороны поля против направления движения (никогда не обгоняет).

## 1.0.057 — скот: плотный пиксель-арт + ходьба со всех сторон
- Коровы/овцы/олени больше не рисуются AI-спрайтом (он был полупрозрачным/рваным):
  drawUnitSprite возвращает false для скота → рисуется процедурный drawLivestock
  (сплошные непрозрачные пиксели, тёмный контель ног, копыта).
- Добавлены три вида: fmode 0 — бок (морда по face, пятна/вымя/рожки у коровы,
  комки шерсти у овцы, рога у оленя), fmode 1 — перёд (морда к камере: глаза, нос,
  уши/рога), fmode 2 — зад (круп, хвост, вымя/пятно). Анимация шага во всех видах
  (ноги в противофазе, подскок bob).
- Движок: изо-направление fmode теперь вычисляется для sheep/cow/deer (раньше только
  для волка) — на камеру/от камеры/вбок по сглаженному вектору движения.

## 1.0.058 — скот перерисован через gpt-image (плотные спрайты со всех сторон)
- Коровы и овцы — новые gpt-image пиксель-арт спрайты на magenta-фоне, обработанные
  scripts/process_livestock.py (хромакей по низкому зелёному каналу — не задевает
  розовый нос/вымя; снятие пурпурного ореола; обрезка; NEAREST-масштаб до 108/96px).
- Кадры: cow/sheep (бок покой), cow_w/sheep_w (бок шаг), cow_f/sheep_f (перёд к
  камере), cow_b/sheep_b (зад — круп/хвост/вымя). Олень остаётся процедурным.
- pixelart: drawUnitSprite рисует скот спрайтами; бок — базовый+2 кадра шага (flip
  по face), перёд/спина — статичные кадры при fmode 1/2 с вертикальным подскоком
  (без крена). Якоря cow/sheep/_f/_b в sprite-art.ts. Движок уже шлёт fmode для скота.

## 1.0.059 — постой в загоне 3.5 мин + работницы-казашки
- Пастуший цикл: фаза 'pen' (стадо в загоне/дойка) длится 210с (~3.5 мин) вместо 25с.
- Женщины-работницы (казашки в платке): Unit.female; addUnit для игрока чередует пол
  (villagerSexToggle). Спрайты gpt-image на magenta-фоне, обработаны
  scripts/process_kzf.py → kz/kz_fem[_w/_f/_b/_gather/_milk].png: бок покой/шаг,
  перёд, спина, сбор урожая (наклон с корзиной), дойка (на корточках с подойником).
- pixelart: женский villager рисуется своими кадрами (дойка/сбор/ходьба по fmode);
  якоря kz_fem* в sprite-art.ts; дойка приземистая (H=50).
- Авто-задачи женщин (updateVillager, idle): приоритет — дойка загона, где стадо в
  фазе pen (pickMilkingPen, Bld.milkWid, wkind='milk', молоко капает у загона),
  затем сбор урожая на ближайшей ферме (nearestFarmForWoman). Мужчина-пастух — как
  раньше верхом; женщина на кнопку «Пасти скот» не назначается принудительно.

## 1.0.060 — главное меню: игра «Казахское Ханство»
- Меню переоформлено под казахскую тему: новый gpt-image баннер hero-khanate.jpg
  (степь на рассвете, юрты, стадо с пастухом, конница батыров, бунчук/туг) вместо
  hero-battle.jpg; заголовок «КАЗАХСКОЕ ХАНСТВО» (золото + небесно-синий), подзаголовок
  «Эпоха Абылай хана».
- Фичи: Стада и загоны / Конница батыров / 4 эпохи ханства / Дипломатия народов.
- «Устав кочевника»: описание загона/пастуха (H, кнопка «Пасти скот»), казашки-доярки/
  сборщицы, дипломатия разведчика. Нижняя подпись и версия — Казахское Ханство.
- Баннер сжат в JPG (205КБ вместо 2.4МБ PNG); исходники _gen/ вынесены в .gitignore.

## 1.0.061 — казахские названия внутри игры + джунгары как главный враг
Меню было переименовано в 1.0.060, но внутри партии оставались европейские
термины AoE (Крестьянин/Рыцарь/Городской центр/Феодальный век) и славянский князь
как главный соперник. Ребрендинг доведён до самой игры.
- **Юниты** (`UNIT_DEFS`): villager → «Шаруа», swordsman → «Сарбаз», spearman →
  «Найзагер», archer → «Мерген», knight → «Батыр», cavalry → «Жасауыл», monk →
  «Баксы» (иконка 🪶 вместо ✝️), scout → «Барлаушы». Ключи юнитов и вся логика
  НЕ тронуты — переименованы только `name`/`desc`.
- **Здания** (`BUILDING_DEFS`): towncenter → «Ханская ставка», house → «Юрта»
  (иконка ⛺), market → «Базар», farm → «Пашня», wall → «Дуал (стена)»,
  wonder → «Мавзолей хана». Описания переписаны под степь.
- **Эпохи** (`AGES`): Тёмный → «Заря степи» 🌄, Феодальный → «Век жузов»,
  Замковый → «Век батыров», Имперский → «Век Абылай хана».
- **Сложности** (`DIFF`/`DIFFS`): Поселенец/Воевода/Завоеватель →
  Кочевник/Батыр/Великий хан.
- **Технологии** (`TECHS`): Острые клинки → «Дамасские сабли», Кованая броня →
  «Кольчуга батыра», Строевая муштра → «Выучка сарбазов», Соколиный глаз →
  «Глаз беркута» 🦅, Племенные кони → «Аргамаки», Тачки → «Арба».
- **Главный соперник** (`nations.ts`): народ `rival` — теперь Джунгарское ханство
  (хунтайджи Галдан Цэрэн, портрет dzungar_leader.png), исторический враг Абылай
  хана. Бывшее племя `dzungar` заменено на `slav` (Славянское княжество, князь
  Ратибор, освободившийся портрет rival_ruler.png, цвет #c084fc). Число народов и
  `TRIBE_IDS` не изменились — раскладка лагерей та же.
- Тексты дипломатии в `engine.ts` (баннеры войны/мира/дани/осуждения/пакта)
  перестали быть привязаны к «князю»: реплики берут `def.title`/`def.ruler` или
  говорят о джунгарах/хунтайджи. Подсказки, квесты, флоатеры и `ageNews` — на
  степной лексике («Аттан!», «шаруа», «Век жузов», «Мавзолей хана»).
- `index.html`: заголовок вкладки «Казахское Ханство — стратегия великой степи».
- `PLAYER_NATION.name`: «Казахский ханат» → «Казахское Ханство» (единообразно с меню).
- Ключи сохранений (`empires-dawn-savegame-v1`) не менялись — старые сейвы грузятся.

## 1.0.062 — склады-дропоффы (AoE-экономика) + фикс казны врага
Первый пункт дорожной карты `plan.md` (лучшие механики AoE/Civ). До этого
`nearestDrop()` возвращал ТОЛЬКО ханскую ставку, и шаруа таскал бревно с дальней
рощи через полкарты — застройка карты не имела смысла.
- **Новое здание `storehouse`** («Қойма (склад)», 100🪵, 420 HP, size 68, клавиша **K**):
  точка сдачи 🪵/🍖/🪙 наравне со ставкой. Спрайт `kz/kz_storehouse.png` (gpt image,
  саманный склад с плоской крышей, бирюзовой плиточной полосой, ящиками/бочками/
  мешками на песчаной платформе) прогнан штатным `scripts/process-kz-buildings.cjs`
  (magenta-кейнг → крупнейшая компонента → кроп → NN-даунскейл до baseW=340) →
  340×217. Якорь `kz_storehouse` в `SPR_ANCHORS`.
- `nearestDrop(u)` теперь выбирает БЛИЖАЙШУЮ из точек {towncenter, storehouse}
  своего владельца (done>=1). Проверено скриптом: ходка от рощи в 1400 ед. от ставки
  сокращается до 200 ед. — путь короче на 86%.
- `placeBld` получил фолбэк на KZ-спрайт: у склада нет европейского аналога, без
  фолбэка неигроковый владелец уронил бы рендер.
- **ФИКС (был давно, вскрылся при работе с дропоффами):** `deposit()` всегда писал в
  `this.res` — казну ИГРОКА, хотя `updateVillager` крутится и для вражеских шаруа.
  Вся добыча врага капала игроку (и накручивала его счёт/квесты). Теперь сдача идёт
  в казну владельца (`v.owner === 'enemy' ? this.eres : this.res`), а очки/флоатеры/
  квесты — только для игрока. Ферма (`state==='gather'`, ветка `fb`) писала еду тем же
  способом напрямую — переведена на общий `deposit()`.
- **ФИКС:** клавиша `h` была назначена дважды в `onKeyDown` (загон, затем «камера
  домой» в той же else-if цепочке — вторая ветка недостижима). Мёртвая ветка убрана,
  склад повешен на свободную `k`.
- UI: кнопка «📦 Склад (K)» в доке стройки, иконка в `bldIcon`, упоминание в «Уставе
  кочевника» и в строках горячих клавиш (QERFKZXC).
- Добавлен **`plan.md`** — дорожная карта улучшений из AoE/Civ (линии апгрейда юнитов,
  конверсия баксы, тревога «нас атакуют», племена как city-states с посланниками,
  случайные события степи, победа «Объединение степи», великие люди) + список
  замеченных багов.

## 1.0.063 — Мечеть и имам, көпес-торговцы, спокойное стадо, разморозка рабочих

Версия про две давние болячки движения (дёрганый скот, застывающие шаруа) и про
экономико-религиозный слой: базар шлёт караваны к друзьям, а «Баксы» стал «Имамом»
из отдельного здания.

### 1. Скот больше не дрожит на месте
Причина: в `updateShepherd` цель каждой головы задавалась через `rand()` **каждый
кадр** — в фазе `'pen'` `a.herdX = pen.x + rand(-40,40)`, в `'home'` `rand(-26,26)`,
в `'back'` `rand(-60,60)`. Животное 60 раз в секунду получало новую случайную точку и
вибрировало вокруг неё.

- Введены **стабильные слоты стада**: локальная функция `slot(a)` считает угол и
  «кольцо» по индексу животного в стаде и его `id` — место детерминировано и не
  меняется между кадрами.
- Фаза `'pen'` (постой): скот стоит по слотам, точка лишь очень медленно «дышит»
  (`sin(time*0.45)`, амплитуда ~5 ед.) — стадо живое, но неподвижное.
- Фазы `'home'`/`'back'`: цель тоже по слоту (кольцо вокруг загона/пастбища).
- В `updateAnimal` вместо жёсткого порога `d > 8` сделан **arrival-подход**:
  `sp = min(speed, d*3)`, шаг `min(sp*dt, d)`, полный покой при `d < 1.2`. Скорость
  гаснет у цели, животное не перелетает точку и не «пилит» туда-обратно.

### 2. Рабочие больше не застывают
Найдено ДВЕ независимые причины.

- **`moveTowardPath` стоял на месте по 0.5 с.** Ветка троттлинга A* делала
  `if (u.noPathT && now - u.noPathT < 0.5) return false;` — то есть при ненайденном
  пути юнит не шёл вообще. Так и выглядело: рабочий с грузом замирает у ресурса
  (точка сдачи лежит внутри коробки склада → `lineBlocked()` всегда true →
  `computePath` возвращает null). Теперь троттлится **только расчёт пути**, а идти
  продолжаем напрямую: `return this.moveToward(...)`.
- **Детектор застревания не срабатывал.** Он жил внутри `if (moved)`, а при полностью
  нулевом смещении (упор в гору/угол здания) `moved` = false, `stuckT` не рос и путь
  не перекладывался никогда. Проверка вынесена наружу и считается всегда; при
  `stuckT > 0.7` добавлено «отлипание» — шаг ~9 ед. вбок по детерминированному от `id`
  направлению.
- Waypoint'ы: раньше на каждый достигнутый тратился отдельный кадр с `return false`
  (на плотной цепочке — заметное залипание). Теперь достигнутые точки выбрасываются
  циклом `while`, а на опустевшем пути юнит идёт к цели напрямую.
- **Выработанная округа.** При исчерпании узла рабочий искал замену в радиусе 1500 и
  при неудаче уходил в `idle` навсегда. Радиус поднят до 2600 (как в idle), а в самом
  `idle` добавлена страховка: простояв 4 с, шаруа идёт к **любому** оставшемуся
  ресурсу на карте, даже далёкому.

### 3. Автодойка после возврата стада
`pickMilkingPen` вызывался только из ветки `u.state === 'idle'`, а занятая работница
(пашня, лес, ягоды) в `idle` практически не попадала — «автоматически доить» не
включалось.

- В начале `state === 'gather'` добавлен приоритетный перехват: женщина не-пастух
  раз в ~1 с проверяет `pickMilkingPen` и, если стадо вернулось, бросает текущую
  работу и уходит доить (недосданный груз сдаётся через `deposit()`).
- `pickMilkingPen`: радиус 1400 → **2200**, и загон ищется у **своего владельца**
  (было жёстко `b.owner !== 'player'` — вражеские работницы не доили вовсе).
- **ФИКС владельца (как у фермы в 1.0.062):** молоко зачислялось напрямую в
  `this.res.food` — теперь через `deposit()`, в казну владельца.

### 4. Мешіт-медресе и Имам
- Юнит `monk` переименован: **«Баксы» → «Имам»** (казахи — мусульмане), описание
  «Служитель мечети — лечит ваше войско», иконка 🪶 → 🕌. Ключ `monk` сохранён —
  на нём завязаны спрайты, анимации и якори.
- Обучается теперь не на базаре, а в новом здании: `bld: 'market'` → **`'mosque'`**
  (гейт получается автоматически — `train()` ищет здание из `d.bld`).
- Новое здание **`mosque` «Мешіт-медресе»**: hp 520, size 86, 175🪵 + 100🪙,
  16 с, обзор 200. Хоткей **`M`** (и русская `ь`).
- Спрайт: `generate_image` → `kz_mosque_raw.png` (первый вариант был с открытым
  внутренним двором — magenta-заливка не выкусывает замкнутый двор, и полый
  «бублик» плохо читается в 86px; перегенерирован компактным монолитом) →
  `scripts/process-kz-buildings.cjs` (`TARGET_BASEW.mosque = 350`) →
  `kz_mosque.png` 351×264, якорь `ax 175.5, ay 263, baseW 350`. На экране ~172×130px —
  между базаром (144) и казармами (211).
- Процедурный фолбэк имама в `pixelart.ts` перерисован: был **крест** на посохе и
  бурая роба — стали бирюзовый чапан, белая чалма и **полумесяц** на посохе.

### 5. Көпес — торговец с базара
- Новый юнит **`trader` «Көпес (торговец)»**: hp 90, 80🪙, 12 с, pop 1, `bld: 'market'`,
  безоружный. Хоткей **`0`**, иконка 🐫.
- `updateTrader` — круговой маршрут `свой базар → партнёр → базар`:
  - **партнёр** (`tradePartner`) — становище племени с `tribeRel === 'friend'` либо
    ставка/базар соперника при действующем `tradeRoute` и не в состоянии войны;
    из подходящих берётся самый **дальний** в пределах 9000;
  - выручка зависит от плеча: `min(90, 14 + leg/22)`, ×1.35 с техом «Чеканка монеты»;
  - на точках — короткая пауза «торга» с монетками, дома выручка идёт в казну
    **владельца** (`eres`/`res`), игроку — очки, флоатер и звук;
  - если дружба оборвалась или лагерь снесли, маршрут пересчитывается; без партнёров
    караван ждёт у базара.
- Свежий көпес выходит с базара сразу на маршрут (`trPhase='out'`), а не к точке сбора.
- Введён **`combatUnit(u)`** — «боевой юнит» вместо повсеместного `key !== 'villager'`.
  Иначе торговец попадал в выделение армии (Ctrl+A-аналог), уходил в attack-move и
  считался военной силой в оценке grievance. Скот/волки/шаруа/көпес исключены.

### Проверка
- `/tmp/tr/trade-test.mjs` — 17 автотестов (вне репозитория): выбор партнёра
  (нейтрал/друг/дальний/за-горизонтом/соперник без договора/с договором/война/
  недострой), формула выручки, стабильность слотов стада и arrival-шаг.
  Ключевая метрика: цель животного смещается **<0.1 ед. за кадр** вместо прыжков
  на ±40 — дрожь устранена; за 3 с «дыхание» уводит точку меньше чем на 12 ед.
- `tsc --noEmit` чисто, `npm run build` — 12,531.86 kB.

### Заметки на будущее
- Хоткеи заняты: цифры `0-9`, буквы `QERFKMZXC`, `H`, `B`, `V`, `W`, `T`, `G`, `Y`, `L`, `.`.
  Свободных букв под здания почти не осталось — следующее здание, вероятно, потребует
  подменю или Shift-модификатора.
- У торговца нет собственного спрайта: рисуется процедурным гуманоидом (фиолетовый
  чапан, тюбетейка, тюк за спиной, кошель). Если понадобится — прогнать через
  `scripts/process_kzf.py`, как остальных юнитов казахской расы.

## 1.0.064 — Спрайт торговца и племена как города-государства

Две темы: көпес получил настоящий арт вместо процедурной заглушки, и реализован
**пункт 7 из `plan.md`** — самая крупная упущенная возможность (племена как
city-states в духе Civ VI).

### 1. Спрайт көпес-торговца
- `generate_image` → полоса из 5 póz (`kz_trader_strip_raw.png`): бок, анфас, спина,
  шаг A, шаг B. Персонаж: пурпурно-золотой полосатый чапан, тюбетейка, тюк товара
  за спиной, кошель в руке — читается как купец, а не как воин.
- Нарезка штатным `scripts/split-kz-strip.cjs` (он сам находит фигуры по пустым
  полосам и приводит их к общему росту 150px):
  `node scripts/split-kz-strip.cjs kz_trader_strip 5 kz_trader kz_trader_f kz_trader_b kz_trader_wa kz_trader_wb`
- Якори в `sprite-art.ts`, `UNIT_TARGET_H.trader = 46`. Подключение в `pixelart.ts`:
  `KZ_BASE`, `KZ_FRONT_CYCLE`, `KZ_BACK_CYCLE`, `KZ_WALK_SIDE` — дальше `isKz`/`kzFB`
  подхватывают юнит автоматически (отдельный код рендера не понадобился).
- Проверка посадки (`/tmp/tr/render-check.cjs`): все 5 кадров на одной базовой линии,
  экранный рост 46.6px — вровень с шаруа (46) и имамом (44).
- Процедурный фолбэк в `drawHumanoid` НЕ удалён: он отрисует торговца, пока PNG
  не загрузился.

### 2. Племена как города-государства (Civ VI)
Раньше дружба за 40–60🪙 давала ровно одно: племя не нападает. Теперь это система
влияния с конкуренцией.

**Модель** (`nations.ts`):
- `TribeKind` — 5 типов: `military`, `trade`, `science`, `farm`, `craft`;
  `TRIBE_KIND_BY_ID` раздаёт типы семи народам (печенеги/огузы — военные,
  Хорезм/Россия — торговые, Бухара — научный, Коканд — аграрный, славяне — ремесленный).
- `TRIBE_TYPES[kind].levels` — по три описания бонусов на уровень.
- Пороги `ENVOY_TIERS = [1, 3, 6]`, цена `envoyCost(have) = 35 + have * 15`.

**Движок** (`engine.ts`): `envoys` / `rivalEnvoys` (Record по nationId),
`envoyLevel()`, `suzerain()` (у кого посланников больше; при равенстве — никто),
`bonusTier(kind, lv)` (есть ли активный бонус уровня), `sendEnvoy()`,
`updateEnvoyAI()` (джунгары раз в 45 с вкладываются, целя в племена, где игрок
близок к сюзеренитету), `updateTribeBonuses()` (дары воинов и доход торговых).

**Куда подключены бонусы:**
| Тип | Ур.1 | Ур.2 | Ур.3 |
|---|---|---|---|
| Военный | +10% HP войскам (`addUnit`) | воин в дар раз в 3 мин | дары раз в 2 мин, включая батыра/жасауыла |
| Торговый | +3🪙 каждые 8 с | обмен на базаре −15% (`tradeRate`) | караваны көпес +50% (`updateTrader`) |
| Научный | исследования +10% | +25% (`researchMult`) | стройка +20% (`updateBuildings`) |
| Аграрный | пашни +15% еды | (то же, задел на рост стад) | пашни/дойка +35% (`farmMult`) |
| Ремесленный | постройки −10% дерева | −20% (`woodDiscount`) | добыча +15% (`gatherMult`) |

- Введён `bldCost(key)` — единая цена постройки со скидкой; все три места оплаты
  (`enterPlacement`, `placeSingle`, клик размещения) переведены на него.
- Скидка прокинута в HUD (`woodDiscount`), в доке цены показываются уже сниженными
  через `bldCostOf(key, wdisc)` — иначе UI врал бы игроку.
- Враждебность или сюзеренитет джунгар **гасят** бонусы (`bonusTier` это проверяет).
- Сериализация: `envoys` / `rivalEnvoys` добавлены в `nations` — старые сейвы грузятся
  (пустые объекты по умолчанию).

**UI** (`App.tsx`): в модалке дипломатии у каждого племени появился блок города-
государства — тип народа, счётчик «вы N против джунгары M», сколько посланников до
следующего уровня, три строки бонусов с отметкой активных и кнопка
«✉️ Посланник (N🪙)». Плашки «👑 вы сюзерен» / «⚠️ под джунгарами».

### Проверка
- `/tmp/tr/envoy-test.mjs` — 26 автотестов: пороги уровней, счётчик до следующего,
  рост цены, сюзеренитет (в т.ч. равенство и перехват 2:3 → 4:3), гашение бонусов
  враждебностью и джунгарами, суммирование по двум племенам одного типа, пересчёт
  цены юрты 50🪵 → 40🪵.
- `/tmp/tr/trade-test.mjs` (17 тестов из 1.0.063) — по-прежнему зелёный.
- `tsc --noEmit` чисто, `npm run build` — 12,692.74 kB.

### Заметки
- Дар воина уважает лимит населения и не выдаётся, если нет ставки.
- `updateEnvoyAI` намеренно редкий (45 с) и вероятностный — иначе на высокой
  сложности джунгары скупали бы всю степь за первые минуты.

## 1.0.065 — Көпес на верблюде и случайные события степи

### 1. Торговец пересел на верблюда
По просьбе: караванщик должен ехать, а не идти пешком.
- `generate_image` → `kz_trader_camel_raw.png`: полоса из 5 поз (бок, анфас, спина,
  шаг A, шаг B). Двугорбый бактриан с коврами и тюками между горбами, латунный
  колокольчик, сверху всадник в пурпурно-золотом чапане и тюбетейке.
- Нарезка `split-kz-strip.cjs` поверх ТЕХ ЖЕ имён (`kz_trader*`) — код рендера,
  подключённый в 1.0.064, менять не пришлось, только якори.
- Якори обновлены (бок стал 119×152 против прежних 68×152), `UNIT_TARGET_H.trader`
  поднят 46 → **58**: верхом юнит выше пешего, вровень с конницей (60).
- `mounted` в `pixelart.ts` теперь включает `trader` — широкая эллиптическая тень
  и кольцо выделения как у всадников.
- **Важная тонкость:** `mounted` в диспетчере фолбэка уводил бы торговца в
  `drawHorse` + `drawRider` (рыцарь на коне вместо купца), если PNG не успел
  загрузиться. Добавлено исключение `mounted && u.key !== 'trader'` — фолбэк
  рисует пешего купца, что честнее коня.
- Анимация: `isKz` даёт `upright = true`, поэтому верблюд идёт «мягким подскоком»
  без лошадиного крена — то, что нужно для его развалистой походки.

### 2. Случайные события степи (пункт 8 плана)
Civ-механика: событие всплывает модалкой и требует выбора из 2–3 реакций с разными
последствиями.

**Шесть событий** (`eventDefs()`), у каждого условие `can()`, чтобы не выпадала
бессмыслица (джут без скота, курган без разведчика):
| Событие | Условие | Выбор |
|---|---|---|
| ❄️ Джут | ≥3 головы скота | зарезать треть (+еда, −поголовье) / откочевать (−90🪵) |
| 🐑 Приплод | есть загон | +3 головы и +80🍖 |
| 🐫 Караван Шёлкового пути | ≥90🪙 | купить (+260🪵 +160🍖) / отказать |
| 🤒 Поветрие | ≥4 шаруа | имамы лечат за 60🪙 (25 с) / терпеть (100 с) |
| 🌵 Засуха | есть пашня | арыки за 80🪵 (30 с) / ждать дождя (150 с) |
| ⚱️ Курган | есть барлаушы | вскрыть (+220🪙, +неприязнь) / почтить (+350 очков) |

- Планировщик `updateEvents()`: не чаще чем раз в 3.5 минуты, дальше редкий
  случайный триггер. Ротация через `eventSeen` — пока не пройдёт круг, событие не
  повторяется; затем список сбрасывается.
- Тикающие последствия: `droughtT` режет `farmMult()` до ×0.55, `plagueT` режет
  `gatherMult()` до ×0.6. Аграрный союз частично компенсирует засуху.
- Сериализация: `events { eventT, seen, drought, plague }` — старые сейвы грузятся.
- UI: модалка выбора (`hud.event`) и плашки в HUD «🌵 Засуха: Nс» / «🤒 Поветрие: Nс»,
  чтобы игрок понимал причину просадки экономики.
- Модалка НЕ ставит паузу — так же, как приветствия правителей (игра остаётся RTS).

### Проверка
- `/tmp/tr/events-test.mjs` — 23 теста: условия отбора всех шести (в т.ч. границы
  2 против 3 голов, 89 против 90 золота), уникальность за круг из 6, сброс на 7-м,
  формула забоя, множители засухи/мора и их сочетание с бонусами.
- `/tmp/tr/camel-check.cjs` — посадка 5 кадров верблюда на одной базовой линии.
- Суммарно 66 автотестов зелёные. `tsc --noEmit` чисто, build 12,704.21 kB.

## 1.0.066

Два пункта бэклога AoE: **конверсия имама** (п.3) и **тревога «нас атакуют»** (п.4).

### Камлание имама (обращение в свою веру)

Правый клик выделенным имамом по чужому юниту начинает «камлание».
Константы в `engine.ts`: `CONV_RANGE = 120`, `CONV_TIME = 7` с.

- Поля `Unit`: `convTarget` (id жертвы), `convT` (накопленные секунды),
  `convertedBy` / `convProg` (на самой жертве — для отрисовки полосы), `converts` (счётчик у имама).
- Логика в начале `updateMonk`: пока идёт камлание, имам **не лечит** — это цена выбора.
  Если цель вышла за `CONV_RANGE`, имам догоняет, а прогресс откатывается вдвое медленнее набора
  (`dt * 0.5`); если она ушла дальше `CONV_RANGE * 2.5` и прогресс обнулился — камлание срывается.
- Нельзя обращать скот, дичь и волков (`sheep/cow/deer/wolf`) — только людей.
- `finishConvert` меняет `owner`, сбрасывает `tribe/aggro/herder/penId/pastureId`
  (обращённый чужой пастух перестаёт обслуживать чужой загон), даёт +120 очков и баннер.
- `killUnit` теперь прерывает камлание с обеих сторон: убитый имам снимает полосу с жертвы,
  убитая жертва освобождает своего имама — иначе полоса «зависала» над трупом.
- ИИ-имам джунгар сам ищет ближайшую жертву в радиусе (в состоянии войны), так что
  переманивание работает в обе стороны. Увод юнита у игрока поднимает тревогу.
- `issueSmart` делит выделение: имамы идут в `orderConvert`, остальные — в `orderAttack`.
  Если цель необращаема, а в выделении только имамы — обычная атака.
- Отрисовка: сиреневая полоса `#c4b5fd` с меткой ☾ над жертвой (`drawUnitIso`), искры во время камлания.

### Тревога «нас атакуют»

- Поля `alert: { x, y, t, sub } | null` и `lastAlertT`; метод `raiseAlert(x, y, sub)` с антиспамом 12 с.
- Срабатывает **только когда бой вне экрана** (`inView` с запасом 40 для юнита и 60 для здания) —
  если игрок и так смотрит на драку, гудеть незачем.
- `sound.alarm()` — двухнотный боевой рог (saw 196 → 147 Гц + подсветка 392 Гц), гейт 6 с.
- Маркер живёт 20 с: пульсирующее красное кольцо на мини-карте (расходящаяся волна).
- В HUD — мигающая плашка «⚠️ НАС АТАКУЮТ!», клик по ней вызывает `jumpToAlert()` и центрует камеру.
- Триггеры: `damageUnit` (юнита игрока бьёт чужой), `damageBld` (здание игрока), `finishConvert` (увели юнита).

Тест `/tmp/tr/convert-test.mjs` — 43/43: правила отбора цели, накопление и откат прогресса,
границы радиусов 119/121 и 299/301, смена владельца и сброс ролей, очистка при смерти,
разделение приказа, антиспам тревоги и условие «только вне экрана».

## 1.0.067

Пункт 2 бэклога: **линии апгрейда юнитов** (AoE-цепочки родов войск).

### Модель

В `config.ts` добавлен `UPGRADES: Record<string, UpgradeDef>` — 8 ступеней, по 2 на род войск.
`UpgradeDef extends TechDef`, поэтому апгрейды идут тем же механизмом `research()`/`applyTech()`,
что и обычные технологии: очередь в здании, таймер, `researchMult()` от союза с научным племенем.

| Род | Ступень 1 (Век жузов) | Ступень 2 (Век батыров) | Здание |
|---|---|---|---|
| Сарбаз | Ауыр сарбаз (+25% HP, +20% атк) | Хан сарбазы (+30% HP, +30% атк) | Казармы |
| Найзагер | Ауыр найзагер (+25% HP, +20% атк) | Қалқан найзагер (+35% HP, +25% атк) | Казармы |
| Мерген | Қыран мерген (+20% HP, +25% атк, +8% дальн.) | Сұңқар мерген (+25% HP, +30% атк, +10% дальн.) | Казармы |
| Жасауыл | Ауыр жасауыл (+25% HP, +20% атк) | Тұлпар жасауыл (+30% HP, +30% атк, +8% скор.) | Конюшня |

Множители **накапливаются**: Хан сарбазы = 110 HP × 1.25 × 1.30 = 178.75.

### Ключевые решения

- **Ретроактивность.** `applyUpgrade` проходит по всем живым юнитам рода и усиливает их.
  Раненый сохраняет ДОЛЮ здоровья (`frac = hp/maxHp`), а не абсолютное значение — иначе
  апгрейд лечил бы армию, и его было бы выгодно жать посреди боя.
- **Новобранцы.** `addUnit` умножает статы на `upgMult(key, owner)`. Без этого воин,
  обученный ПОСЛЕ исследования, выходил бы слабее старых — классическая ловушка,
  поскольку `applyTech` мутирует только уже существующие юниты.
- **Ступень 2 требует ступень 1** — проверка и в `research()`, и в `upgradeRows()`, и в досье.
- **Враг растёт по эпохе.** ИИ не исследует техи, поэтому `upgTier` для `enemy` выводится
  из `eage`: ступень 1 в Веке батыров, ступень 2 в Веке Абылай хана. Иначе поздняя игра
  превращалась бы в тир по безоружным джунгарам.
- **Имена.** `unitName(unit, owner)` даёт имя с учётом ступени; оно идёт в панель выделения,
  в кнопки обучения (через `hud.unitNames`) и в сводку волны — игрок видит «3×Ауыр сарбаз».

### Визуальная метка ступени

Сперва рисовался султан на макушке, но проверка `/tmp/tr/plume-x.cjs` показала, что верхний
непрозрачный пиксель кадра — это НЕ голова: у найзагера это наконечник пики (x=79 при якоре 44.6),
у мергена — лук, у жасауыла — пика всадника (x=124). Перо висело бы в воздухе.
Заменено на **перекраску брони**: ступень 1 — холодный стальной отлив `#e2e8f0` (alpha .20),
ступень 2 — золочёный `#fbbf24` (alpha .30). Тонирование через `source-atop` (бережёт альфу),
результат кладётся в `TINT_CACHE` по ключу `кадр|цвет|alpha` — перекраска идёт один раз на кадр,
а не каждый тик на каждого юнита.

### Попутно исправлен баг сохранений

В `loadFromSave` юниты создавались ДО восстановления `this.age`/`this.eage`/`this.tech`,
а `addUnit` масштабирует статы по эпохе — загруженная армия выходила слабее сохранённой
(в Веке Абылая теряла до 55% HP и атаки). Восстановление эпохи и технологий поднято
в начало метода, до цикла создания юнитов.

Тест `/tmp/tr/upgrade-test.mjs` — 50/50: структура линий, накопление множителей, имена,
ретроактивность с сохранением доли HP, паритет новобранца и ветерана, предусловия ступеней,
ступени врага по эпохе, порядок восстановления сохранёнки, кэш перекраски.

## 1.0.068

**Посменная работа шаруа** (запрос пользователя, п.1) — усталость, отдых у юрт
с настоящими сценками быта и бонус «после выходных».

### Единые сутки

В игре уже был визуальный цикл день→ночь, но он крутился от `this.time` по своему
счётчику и ни с чем не был связан. Теперь есть **одни сутки на всю игру**:
`DAY_LEN = 240` с, `dayT` (фаза), `dayNum` (номер дня). Освещение читает фазу из
`dayPhase()`, оттуда же живут смены — картинка и логика больше не расходятся.
Договорённость фазы унаследована от старого визуала: **0 — полдень, 0.5 — полночь**,
темнота = `0.5*(1-cos(2πφ))`. Хелперы: `darkness()`, `isNight()`, `dayName()`, `dayIcon()`.
Сутки сериализуются (`day: { dayT, dayNum, restedTotal }`).

### Усталость и смены

- Поля `Unit`: `fatigue` (0..1), `resting`, `restT`, `restKind`, `restX/restY`,
  `freshT` (бодрость), `shiftBack` (куда вернуться).
- `TIRE_RATE = 1/150` — полная усталость за 150 с непрерывной работы; **ночью ×1.6**.
  Копится только в состояниях `gather`/`return`/`build` — стоящий без дела не устаёт.
- `updateShifts` раз в 2 с отбирает самых уставших. Порог — 0.8 днём и **0.55 ночью**.
- **Ключевое ограничение: одновременно отдыхает не больше трети артели**
  (`cap = max(1, floor(n/3))`). Иначе добыча вставала бы колом и «выходные»
  ощущались бы наказанием, а не бонусом. Единственного работника не отпускаем вовсе.
- Пастухов не трогаем (у них свой цикл выпаса), только что отдохнувших — тоже.
- После отдыха `fatigue = 0`, `freshT = 70` с и работник **возвращается к тому же
  ресурсу**, с которого ушёл (если тот не выработан).

### Производительность

Новый `workMult(u)` — личный множитель поверх глобального `gatherMult()`:
отдохнувший даёт **+35%** (`FRESH_BONUS`), вымотанный сверх 0.7 теряет до 24%.
Все шесть точек добычи шаруа (пашня, дойка, ягоды, узлы) переведены на `workMult(u)`;
`gatherMult()` остался глобальным для техов, союзов и поветрия.

### Сценки быта у юрт

Три полосы через `split-kz-strip.cjs`, по 3–4 кадра:
- `kz_swing_l/c/r` — **алтыбакан**: девушка качается на шестиопорных качелях (H=72);
- `kz_kazan_a/b/c` — **казан**: женщина помешивает, поднимает половник, утирает лоб (H=54);
- `kz_asyk_a/b/c` — **асыки**: мужчина бросает битку, следит за костью, радуется (H=50).

Женщины идут к казану (55%) или на алтыбакан, мужчины — в асыки (60%) или на качели.
Сценки рисуются с `upright`, без подскока, крена и выпада: рама и казан стоят на земле,
любое покачивание «поехало» бы вместе с композицией. Кадр выбирается по фазе `u.anim`.

### Защита от конфликтов (найдено при проверке)

- **Прямой приказ игрока отменяет отдых** (`issueSmart`) — игрок главнее расписания.
  Усталость при этом НЕ обнуляется: встал с качелей недоотдохнувшим.
- Отдыхающие **исключены из счётчика простоя**, из `idleSelect()` и из `workIdle()` —
  иначе индикатор простоя врал бы, а кнопка «за работу» срывала бы людей с законного отдыха.
- **Доярка освобождает загон** при уходе на отдых: `sendToRest` чистит `pen.milkWid`.
  Без этого загон навсегда оставался «занят» ушедшей работницей и дойка вставала.

### HUD

Плашка «☀️/🌙 День N» с подсказкой и тремя счётчиками: 😴 отдыхают, ✨ бодрые, 💤 устали.

Тест `/tmp/tr/shift-test.mjs` — 55/55: фазы суток и смена дня, накопление усталости
(в т.ч. ночной множитель), пороги и приоритет отбора, лимит трети артели, восстановление
и таяние бодрости, отмена отдыха приказом, возврат к своему ресурсу, распределение занятий.

## 1.0.069

Правки сценок отдыха по замечаниям с игрового скриншота: **масштаб** и **консистентность казана**.

### Масштаб (замечание 1)

На скриншоте девушка на алтыбакане была вдвое мельче шаруа, а женщина у казана — вдвое крупнее.
Причина в том, как работает `split-kz-strip.cjs`: он нормирует **самую низкую фигуру полосы**
в 150 логических px. Эталон у каждой полосы оказался свой:

| Сценка | Что стало эталоном 150px | Множитель к росту шаруа |
|---|---|---|
| Алтыбакан | вся РАМА качелей (~3.2 м) | ×1.9 → H=87 |
| Казан | женщина стоя | ×1.0 → H=46 |
| Асыки | мужчина НА КОРТОЧКАХ | ×0.65 → H=30 |

Захардкоженные «на глаз» H (72/54/50) заменены на расчёт от `UNIT_TARGET_H.villager`:
теперь при изменении роста шаруа сценки поедут за ним автоматически.

### Казан переснят (замечание 2)

Прежняя полоса была несогласована: женщина меняла рост, позу и положение относительно
котла между кадрами. Новая полоса сгенерирована с явным требованием «та же женщина,
та же поза, та же высота головы, меняется ТОЛЬКО рука с половником»: три кадра вышли
123×152, 124×152, 123×152 с якорем 60.7 — фигура больше не прыгает.

### Инструмент проверки

Цветовые эвристики для поиска «человека в кадре» оказались ненадёжны (ловили платок
вместо фигуры, чёрный контур опор считали телом). Вместо них:
- `tests/rest-preview.cjs` — собирает PNG со сценками и обычным шаруа **на одной базовой
  линии** в реальных экранных высотах; соразмерность видно глазами (`tests/rest-preview.png`);
- `tests/rest-scale.mjs` — 19 проверок формул масштаба и согласованности кадров внутри
  сценки. Тест поймал, что у асыков разброс высот 80px, и это оказалось **корректно**:
  кадр «встал и радуется» обязан быть выше приседа — проверка уточнена до
  «присед ниже стойки, стойка = росту шаруа».

Удалены неиспользуемые кадры-пустышки `kz_swing_0`, `kz_kazan_0` и осиротевший якорь.

---

## 1.0.070 — Азан и намаз + переснятые асыки со зрителями

Два пункта: переделать неконсистентную анимацию игры в асыки (кости должны быть хорошо
видны, рядом — наблюдающие люди) и подключить `public/voices/azan.mp3`, который
пользователь сам добавил в `main` коммитом `b85b96e`.

### Асыки: от одиночной фигуры к групповой сценке

Прежняя сценка была одиночным мужчиной на корточках: кости-бабки терялись (россыпь
мелких пятен нарезчик выбрасывал как мусор), зрителей не было вовсе, а кадры «дышали».

**Почему кадры «дышали».** Причина оказалась не в рисунке, а в нарезке. Каждый кадр
клался на СВОЙ холст по своим границам, а движок вписывает холст в фиксированную высоту
(`H / an.h` в `pixelart.ts:711`). Кадр с поднятыми руками выше кадра с опущенными →
разные `an.h` → один и тот же человек на соседних кадрах получал разный масштаб и
съезжал вбок. Отсюда дёрганье, которое не лечится перерисовкой.

**Решение — общий холст.** Новый `scripts/split-kz-scenes.cjs` (полосу СЦЕН нельзя резать
тем же `split-kz-strip.cjs`, который сегментирует по связным компонентам = по отдельным
фигурам и разорвал бы группу):

1. раскладка ищется по пустым magenta-полосам: сначала строки, потом столбцы —
   генератор упорно выдаёт сетку 2×2 вместо одного ряда, и это уже учтено параметрами;
2. каждая ячейка сетки берётся ЦЕЛИКОМ как одна сцена (метальщик + зрители + кости);
3. в сцене находится **метальщик** — первая группа «высоких» колонок (столбик выше 30 %
   высоты сцены). Порог по высоте обязателен: летящие кости и россыпь на земле дают
   короткие столбики и склеивали метальщика со зрителями в одну фигуру;
4. все кадры кладутся на **общий холст** (283×157) с общим якорем по метальщику, низ =
   линия земли. Теперь `an.h` одинаков у всех кадров → масштаб постоянен, фигура стоит
   как вкопанная, меняются только рука и кости.

Заодно убрана фиолетовая бахрома: полупрозрачные пиксели по контуру «замешаны» с фоном
и остаются лиловыми. В палитре сценки (олива, охра, серый, кость) пурпурного нет,
поэтому пиксель с `R−G > 28 и B−G > 28` считается грязью от фона и вырезается.

Из четырёх сгенерированных сцен взяты три с ОДИНАКОВЫМ числом зрителей (в четвёртой
генератор нарисовал третьего) — иначе зритель появлялся бы и исчезал на каждом цикле.

| Кадр | Что происходит |
|---|---|
| `kz_asyk_a` | замах: асык поднят над головой, зрители ждут |
| `kz_asyk_b` | бросок: кость в полёте, зрители вскинули руки |
| `kz_asyk_c` | кость упала в кон, зрители ликуют |

Цикл `KZ_ASYK` теперь идёт **только вперёд** — `a, a, b, c`. Прежний вариант `a, b, c, b`
возвращал последний кадр к среднему, и брошенная кость «прилетала обратно в руку»;
удвоенный первый кадр даёт паузу на прицеливание.

**Масштаб.** Сценка групповая, поэтому эталон 150px сменился: раньше им был мужчина на
корточках (×0.65 → H=30), теперь — весь холст, где стоящий зритель занимает 148 из
157px. Рост шаруа достигается при `H = 46 · 157/148 ≈ 49`, то есть **×1.06**.
Сценка стала заметно крупнее (88×49 против 57×32) — в ней теперь трое.

### Азан и намаз

`public/voices/azan.mp3` — 46.6 с, 128 kbps (длительность посчитана по MPEG-фреймам,
без сторонних библиотек; тот же код лежит в тесте). Константы: `AZAN_LEN = 47`,
`PRAYER_LEN = 20`, `BEREKE_LEN = 90`.

**Расписание.** Азан звучит трижды в сутки, привязка — к существующей фазе `dayPhase()`,
**второй счётчик суток не заводился**: `azanPhase = [0.78, 0.02, 0.40]` — таң (рассвет),
бесін (полдень), ақшам (закат). Окно срабатывания `|ph − target| < 0.012` ≈ 5.8 с
реального времени: шире игрового тика даже на максимальной скорости, но уже, чем
промежуток между намазами. `azanDone` помнит отзвучавшие намазы и сбрасывается со
сменой суток. Мечеть (`Мешіт-медресе`) обязательна — без неё азана нет.

**Кто идёт.** Только мирные игрока: шаруа, көпес, имам. Пастух стада в степи не бросит,
отбивающийся от врага — тоже. Если враг ближе 900 px от ставки, намаз пропускается:
оборона важнее. Пришедших рассаживают рядами по пять перед мечетью лицом к кибле,
после намаза каждый возвращается к своему ресурсу (`prayBack`).

**Береке.** Сила благодати зависит от явки: `berekePower = clamp(пришло / (всего · 0.6),
0.25, 1)` — полный бонус уже при явке 60 % общины. Даёт `×(1 + 0.15 · berekePower)` к
добыче на 90 с, +60 очков и мораль. При полной явке каждый пятый намаз приносит по
посланнику к знакомым мусульманским народам (хорезм, коканд, бухара).

**Звук.** Азан играет одним экземпляром: повторный вызов поверх играющего запрещён
(`azanPlaying()`), громкость `max(0.35, voiceVolume)` — слышен поверх шума боя,
`stopVoice()` глушит и его. Блокировка автоплея браузером не роняет игру.

**Грабли, проверенные заранее.** Те же конфликты, что ловились у механики отдыха:
уходящая на намаз доярка теперь освобождает загон (иначе `pen.milkWid` навсегда занят
ушедшей работницей), а кнопка «Простой» больше не срывает людей с молитвы. Приказ
игрока намаз отменяет — принуждения нет.

### Тесты

`tests/prayer-test.mjs` — 46 проверок; константы и формулы вытаскиваются **из самого
`engine.ts`**, поэтому тест не разъедется с кодом. Проверяет длительность mp3 против
`AZAN_LEN`, непересечение окон намазов, что между намазами хватает времени на сам намаз,
границы благодати, сохранение/загрузку и все перечисленные конфликты механик.
`tests/rest-scale.mjs` (20/20) переписан под групповую сценку: вместо «присед ниже
стойки» теперь требование **пиксель-в-пиксель одинаковых холстов** у всех кадров асыков —
ровно та поломка, что вызывала дёрганье.

---

## 1.0.071 — Сутки 30 минут

Прежние сутки длились 240 с (4 минуты) — за партию проходило два десятка «дней»,
и суточный цикл читался как мигание освещения, а не как смена времени суток.
Сутки удлинены до **1800 с (30 минут)** при скорости 1×.

### Главное: сутки стали единственной ручкой времени

Простая замена `DAY_LEN = 240` на `1800` была бы тихим ребалансом. Константы смен
подбирались вручную под старые сутки, и в новых означали бы совсем другое:

| Константа | Было (доля суток) | Стало бы при 30 мин | Как исправлено |
|---|---|---|---|
| `TIRE_RATE` | 150 с = 0.625 суток | 150 с = 1/12 суток → 12 смен за день | `1/(DAY·0.625)` |
| `REST_TIME` | 26 с = 0.108 суток | 26 с = 1/70 суток → отдых-миг | `DAY·0.108` |
| `FRESH_TIME` | 70 с = 0.292 суток | 70 с = 1/26 суток | `DAY·0.292` |

Введена экспортируемая `DAY_LEN_SEC` в `engine.ts`; всё «раз в день» и «за смену»
считается ОТ НЕЁ в долях. Доли взяты **ровно те, что сложились при 240-секундных
сутках**, поэтому ритм сохранён: работник по-прежнему отдыхает **1.6 раза за сутки**
(проверяется тестом), просто смена теперь длится 18.8 минут, а не 2.5.

Разброс длительности отдыха тоже стал относительным (`×rand(0.85, 1.2)` вместо
`±(-4, 6)` секунд): при 194-секундном отдыхе прежний разброс был незаметен, и артель
уходила бы на отдых и возвращалась строем.

### Намаз пересчитан

- `PRAYER_LEN` = 4.5 % суток ≈ **81 с** (было 20 с). Намаз обязан длиться дольше самой
  записи азана (46.6 с), иначе люди расходятся под ещё звучащий призыв; заодно хватает
  времени дойти до мечети через полкарты.
- `BEREKE_LEN` = 20 % суток ≈ **6 минут** (было 90 с).
- **Окно срабатывания азана задано в секундах, а не в долях суток.** Доля `0.012` при
  240-секундных сутках давала 5.8 с, а при 30-минутных растянулась бы до 43 с. Теперь
  `win = 4 / DAY_LEN` — точечное окно с запасом на медленный кадр и ускорение 2×.

### Два бага, которые вскрыл прогон суток

Модельный прогон трёх игровых дней (`/tmp/daysim.cjs`) показал то, что при
четырёхминутных сутках проскакивало незамеченным:

1. **«Закатный» ақшам звучал глухой ночью.** Фаза 0.40 при договорённости
   «0 = полдень, 0.5 = полночь» — это темнота **0.90**. Расписание исправлено на
   `[0.75, 0.02, 0.25]`: таң на рассвете, бесін в полдень, ақшам на закате. Теперь
   азаны приходятся на Таң / Күндіз / Кеш, ни один не попадает в Түн.
2. **Тёплая «закатная» подсветка светила в полночь.** Формула `1 − |ph − 0.5|·8`
   давала максимум ровно в полночь. Исправлено на расстояние до ближайшей из фаз
   0.25 и 0.75 — тёплый свет теперь на закате и рассвете, в полночь его нет.

### Совместимость сейвов

Старые сейвы писали `dayT` в пределах прежних 240 с. Подставленное как есть, это
число заклинило бы часы около полудня, поэтому в сейв добавлена **доля суток**
(`dayF`), которая переносится между любыми длинами; старое поле читается как
запасной вариант и подрезается по текущей длине суток. Благодать из старого сейва
подрезается до нового `BEREKE_LEN`.

### HUD

Таймеры намаза и благодати переведены в формат `м:сс` (6 минут благодати в виде
«360с» не читались). В плашке дня рядом с номером показывается фаза суток
(Күндіз / Кеш / Түн / Таң) — при получасовых сутках время дня важнее номера дня.

### Тесты

Константы в `tests/shift-test.mjs` и `tests/prayer-test.mjs` больше не дублируются
цифрами — вытаскиваются **из `engine.ts`**, поэтому следующая смена длины суток не
потребует правки тестов. Добавлены проверки: сутки равны 30 минутам, ритм смен
(1.2–2.2 отдыха за сутки), ни один намаз не попадает в глухую ночь, тёплый свет
приходится на закат и рассвет, а сейв переживает смену длины суток.
Итого 67/67 (`shift-test`) и 54/54 (`prayer-test`).

---

## 1.0.072 — Ночь стала явным бюджетом времени: 4 минуты из 30

Пользователь попросил выделить на ночь 4 минуты. Прежде «ночь» не была величиной,
которую можно назначить: темнота считалась косинусом `0.5·(1−cos 2πφ)`, а ночью
считалось всё, что темнее порога 0.62. Из 30-минутных суток так получалось
**12.7 минуты** — и это ещё не совпадало с подписью в HUD.

### Что было сломано

| Момент суток | Подпись в HUD | Механика |
|---|---|---|
| 8.7 мин | 🌇 Кеш (вечер) | уже ночь: усталость ×1.6 |
| 11.2–18.8 мин | 🌙 Түн (ночь) | ночь |
| 21.3 мин | 🌅 Таң (утро) | ещё ночь |

Около 5 минут за сутки игрок видел «вечер» или «утро», пока рабочие уже выматывались
по ночной ставке. Подписи делили сутки на четыре равные четверти, а механика жила по
порогу на косинусе — две независимые модели времени.

### Как сделано

Введены `NIGHT_LEN_SEC = 240` и `TWILIGHT_SEC = 240` рядом с `DAY_LEN_SEC`.
Сутки теперь раскладываются явно:

```
18 мин день + 4 мин закат + 4 мин ночь + 4 мин рассвет = 30 мин
```

`darkness()` переписана с косинуса на **явное расписание**: посреди ночи ровно 1,
днём ровно 0, между ними плавная ступень (smoothstep) длиной в сумерки. Косинус
растягивал полутьму на 82 % суток, из-за чего «ночь» была вопросом выбора порога.

`isNight()` больше не сравнивает темноту с порогом, а просто проверяет попадание
в отведённые ночи 4 минуты. Подписи и иконки считаются от **тех же** границ, поэтому
«Түн» показывается ровно тогда, когда действуют ночные правила — тест перебирает
1000 точек суток и требует нулевого расхождения.

| Отрезок | Минуты суток | Фаза | Подпись |
|---|---|---|---|
| День | 0–9 и 21–30 | тьма 0 | ☀️ Күндіз (день) |
| Закат | 9–13 | 0 → 1 | 🌇 Кеш (закат) |
| Ночь | 13–17 | тьма 1 | 🌙 Түн (ночь) |
| Рассвет | 17–21 | 1 → 0 | 🌅 Таң (рассвет) |

### Следствия, которые пришлось учесть

- **Рендер держал свою копию формулы освещения.** В `drawUI` темнота считалась
  отдельным косинусом — картинка разошлась бы с механикой. Теперь вызывается общая
  `darkness()`, порог отрисовки снижен с 0.08 до 0.02 (новая кривая даёт честный ноль
  днём, и старый порог просто съедал бы начало сумерек).
- **Тёплая подсветка** привязана к середине сумерек, а не к фиксированным 0.25/0.75:
  сдвинется длина ночи — закатное золото сдвинется вместе с ней.
- **Расписание азана следует за освещением.** Числа `[0.75, 0.02, 0.25]` при новой
  кривой оказались бы в ярком дне (темнота 0.00). Теперь ақшам и таң вычисляются как
  середина сумерек, где темнота ровно 0.5, — при любой длине ночи «закатный» намаз
  останется закатным. Промежутки между намазами: 11.6 / 10.4 / 8.0 мин.
- **Ритм смен сохранился:** 1.7 отдыха за сутки (было 1.6). Ночь короче, но ночной
  штраф ×1.6 действует все 4 минуты в полную силу, а не размазан порогом.

### Тесты

`shift-test` 75/75, `prayer-test` 57/57. Добавлены проверки: ночь длится ровно
4 минуты (доля считается перебором суток, а не формулой), день+сумерки+ночь дают
ровно 30 минут, подпись «Түн» совпадает с `isNight()` во всех 1000 точках суток,
тёплый свет приходится на середину сумерек, а сумеречные намазы попадают в полутьму.
Константы по-прежнему читаются из `engine.ts`, так что длину ночи можно менять одной
строкой.

---

## 1.0.073 — Ночные факелы у зданий

Ночь стала настоящей темнотой (4 минуты полной тьмы), и посёлок в ней пропадал.
Нужен был свет у построек.

### Почему это почти не рисование

Вопрос звучал как «наверное, это надо нарисовать?». Спрайты тут решают только
десятую часть задачи. Нарисованный «светлый круг» под зданием кладётся ПОВЕРХ
затемнения и даёт мутное молочное пятно: подсвечивается сама пелена ночи, а не
земля под ней. Освещение — не картинка, а операция над слоем темноты.

Сделано так:

1. затемнение рисуется не прямо на холст, а в **отдельный слой** (второй канвас);
2. каждый источник **вырезает** в этом слое дырку через `destination-out` —
   свет буквально снимает ночь, обнажая настоящие цвета земли и построек;
3. готовый слой с дырками выводится на экран одним `drawImage`;
4. сверху добавляется слабый тёплый отблеск (`lighter`), чтобы пятно читалось как
   огонь, а не как выцветшая проплешина.

Второй холст обязателен: вырезать из основного нельзя — дырка выела бы саму карту.
Он создаётся один раз и переиспользуется, размер следит за окном; если контекст
почему-то недоступен, есть запасной путь с обычным затемнением.

Нарисован при этом ровно один маленький спрайт — сам факел (`drawTorch` в
`pixelart.ts`): шест, чаша и пламя из четырёх язычков от тёмно-оранжевого к белому.
Он ставится парой по бокам от входа и мерцает; фаза мерцания разведена по `b.id`,
иначе все факелы посёлка пульсировали бы в унисон.

### Правила освещения

| Условие | Поведение |
|---|---|
| День (тьма < 0.12) | факелов нет вовсе, циклы по зданиям не выполняются |
| Сумерки | разгораются плавно: `lit = (тьма − 0.12) / 0.5` |
| Глухая ночь | горят в полную силу |
| Стройка не завершена | не светит |
| Дувал и ворота | факелов нет |
| Земля не разведана | свет не рисуется — иначе выдавал бы чужой лагерь |
| Цикл суток выключен в настройках | факелов нет |

Радиус зависит от размера постройки: ханская ставка светит далеко, юрта чуть-чуть
(`size · 0.85 + 34`). Первый вариант с радиусом `size · 1.5 + 60` пришлось убрать —
пятна сливались и съедали саму ночь, превращая её в мутные сумерки по всей карте.
Центр снимает 78 % темноты, а не все 100: у зданий должна оставаться ночь, просто
различимая.

### Производительность

Источники собираются **одним проходом** по зданиям — список нужен и слою ночи, и
тёплому отблеску, а куллинг с проверкой тумана самый дорогой. Изначально проходов
было два (240 градиентов за кадр при 120 постройках), стало вдвое меньше. Днём
циклы не выполняются вообще.

### Проверка

Браузера в песочнице нет, поэтому композиция повторена офлайн в
`tests/night-light.cjs`: та же математика `destination-out`, результат — картинка
`tests/night-light.png` с тремя постройками разного размера. Первый прогон показал,
что свет добивает до дальнего угла (альфа ночи 0.361 вместо 0.42) — радиус был
уменьшен, теперь вдали ровно 0.420, то есть нетронутая ночь.

`tests/light-test.mjs` — 34 проверки логики: свет именно вырезает ночь, холст
переиспользуется, градиент гаснет к краю в ноль, в центре остаётся немного темноты,
факелы зажигаются только в сумерках и не светят сквозь туман войны.

## 1.0.074 — Пастух не залипает, казан и алтыбакан у ставки, плашка волны только в войну

Четыре правки по итогам игровой сессии: сломанный пастух, две постоянные
декорации стойбища и лишний элемент HUD в мирное время.

### 1. Пастух завис на месте и не выходил за пределы базы

Две независимые причины, обе воспроизведены до правки.

**Причина А — `moveToward` не умеет обходить преграды.** Все пять перемещений в
`updateShepherd` шли через `moveToward`, а это движение строго по прямой: упёршись
в гору или здание, юнит скользит вдоль одной оси, а когда перекрыты обе — молча
стоит. Прогон `node -e` с полосой-препятствием: цель x=500, пастух встал на
x=99.6, пройдя 49.6 из 450 единиц.

**Причина Б — пустое стадо намертво вешало цикл.** В фазе `graze` пастух двигался
относительно `herdCenter(false)`. Если волки задрали весь скот, `ctr === null`,
а ветки `else` не было — пастух замирал у базы навсегда. То же самое в фазе `back`.

Решение — метод `herdMove(u, tx, ty, dt, arrive = 6)`:

* ходит через `moveTowardPath` (A*), а не по прямой;
* сторожит застревание: если за кадр смещение меньше `speed * dt * 0.25`,
  копится `herdStuckT`;
* дольше 1.5 с — сброс `path/pathGoal/noPathT` и шаг вбок на 90 единиц
  (сторона по чётности `u.id`, чтобы соседние пастухи расходились);
  пересчёт маршрута ограничен интервалом 0.75 с, иначе A* звался бы 30 раз в секунду;
* дольше 6 с — переключение фазы `graze` ↔ `home` и сброс маршрута. Телепорта нет:
  пастух просто выбирает новую цель вместо вечного упора в стену.

Плюс ветки `else` для пустого стада: в `graze` пастух едет на пастбище сам и,
подъехав ближе 260 единиц, заводит новое стадо (`stockPasture` + флоатер
«🐑 Новое стадо»); в `back` — просто едет к пастбищу.

Симуляция 30 с глухого упора: 4 смены фазы (на 6.0 / 12.1 / 18.1 / 24.1 с) —
цикл перезапускается, вечного залипания больше нет.

### 2. Казан и алтыбакан — постоянные объекты у ханской ставки

Раньше казан и качели существовали только как часть анимации отдыха: никто не
отдыхает — инвентарь исчезает. Теперь это стационарные декорации.

Спрайты **не рисовались заново**, а вырезаны из готовых кадров сценок отдыха
(`scripts/make-camp-props.cjs`):

* `kz_prop_kazan` (73×100) — из `kz_kazan_b`, женщина срезана по колонке x≥74,
  обрубок рукава добит фильтром «светлая ткань сверху справа»;
* `kz_prop_swing` (81×149) — из `kz_swing_c`. Разбор кадра по ASCII-карте показал
  структуру: перекладина на y≈16, две верёвки на x≈30 и x≈50, четыре диагональные
  стойки по краям, девушка с сиденьем в центре y≈80…135. Рама сохранена по
  «дерево у краёв + верёвки», фигура убрана.

Оба спрайта прогоняются через `despeckle` — иначе на верёвках висела пыль от платья.

Размещение — `campProps()`, шаг гекса в мире ≈32:

| объект | дистанция | требование |
|---|---|---|
| казан | 2.5 клетки (80 ед.) | 2-3 клетки |
| алтыбакан | 7.5 клетки (240 ед.) | 7-8 клеток |

Позиция зависит только от ставки, поэтому считается один раз и кешируется
(`campCache`), а не пересчитывается каждый кадр. Рисуются через общий `drawList`,
так что корректно перекрываются юртами и людьми. Размер — от роста шаруа
(`drawCampProp`): казан ×1.0, рама алтыбакана ×1.9, как в сценках отдыха.

Отдых привязан к реальным объектам: в `sendToRest` работник идёт именно к казану
или к качелям. Инвентарь не делится — второй желающий уходит к юрте, иначе две
сценки наложились бы в одной точке. Если у декорации кто-то уже отдыхает, сама
сценка рисует и инвентарь, поэтому декорация пропускается — иначе получилось бы
два казана.

### 3. Плашка волны убрана в мирное время

Набеги идут только во время войны (`if (this.atWar && !this.over)`), в мире
`waveT` просто заморожен — отсчёт до несуществующей атаки пугал игрока зря.
Счётчик «Волна N → Mс» и мобильный «🌊Mс» теперь под условием `hud?.atWar`.

### Проверки

`tests/camp-test.mjs` — 30/30: пастух ходит только через `herdMove`, сторож
застревания на месте, дистанции декораций в заданных диапазонах, оба вывода
волны скрыты в мире. Превью размещения — `tests/camp-props.png`.

## 1.0.075 — Пастух: настоящая причина зависания (A* врал физике)

В 1.0.074 пастух чинился «по исходнику»: регулярки подтверждали наличие кода,
но в игре он всё равно стоял. Здесь баг найден headless-прогоном НАСТОЯЩЕГО
движка (`tests/shepherd-sim.mjs`, `shepherd-wall.mjs`, `shepherd-gate.mjs`:
esbuild собирает `engine.ts` в ESM, браузерное окружение подменяется заглушками).

### Почему правки 1.0.074 не работали

В чистом мире пастух ходил всегда — поэтому баг и не ловился. Он воспроизводится
только когда **база обнесена дуалом**, а это обычное дело у игрока.

Три причины, найденные последовательно:

**1. Сторож застревания стоял не в том месте.** `herdMove` мерил смещение сразу
после `moveTowardPath`, а отталкивание от зданий происходит ПОЗЖЕ, в общем цикле
`update`. Диагностика показала абсурд: `сместился=3.93` каждый кадр при полностью
неизменных координатах. `moveToward` честно двигал юнита, коллизия со стеной тут
же возвращала назад, `herdStuckT` оставался нулём. Замер перенесён в `update`,
после разрешения коллизий.

**2. Покадровый порог не ловит «дёрг-стоп-дёрг».** Упёршийся в стену юнит всё
время мелко дрожит — в том числе от наших же попыток объехать, — счётчик
обнулялся и порога не достигал. Теперь меряется ПРОГРЕСС ЗА 2 СЕКУНДЫ
(`herdProbeT/X/Y`): за это время на скорости 175 юнит проходит ~350 единиц,
меньше 60 — заведомый затык.

**3. Главное: A* и физика считали стену по-разному.**

| подсистема | геометрия стены | эффективный радиус |
|---|---|---|
| `computePath` (A*) | круг | **15** |
| `moveTowardPath` → `lineBlocked` | круг | **15** |
| физическая коллизия в `update` | бокс `size/2` + отступ 13 | **28** |

Секции сплошного дуала стоят с шагом ~28.6 единиц. Для A* с радиусом 15 круги
почти не перекрывались — планировщик видел «щели» между секциями и уверенно
прокладывал маршрут СКВОЗЬ стену. Юнит шёл по этому маршруту и упирался телом:
для физики боксы перекрываются на 27 единиц, кольцо непроходимо. Радиус в A* и
в `lineBlocked` приведён к физическому: `b.size / 2 + 12`.

### Что теперь делает пастух, если выход всё же перекрыт

* **2 с** без прогресса — перепрокладка маршрута и шаг вбок;
* **4 с** — `routeThroughGate`: ищем СВОИ ворота, которые ближе к цели, чем сам
  пастух, и ведём его к точке за воротами (не в проём, иначе встанет в нём);
* **8 с** — ворот нет вовсе: пастбище переносится в достижимую зону, стадо
  перегоняется туда же (иначе скот остался бы за стеной и фаза «пригнать в
  загон» не завершилась бы никогда), баннер «🐑 Новое пастбище».

Плюс `findPasture(pen, u)` теперь проверяет достижимость и, если дальние кольца
1700…2700 недоступны, поджимается кольцами 1400 → 300. Единственный критерий —
маршрут от A*. Прежний fallback «прямая свободна» был ошибкой: он шагает по
отрезку редкими точками, проскакивает между секциями дуала и объявляет
достижимым место за стеной.

### Результаты прогонов (6 минут игрового времени)

| сценарий | отход пастуха | пастбище |
|---|---|---|
| открытая база | 1811 ед. (57 клеток) | 52 клетки, полный цикл выпаса |
| база в глухом кольце | 703 ед. (22 клетки) | поджато до 21 клетки, цикл идёт |
| кольцо с воротами | 2033 ед. (64 клетки) | 59 клеток, ходит через ворота |

Регресс экономики (`tests/regress-sim.mjs`): 4/4 рабочих двигаются, за 5 минут
добыто 4558 ресурсов — синхронизация радиусов стен обычных работников не сломала.

### Проверки пунктов 2-4 на живом движке

`tests/camp-sim.mjs` — 10/10: казан 2.5 клетки, алтыбакан 7.5, позиции не
меняются за 2 минуты, декорации не исчезают и не зависят от отдыхающих;
`hud.atWar` false в мире (плашка скрыта) и true в войну.

Отдельная грабля теста: `pushHud()` зовётся из `frame()` раз в 0.12 с, а
headless-прогон крутит `update()` напрямую — снимок нужно запрашивать явно.
И ИИ может сам заключить мир (`sueForPeace(auto)`) прямо во время прогона.

## 1.0.076 — Алтыбакан заново: пара поперёк доски; казан отодвинут; КЛЕТКА = ГЕКС

### Единица измерения: клетка — это ГЕКС

Раньше дистанции декораций считались от «32» — числа, взятого из `TILE_STEP`
(шаг привязки стен). Это неверно: клетка в терминах игрока — гекс, его шаг
`√3 · HS ≈ 34.64`. В `iso.ts` добавлен экспорт `HEX_CELL`, все дистанции
переведены на него. Старые 7.5×32 = 240 единиц были на самом деле 6.93 клетки,
а не 7.5.

| объект | было | стало |
|---|---|---|
| казан | 2.5 клетки (80 ед.) | **4.5 клетки** (156 ед.) — отодвинут на 2 |
| алтыбакан | 6.93 клетки (240 ед.) | **7.5 клетки** (260 ед.) |

### Алтыбакан перерисован

Прежний спрайт был вырезкой из старой сценки — рама разваливалась, каталась
одна девушка боком. Сделано заново по референсу:

* **Конструкция** — шесть шестов, обвитых красно-белой лентой, связанных в два
  треножника; сверху тяжёлая перекладина с верёвочными узлами; на двух арканах
  висит широкая доска.
* **Посадка пары** — двое стоят **поперёк доски**, лицом по ходу качания:
  парень в коричневом чапане спиной к камере, девушка в синем платье и саукеле
  лицом к камере, оба держатся за верёвки.

Кадры (`scripts/build-altybakan.cjs`):

| файл | что |
|---|---|
| `kz_prop_swing` | пустые качели — постоянная декорация у ставки |
| `kz_swing_ride_c` | пара в центре |
| `kz_swing_ride_l/r` | отклонение влево/вправо |

**Боковые фазы построены программно, а не генерацией.** Модель на просьбу
наклонить маятник возвращала тот же кадр (а правый — ещё и с артефактом).
Поэтому `splitPendulum` заливкой отделяет качающуюся часть (верёвки + доска +
пара, 38.9 % пикселей) от неподвижных опор, а `rotateAbout` поворачивает её
на ±13° вокруг точки подвеса на перекладине. Опоры на всех кадрах совпадают
пиксель в пиксель.

**Масштаб ×1.67, а не ×1.9.** Замерен по кадру: парень занимает 91 из 152 px
(59.9 %), значит для роста шаруа 46 px кадр должен быть 77 px. Тот же множитель
у пустой декорации — иначе при посадке пары рама «прыгала» бы в размере.

### Грабли

* **Заливка фона от краёв не годится** — между верёвками и под перекладиной
  остаётся ЗАМКНУТЫЙ карман магенты, куда заливка не доберётся. Гасим любой
  магентовый пиксель: в самом спрайте этого цвета нет.
* **Имя `process` для функции в CJS-скрипте** затеняет глобальный `process`
  Node — падает на `process.argv`. Переименовано в `makeSprite`.
* Уменьшение только усреднением (box), не nearest: тонкие верёвки рвутся.

### Проверки

`tests/camp-test.mjs` — 36/36 (добавлен раздел про кадры катания и `HEX_CELL`),
`tests/camp-sim.mjs` — 10/10 на живом движке: казан 4.5 клетки, алтыбакан 7.5.
Превью: `tests/altybakan.png` (4 кадра), `tests/camp-props.png` (размещение).

## 1.0.077 — Алтыбакан в ИЗОМЕТРИИ, доска вдоль оси раскачки

В 1.0.076 качели были нарисованы во фронтальной проекции — плоский вид спереди
в изометрической игре. Перерисовано в проекции 2:1, как здания в
`src/assets/sprites/kz` (эталон — `kz_pen.png`): перекладина уходит вглубь по
изо-оси, треножники имеют объём, ноги шестов образуют ромбовидный след.

**Вторая ошибка — ориентация доски.** Сначала доска легла вдоль перекладины,
но качели ходят ПЕРПЕНДИКУЛЯРНО ей. Правильно: длинная ось доски идёт по второй
изо-оси (от нижнего-левого края к верхнему-правому), то есть вдоль направления
раскачки. Пара стоит на концах доски лицом друг к другу вдоль той же оси:
парень в чапане на ближнем конце спиной к камере, девушка в саукеле на дальнем
лицом к камере.

### Анимация: композиция двух слоёв вместо заливки

Разделять цельный кадр заливкой (`splitPendulum`), как в 1.0.076, в изо-ракурсе
не работает: доска перекрывает шесты, маска перетекает через точку касания в
опоры — получалось 86.6 % «маятника», и на боковых фазах конструкция
разваливалась. Ограничение рамкой пролёта тоже дало кривой разрез (парень
раздваивался).

Решение — генерировать слои отдельно:

| файл | содержимое |
|---|---|
| `raw/kz_alt_frame_only.png` | только опоры и перекладина |
| `raw/kz_alt_board_riders.png` | только доска с парой и обрывки верёвок |
| `raw/kz_alt_iso3_empty.png` | пустые качели целиком |

`build-altybakan.cjs` обрезает все три по ОБЩЕЙ рамке (иначе слои разъедутся),
накладывает маятник на раму и поворачивает его на ±12° вокруг подвеса.
Соотношение маятник/рама — 35.5 %, опоры на всех кадрах совпадают.

### Грабля: «шахматка прозрачности» как настоящие пиксели

Модель при удалении объектов дорисовывает фон шахматкой — строгая сетка с шагом
12 px из белого (255,255,255) и серого (~175,174,172). Это попадало в спрайт
решёткой поверх качелей. Фильтр по порогу «светлее 178» ловил только белые
клетки; попытка «защитить блики» по числу соседей оставляла сетку тонких линий.
Рабочий вариант — бить по обоим тонам без оговорок плюс `despeckle` кластеров
меньше 12 px.

### Масштаб

Пересчитан по новому кадру: парень занимает 68 из 132 px (51.5 %), значит для
роста шаруа 46 px кадр = 89 px, множитель **×1.94** (был ×1.67 у фронтального
спрайта). Тот же множитель у пустой декорации. Размер спрайтов 135×132,
якорь `kz_swing_ride` обновлён.

### Проверки

`tests/camp-test.mjs` — 40/40, добавлен раздел «алтыбакан нарисован в
изометрии»: кадр шире, чем высок (135×132 против фронтальных 106×152), все
фазы одного размера, пустые качели совпадают с катанием.
Превью: `tests/altybakan.png`.

## 1.0.078 — Стрелки прокрутки, курсоры действий, плашка ресурса, очередь построек

### 1. Автопрокрутка по краю убрана, вместо неё — стрелки по клику

Камера уезжала сама, стоило курсору задеть край экрана (полоса 16 px), — мешало
целиться по юнитам у границы окна. Теперь автоскролла нет вовсе.

Вместо него: в полосе `EDGE_ZONE = 52` px появляется золотая стрелка-таблетка в
сторону прокрутки, и **камера едет только по клику** — на `EDGE_STEP = 620` px
с плавным докатом `EDGE_GLIDE_T = 0.28` с.

Тонкости:
- Клик по стрелке перехватывается в `pDown` **до** рамки выделения и приказов,
  иначе один клик у края двигал бы камеру и одновременно командовал юнитами.
- В режимах постройки / точки сбора / атаки стрелка клик не забирает.
- На тач-экране стрелок нет: там удобнее жест панорамы (`mouse.isTouch`).
- Стрелка прячется во время рамки выделения, панорамы и протяжки стены.

### 2. Свой курсор под каждое действие (gpt image)

7 курсоров 32×32 в `src/assets/cursors/`, арт нарисован gpt image, сборка —
`scripts/build-cursors.cjs`, превью — `tests/cursors.png`.

| курсор | когда |
|---|---|
| сабля | враждебный юнит или здание при выделении |
| топор | лес, если выделен шаруа |
| кирка с самородком | золотая жила |
| корзина с ягодами | ягодник и своя пашня |
| рыба | рыбное место |
| молоток с доской | режим постройки и недострой |
| зелёная стрелка | приказ идти |

Реализация — `src/game/cursors.ts` + `cursorFor()`/`syncCursor()` в движке.
Ставим CSS-свойством `canvas.style.cursor`, а НЕ рисуем на канвасе: системный
курсор не отстаёт от указателя на кадр и не мигает при просадках fps. Строка
стиля переписывается только при смене вида курсора — иначе Chrome моргает.

У каждого курсора своя горячая точка: у сабли остриё (2,2), у топора лезвие
(2,9), у кирки клюв (6,4), у стрелки её жало (16,30), у предметов-символов
центр. В `cursorCss()` после `url(...)` указан системный запасной `crosshair` —
если png не отдался, курсор не превратится в стрелку по умолчанию.

**Размер строго 32 px**: курсоры крупнее Chrome и Safari молча игнорируют.

### 3. Плашка ресурса по клику, полоска запаса скрыта

Раньше полоска остатка висела над каждым початым кустом и деревом — карта
рябила во время добычи. Теперь:
- полоска рисуется **только у выбранного** узла (`n.id === this.selNode`);
- клик по ресурсу **без выделения** = осмотр: над узлом всплывает плашка с
  названием («Лес», «Золотая жила», «Ягодник», «Рыбное место»), текущим
  остатком `amount / max` и мини-полоской;
- клик по ресурсу **с выделенным шаруа** по-прежнему отдаёт приказ добывать —
  осмотр не перехватывает управление;
- `clearSel()` снимает и выбор ресурса.

`drawNodePlate()` считает экранные координаты по матрице камеры
(`camIsoX()/camIsoY()`, зум) и рисует с учётом `dpr`, иначе на retina плашка
размывалась.

### 4. Очередь построек Shift+клик (пункт 5 плана)

Shift и раньше не сбрасывал режим постройки, но **очереди не было**: каждый
следующий фундамент отвлекал НОВОГО шаруа, и трое рабочих бросали работу ради
трёх домов.

Теперь при Shift фундамент уходит в личную очередь `buildQueue` того же шаруа,
который взялся за первый дом; над точкой всплывает «в очередь (N)».
`nextQueuedBuild()` разбирает её по FIFO, пропуская снесённые и уже достроенные.

**Дефект, найденный headless-прогоном:** очередь бралась только из ветки
`state === 'build'`. Стоило шаруа отвлечься (отнести ресурс, поесть, отдохнуть)
— он уходил в `return`/`idle`, и очередь зависала навсегда, а он шёл рубить лес.
Исправлено: проверка очереди стоит **первой** в ветке `idle`, впереди дойки,
пашни и поиска ресурсов.

### Проверки

- `tests/ui-test.mjs` — 40/40, статический разбор всех четырёх пунктов.
- `tests/ui-sim.mjs` — 29/29 на **настоящем движке**: камера не двигается при
  курсоре у края (0.00 px за 2 с), клик даёт 689 px и останавливается; курсор
  над лесом/золотом/ягодником/врагом; выбор ресурса; очередь из трёх домов
  одним шаруа.
- Прежние: camp-test 40/40, light-test 34/34, shift-test 75/75,
  prayer-test 57/57, rest-scale 20/20, camp-sim 10/10.

**Грабли теста (не повторять):** изо-проекция в headless — ровно
`toIso = [wx - wy, (wx + wy) * 0.5]`; с неверными множителями мышь промахивалась
мимо цели и все курсоры возвращали `default`. Координаты под застройку в тесте
нельзя брать «на глаз» — лес, горы и вода делают точку невалидной; надо
перебирать кольцо и спрашивать `placementValid()`.

## 1.0.079 — Курсоры 16×16, новая стрелка приказа

Курсоры 1.0.078 оказались громоздкими: 32 px перекрывали то, на что наводишься.
Уменьшены до **16×16** — вчетверо по площади, размер обычного системного
курсора. Системная «рука» (`pointer` на своих юнитах и зданиях) не тронута:
это не PNG, её размер задаёт ОС.

Стрелка приказа «идти» перерисована: изумрудное тело с **золотой каймой** в тон
интерфейсу, узкое древко, острое жало. Прежняя была плоским зелёным
треугольником с чёрным контуром.

### Что пришлось учесть при уменьшении

- **Обводка по 8 соседям на мелком кадре губит рисунок.** Контур в 1 px по
  диагоналям на 16 px утолщается вдвое, и иконка превращается в тёмное пятно.
  Оставлены только 4 стороны (вверх/вниз/влево/вправо).
- **Стрелке чёрная обводка не нужна вовсе** — у неё своя золотая кайма, а
  добавленный контур съедал форму. В `CURSORS` появился флаг `noOutline`.
- **Горячие точки пересчитаны автоматически** из `hotspots.json`, а не руками:
  при 16 px они вдвое меньше (сабля 1,1; топор 1,5; кирка 3,2; стрелка 8,14).
  В `tests/ui-test.mjs` добавлена проверка, что значения в `src/game/cursors.ts`
  совпадают со сборщиком и лежат внутри кадра — иначе курсор кликает не туда,
  куда показывает остриё.

### Грабля: перепутанные файлы генерации

Для стрелки было сгенерировано два варианта — `cur_move3` (стрелка с древком) и
`cur_move4` (двойной шеврон). В сборщик по ошибке попал `cur_move4`, и на превью
вместо стрелки оказался «щит». Диагностика — ASCII-профиль готового PNG по
строкам: у стрелки узкий верх (древко), резкое расширение на «плечах» и сужение
к жалу; у шеврона профиль симметрично-угловой. Проверять глазами увеличенный
кадр (`tests/cursors.png`), а не доверять имени файла.

### Проверки

`tests/ui-test.mjs` — 42/42 (добавлены сверка горячих точек со сборщиком и
их попадание в кадр). Остальные наборы без изменений: ui-sim 29/29,
camp-test 40/40, light-test 34/34, shift-test 75/75, prayer-test 57/57,
rest-scale 20/20, camp-sim 10/10. Превью: `tests/cursors.png`.

## 1.0.080 — Кликабельные гексы: обводка при наведении, приказ в центр клетки

Клетка = гекс, и теперь это видно в игре. Гекс под курсором обводится контуром,
клик по нему отправляет выделенный отряд именно в эту клетку.

### Подсветка

`updateHoverHex()` каждый тик считает гекс под мышью через `worldToHex()` —
ту же функцию, по которой садятся постройки, поэтому подсветка совпадает с
сеткой пиксель в пиксель. `drawHexHover()` рисует контур в мировой
iso-трансформации (не в экранной), иначе шестиугольник не лёг бы на плитку.

Цвет зависит от того, есть ли кому идти: с выделением — зелёный «можно идти»
(заливка 0.13 + контур 0.9), без выделения — нейтральный золотой.

**Подсветка гаснет там, где клик значит другое** — иначе она обещала бы
действие, которого не будет: в режиме постройки (там свой призрак здания),
над стрелками прокрутки края, над миникартой, во время рамки выделения,
панорамы и протяжки стены, а также на тач-экране.

### Приказ

`orderToHex()` привязывает цель к `hexCenterWorld(q, r)` и даёт вспышку
`hexPing` на клетке (0.7 с).

**Тонкость со строем.** `issueSmart` раскидывает цели на `rand(±24)`, а это
БОЛЬШЕ радиуса гекса (вписанная окружность ≈17): одиночный юнит по клику мог
встать в соседнюю клетку. После вызова цели переставляются: один юнит — ровно
в центр, группа — кольцами по 6 с шагом `HEX_CELL * 0.62`, чтобы отряд не
толпился в одной точке, но шёл именно на выбранный гекс.

Приказ по гексу срабатывает ТОЛЬКО на пустой земле: клик по юниту, зданию или
ресурсу разбирается раньше в `handleTap` и работает как прежде.

### Проверки

`tests/hex-sim.mjs` — 18/18 на настоящем движке: гекс под курсором совпадает
с геометрией `iso.ts`, мелкое смещение мыши клетку не меняет, подсветка гаснет
во всех перечисленных режимах, шаруа и сарбаз доходят до центра (3 px),
отряд из трёх юнитов идёт кольцом, клики по объектам не перехвачены.

**Грабля теста:** шаруа, дойдя до пустого гекса, штатно уходит на ближайший
ресурс — рабочий не простаивает. Мерить конечное расстояние бессмысленно
(получалось 285 px «не дошёл»), нужен МИНИМУМ за проход. Для проверки «пришёл
и остался» брать боевого юнита: его никакая работа с места не уведёт.

## 1.0.081 — Стрелка прокрутки следует за курсором; нижняя заработала

Две правки к 1.0.078.

### Стрелка едет за курсором вдоль края

Раньше она висела в середине стороны: подводишь мышь к краю в одном месте, а
кнопка появляется в другом — и до неё надо ещё доехать. Теперь стрелка
позиционируется по курсору: **поперёк края** прижата к нему (`EDGE_ZONE*0.55`),
**вдоль края** следует за мышью с полями `EDGE_ZONE*0.9`, чтобы в углах не
вылезать за экран.

### Почему не работала прокрутка вниз

Причина была не в движке, а в разметке: три контейнера HUD растянуты
`absolute inset-x-0` во всю ширину и **перехватывали события мыши** до канваса
— нижний док (`bottom-0`) и две плашки-баннера (`bottom-[132px]`). Курсор
физически не мог попасть в нижнюю полосу карты.

Добавлен `pointer-events-none` на сами контейнеры-обёртки. Панели внутри уже
помечены `pointer-events-auto`, поэтому кнопки дока, вкладки «Войска/Стройка»
и баннеры остаются кликабельными.

**Правило на будущее:** любой растянутый оверлей над канвасом обязан быть
прозрачным для мыши, иначе он молча съедает край карты. В `tests/ui-test.mjs`
добавлена проверка: все `absolute inset-x-0` контейнеры в `App.tsx` (кроме
верхней панели, она уже прозрачна) должны иметь `pointer-events-none`.

### Миникарта

Она сидит в правом нижнем углу — прямо в зоне стрелок «вниз» и «вправо».
Клик по ней перехватывается раньше, поэтому и стрелку там больше не рисуем:
иначе кнопка обещала бы прокрутку, а карта прыгала бы к точке на миникарте.

### Проверки

`tests/edge-sim.mjs` — 15/15 на живом движке: доступны все четыре стороны
(включая низ), стрелка ездит за курсором по X у нижнего края и по Y у боковых,
поперёк прижата к краю, в углах не вылезает, над миникартой не появляется,
клик по нижней стрелке двигает камеру на 689 px.
`tests/ui-test.mjs` — 44/44 (добавлен раздел «HUD не перехватывает мышь»).

## 1.0.082 — Мерген перестал быть ниже остальных (поправка на надголовный вынос)

Лучник выглядел подростком рядом с сарбазом. Замер подтвердил: рост фигуры на
экране 39.6 px против 45.7 у шаруа — **−13 %**, худшее отклонение среди пеших.

### Причина

`UNIT_TARGET_H` задаёт высоту КАДРА, а масштаб считался как `H / an.h` — по
всему кадру целиком. У большинства юнитов макушка у верхней кромки, и это одно
и то же. Но у мергена **лук поднят над головой** (21 px из 150), у найзагера —
наконечник пики (16 px). Кадр ужимался до 46 px вместе с луком, а сам человек
получался мельче.

Диагностика — профиль заполнения по строкам: у мергена верхние ~40 строк имеют
ширину 2-28 px (тонкий лук), у сарбаза с первых строк идёт голова шириной 30-40.

### Решение

В `pixelart.ts` добавлена таблица `HEAD_TOP` — доля кадра ВЫШЕ макушки, и
масштаб делится на долю человека:

```ts
const scale = (H / an.h) / (1 - headCut);
ctx.drawImage(drawIm, -an.ax * scale, -an.ay * scale, w, an.h * scale);
```

Высота отрисовки стала `an.h * scale` вместо `H`: кадр рисуется крупнее целевой
H, чтобы совпал рост САМОЙ ФИГУРЫ. Якорь `ay` задан от низа кадра и тоже
умножен на `scale`, поэтому ноги остаются на земле.

**Значения покадровые, а не один коэффициент на юнита:** у мергена вынос гуляет
0.067…0.140 между кадрами (бок / спина / анфас / шаг), и общий множитель
заставлял бы его прыгать в росте при ходьбе.

Макушка замерена так: первая сверху строка со сплошным горизонтальным отрезком
>= 12 px. Тонкие лук и пика такого отрезка не дают, поэтому в рост не попадают.
Замер устойчив — проверен на порогах 8/12/16/20.

### Попутно: найзагер

Его `UNIT_TARGET_H` был 50 вместо 46 — ручная компенсация за пику. После
введения `HEAD_TOP` получалась двойная поправка (+9 %), поэтому значение
возвращено к 46. Правка внесена **в оба места** — `sprite-art.ts` и
`scripts/build-sprites.cjs`, который этот файл генерирует; иначе регенерация
вернула бы старое значение.

Сама поправка `HEAD_TOP` живёт в `pixelart.ts` именно потому, что
`sprite-art.ts` перезаписывается генератором целиком.

### Результат

Разброс роста пеших юнитов сжался с **−13 %…+4 %** до **−6 %…+4 %**, мерген
теперь +1 %. Разброс между кадрами одного юнита: лучник 0.4 px, шаруа 0.3 px.

### Проверки

`tests/scale-test.mjs` — 13/13: поправка на месте, все пешие в пределах ±8 %
от шаруа, мерген в пределах ±4 %, рост не прыгает между кадрами (≤2.5 px),
`UNIT_TARGET_H` синхронен в генераторе и в `sprite-art.ts`.

## 1.0.083 — Сводка экономики в HUD (пункт 6) и экран итогов эпохи (пункт 10)

### Пункт 6: панель «ЭКОНОМИКА»

Слева под кнопками армии — куда распределены шаруа: полоска долей и строки
🪵 лес / 🍖 еда / 🪙 золото / 🔨 стройка / 💤 простой / 🌙 отдых. **Каждая строка
кликабельна** (`tradeSelect`) и выделяет своих работников.

Две тонкости, без которых цифры врали бы:

- **Считаем по НАМЕРЕНИЮ, а не по кадру.** Несущий груз домой (`state === 'return'`)
  по-прежнему числится за своим промыслом, иначе счётчики прыгали бы каждую
  секунду при каждой ходке.
- **Процент простоя — от РАБОТОСПОСОБНЫХ**, то есть без отдыхающих в ночную
  смену. Иначе ночью показывало бы «простой 80 %», хотя это законный отдых.
  Проверено тестом: когда все ушли отдыхать, простой 0 %, а не 100 %.

Вид промысла определяется одинаково в подсчёте и в `tradeSelect` — иначе цифра
в панели и выделение по клику разошлись бы. Рыба нормализуется в «еду».

### Пункт 10: экран итогов эпохи

Модалка `AgeReport` при переходе: добыча по видам за эпоху, убито/снесено/
построено, пик армии, полоса расклада сил с джунгарами с вердиктом («можно
наступать» / «силы равны» / «крепите оборону») и список открывшегося.

**Все цифры — разница с меткой `ageMark` на входе в эпоху**, а не итог с начала
партии. Проверено тестом: вторая эпоха показывает 200/150/100, а не
накопленные 700/550/400.

Добавлен подробный учёт добычи по видам (`gotWood/gotFood/gotGold`) — раньше
копился только `woodGathered` и общий `gatheredTotal`. Все новые счётчики и
`ageMark` **попадают в сохранение**: без этого после загрузки сводка считалась
бы «с нуля» и следующий отчёт вышел бы завышенным.

### Проверки

`tests/econ-sim.mjs` 13/13 — сумма частей равна целому, несущий груз не выпадает
из сводки, процент простоя не врёт при ночном отдыхе, клик выделяет ровно
столько, сколько показано.
`tests/age-sim.mjs` 17/17 — отчёт открывается и закрывается, вторая эпоха
считает только свою добычу, `gotWood` копится в живой игре.

## 1.0.084 — Победа «Объединение степи» (пункт 9) и великие люди (пункт 11)

Закрыты последние два пункта бэклога — план выработан полностью.

### Пункт 9: дипломатическая победа

Стать сюзереном **5 из 7** кочевых народов и удержать союз **3 минуты**.
Механика сюзеренитета уже существовала (посланники против джунгарских),
добавлены условие победы и индикатор «🤝 Степь N/5» в HUD.

Ключевое решение — **распад союза откатывает прогресс, а не обнуляет его**:
`uniteT` убывает вдвое медленнее, чем набирался. Иначе одна перехваченная
джунгарами делегация стирала бы десять минут дипломатии, и путь стал бы
неиграбельным. Враждебное племя не засчитывается, даже если посланников там
больше: союзом это не назвать.

### Пункт 11: великие люди

Мягкий ресурс **мудрость** капает от Мешіт-медресе (1.2/с), Мавзолея (2.5/с) и
реликвий (0.8/с каждая). Он не тратится на войска и стройку, поэтому
«культурная» линия идёт параллельно экономике, а не вместо неё.

Совет по клавише **J** (рус. О) — три бия, авторы «Жеті Жарғы», по одному
призыву на каждого:

| Бий | Цена | Эффект |
|---|---|---|
| Толе би | 220 | −30 неприязни, враждебные племена → нейтральные |
| Казыбек би | 260 | +300 золота разово, торговый доход ×2 |
| Айтеке би | 300 | Исследования −20 % времени, постройки −10 %, мудрость +15 % |

Портреты сгенерированы и приведены к 256×256 скриптом `scripts/fit-portraits.cjs`.
**Кадрируем со смещением вверх (0.18 высоты), а не по центру** — в портретных
кадрах лицо в верхней трети, центральный квадрат срезал бы голову.

Мудрость и состав совета попадают в сохранение.

### Проверки

`tests/unite-sim.mjs` 16/16 — отсчёт только при собранном союзе, откат вместо
обнуления, враждебные не в счёт, удержание приводит к победе.
`tests/greats-sim.mjs` 18/18 — мудрость копится от построек и реликвий, призыв
требует ресурса и возможен один раз, все три эффекта проверены по фактическим
величинам (неприязнь, золото, цена дома).

**Грабли тестов:** `tribeRel` и `envoys` пусты до первой встречи с племенем —
список племён берётся из `nations.ts`, причём `id` и `kind` лежат на РАЗНЫХ
строках, поэтому однострочная регулярка не работает (нужен построчный разбор).
`onGameOver` вызывается через `setTimeout(900)` — прокрутка игровых кадров его
не приблизит, нужен реальный `await`.

## 1.0.085 — Азан по РЕАЛЬНОМУ времени намаза

Новый режим: азан звучит в тот же миг, что и в настоящей мечети выбранного
города. Настройка **«Азан по реальному времени»** + выбор города, по умолчанию
ВЫКЛ — старое поведение (по игровым суткам) остаётся основным.

### Расчёт — локальный, без сети

`src/game/prayer-times.ts` считает времена астрономически: склонение Солнца,
уравнение времени, часовой угол для угла погружения. Метод углов —
**ДУМК Казахстана** (фаджр 18°, иша 17°, аср по ханафитской тени), поэтому
времена совпадают с городским азаном. Сеть не нужна — важно, из песочницы она
всё равно недоступна.

Проверено на 9 сентября 2026, Астана: таң 03:40, бесін 12:12, ақшам 18:44 —
сходится с календарём ДУМК.

8 городов: Астана, Алматы, Шымкент, Ақтөбе, Атырау, Қарағанды, Түркістан,
Өскемен. Часовой пояс задан числом (+5), а не строкой IANA: tzdata в игре нет,
а весь Казахстан с 2024 года в едином поясе.

### Две ловушки высоких широт

**1. В июне на 51°N фаджр и иша не наступают вовсе** — Солнце не опускается на
18°, формула даёт NaN. Без поправки намаз просто пропал бы из игры на месяц.

**2. Проверки на NaN мало.** Сначала применил правило «седьмой части ночи»
(1/7) — NaN исчез, но вылезло другое: иша 21 июня 21:37, а 15 июля **23:53**,
то есть время скакало НАЗАД по календарю. Причина — разрыв между обычным
расчётом и правилом в переходные недели.

Итоговое решение — **угловое ограничение** (AngleBased): доля ночи,
пропорциональная углу, `portion = angle/60 · ночь`. Правило 1/7 тоже убирает
скачок, но оно жёстче и срезало ВЕРНЫЙ расчёт в обычные дни: таң 9 сентября
уезжал с 03:40 на 04:06. Угловое в такие дни не срабатывает вообще.

### Гибридное поведение (выбор пользователя)

Игровые сутки НЕ трогаем: освещение, смены и отдых живут своим 30-минутным
циклом — иначе развалилась бы посменная механика.

- **Окно 60 минут**: если партия начата через полчаса после таң, азан всё равно
  прозвучит («догоняющий»). Иначе за 40-минутную сессию реальный намаз почти
  никогда не попадал бы в игру — их всего 5 на 24 часа.
- **Запасной по игровым суткам**: если реальный намаз за игровые сутки не
  выпал, звучит прежний по фазе. Геймплей (берекет) не теряется.
- Прозвучавший реальный азан помечает игровые фазы пройденными — за одни
  игровые сутки не прозвучат оба.
- Проверка раз в секунду, а не каждый кадр: `new Date()` и тригонометрия 60 раз
  в секунду — пустая трата.

В HUD — плашка ближайшего намаза с полным расписанием в подсказке.

### Проверки

`tests/azan-real.mjs` 23/23: времена сходятся с ДУМК, весь июнь считается без
NaN, құптан меняется плавно (проверены 60 дней подряд), на западе закат позже,
догоняющее окно работает, режим по умолчанию выключен, смена города
подхватывается.

## 1.0.086 — Расписание намаза по ДУМК, перемирие азана, пространственный звук

### 1. Расписание исправлено: таң 04:03, а не 03:40

В 1.0.085 были взяты углы Muslim World League (18°/17°) — для Казахстана они
НЕ подходят и давали таң на 23 минуты раньше официального.

Параметры выверены по таблице ДУМК (muftyat.kz / sunna.kz) перебором с
минимизацией отклонения:

| параметр | было | стало |
|---|---|---|
| фаджр | 18° | **15°** |
| иша | 17° | **14°** |
| аср | тень 1 | **тень 2** (ханафитский) |
| зухр | истинный полдень | **+5 мин** |
| высота | 0 м | **350 м** (Сарыарка) |

**Отклонение от ДУМК ≤3 минут** на 7 датах двух сезонов (сентябрь + февраль —
контроль, чтобы параметры не оказались подгонкой под один месяц).

Две ошибки, найденные при сверке:
- **Сдвиг зухра нельзя применять к истинному полудню** — от него отсчитываются
  восход и закат, и всё расписание уезжало на +5 мин. Введена отдельная
  переменная `noon`, сдвиг применяется только к самому бесін.
- **Аср и иша ДУМК считает от ОБЪЯВЛЕННОГО бесін**, а не от истинного полудня:
  без этого аср стабильно отставал на 4-6 минут во все сезоны.

### 2. Перемирие азана: враг опускает оружие

Логика перевёрнута. Было: «враг у ворот — намаз отменяется, оборона важнее».
Стало: **азан звучит всегда, а нападающие приостанавливают атаку**.

- Вражеские ВОИНЫ на время намаза сбрасывают цель, замирают и не бьют.
  Крестьяне врага продолжают работать: перемирие о бое, не о труде.
- Таймер набега `waveT` замирает, но НЕ сбрасывается — волна придёт сразу
  после намаза. Это отсрочка, а не отмена, иначе мешіт превратилась бы в
  постоянный щит от волн.
- `u.cd` держится на 0.4 с, чтобы враг не ударил в тот же кадр, когда призыв
  смолк.
- Если враг рядом, игрок видит баннер «🤍 Враг опустил оружие» — иначе
  перемирие выглядело бы как зависший ИИ.

Условие остаётся прежним: **азан звучит, только если построена мешіт-медресе**,
значит и перемирие работает лишь у того, кто её возвёл.

### 3. Пространственный звук

Камера — «уши» игрока (`sound.setListener(cam.x, cam.y, cam.zoom)` каждый кадр).

- Ближняя зона 260 мировых единиц — без ослабления, дальше 1750 — тишина.
- Спад **линейный с квадратом кривой**, а не 1/d²: обратный квадрат на игровых
  масштабах глушит звук сразу за краем экрана и звучит неестественно.
- **При отдалении камеры слышно дальше** (радиусы делятся на zoom): игрок
  «поднялся выше», видит больше — логично слышать всю видимую область.
- Синтезируемые звуки (`tone`/`noise`) получили ещё и **панораму**
  через `StereoPanner` — звук идёт с той стороны, где событие.
- Записи (`<audio>`: голоса юнитов, батальный шум) панорамы не поддерживают, но
  громкость по расстоянию работает. Совсем далёкие реплики не проигрываются
  вовсе — они бы съедали лимит из 3 одновременных клипов, заглушая близкие.
- **Азан затухает мягче прочих**: пол 0.35 — минарет на то и высокий, чтобы
  призыв слышало всё становище.

Позиционными стали: sword, arrow, hit, chop, mine, battleClash, azan, голоса.

### Проверки

`tests/truce-sim.mjs` 25/25 — без мешіті азана нет; враг у ворот не отменяет
намаз; вражеский воин замирает и не наносит урона ставке; таймер волны
замирает и возобновляется после намаза; громкость убывает с расстоянием;
слушатель следует за камерой; азан слышен дальше прочих звуков.
`tests/azan-real.mjs` 25/25 — сверка с ДУМК на 7 датах.

**Грабля:** `body(src, sig)` в `prayer-test.mjs` ищет первую `{` после
сигнатуры. У `azan(at?: { x: number; y: number })` это скобка ТИПА, а не тело
метода — тест ломался на верном коде. Искать от `'): boolean'`.

## 1.0.087 — Автосохранение: партия переживает перезагрузку страницы

### Почему НЕ cookies

Замер `serialize()` на живом движке: партия весит **96 КБ в начале и 114 КБ в
разгаре** (юниты, здания, 629 узлов ресурсов, туман, дипломатия). Лимит одной
cookie — **4 КБ**, то есть не влезает в 25 раз.

Даже если бы влезало, cookies для этого плохи: они уходят на сервер с КАЖДЫМ
запросом, раздувая трафик. `localStorage` даёт 5-10 МБ и остаётся в браузере —
это и есть правильное место для сохранений. Оно уже использовалось, не хватало
только автоматики.

### Что добавлено

Сохранение в `localStorage` существовало, но лишь по кнопке «Сохранить партию».
Теперь:

- **Автосейв раз в 20 с игрового времени** (`AUTOSAVE_SEC`). Не каждый кадр:
  сериализация ~100 КБ заметно дороже обычного тика, на слабой машине это дало
  бы рывок.
- **Запись при уходе со страницы** — `pagehide` + `beforeunload` +
  `visibilitychange`. Именно `visibilitychange` критичен на мобильных: там
  `beforeunload` часто не срабатывает вовсе.
- **Возврат в бой после F5.** Метка `empires-dawn-ingame-v1` ставится при входе
  в партию; если страница закрыта во время боя, App сразу открывает игру, а не
  меню. При выходе в меню метка снимается (сохранение остаётся — работает
  кнопка «Продолжить»).
- **Проигранная партия не восстанавливается**: `finish()` зовёт `clearSave()`,
  который стирает и сохранение, и метку. Иначе после F5 игрок попадал бы прямо
  на экран поражения.
- Переключатель «Автосохранение» в настройках, ВКЛ по умолчанию. Ручное
  сохранение кнопкой работает независимо от него.

### Проверки

`tests/save-sim.mjs` 28/28 — размер против лимита cookie, автосейв по времени,
запись при уходе, точное восстановление (ресурсы, юниты, здания, время, очки,
постройки), возврат после F5, стирание проигранной партии, отключение настройки.

**Грабля:** заглушка `g.localStorage` в `camp-sim.mjs` всегда возвращает `null`
и глотает запись — с ней сохранение не проверить в принципе (15 ложных
падений). Нужно настоящее хранилище на `Map`.

**Вторая:** эталон для сравнения надо снимать РОВНО в момент записи. Автосейв
срабатывает на 20-й секунде, а прогон шёл до 25-й — за эти 5 секунд набегали
ресурсы и очки, и «расхождение» выглядело как баг сериализации.

**Замечено:** `hex-sim` дал одно падение в общем цикле прогона, но 25 запусков
подряд отдельно — все зелёные. Тест плавающий под нагрузкой, к сохранению
отношения не имеет.

## 1.0.088 — Сутки 15 минут и мобильная адаптация

### 1. Сутки сокращены вдвое, пропорции сохранены

`DAY_LEN_SEC` 1800 → **900 с**. Ночь и сумерки заданы АБСОЛЮТНЫМИ секундами,
поэтому их пришлось делить руками: `NIGHT_LEN_SEC` и `TWILIGHT_SEC` 240 → **120**.

| | было (30 мин) | стало (15 мин) | доля суток |
|---|---|---|---|
| ночь | 240 с | 120 с | 13.3% |
| сумерки (каждые) | 240 с | 120 с | 13.3% |
| светлый день | 18 мин | 9 мин | 60% |

Всё остальное задано **долями** `DAY_LEN_SEC` и пересчиталось само: благодать
360→180 с, отдых 194→97 с, бодрость 526→263 с, полная усталость 1125→563 с.

**Найденная проблема.** `PRAYER_LEN` = `DAY_LEN_SEC * 0.045` дал бы **40.5 с**,
а запись азана длится **47 с** — молящиеся расходились бы под ещё звучащий
призыв. Формула заменена на `Math.max(DAY_LEN_SEC * 0.045, 47 + 14)` = 61 с.
Так она переживёт любую длину суток, а не только текущую.

### 2. Мобильная адаптация

Базовое уже было (viewport, pinch-zoom, `touch-action: none`, отдалённая
камера, тач-подсказки). Добавлено:

**Управление пальцем**
- Порог «указатель сдвинулся» для тача **16 px против 9 у мыши**: палец при
  тапе всегда чуть ползёт, и с мышиным порогом обычное касание превращалось
  в рамку выделения вместо приказа.
- Зона попадания при касании шире: по юниту **42** вместо 30, по ресурсу
  **46** вместо 34 — палец накрывает больше площади, чем остриё курсора.

**Интерфейс**
- Панель заданий на телефоне **свёрнута по умолчанию**: 172 px поверх экрана
  360 px закрывали треть карты.
- Кнопки `IconBtn` — минимум **44×44 px** (гайдлайн Apple/Google), `MiniBtn` —
  38 px по высоте. На десктопе через `sm:` возвращается прежний размер.

**CSS**
- `touch-action: manipulation` на кнопках — двойной тап в Safari больше не
  зумит страницу.
- `env(safe-area-inset-*)` — HUD не уезжает под «чёлку» и полосу жестов.
- Правило для **альбомной ориентации** (высота ≤460 px): панели ужимаются,
  иначе HUD съедал почти весь экран.

### Проверки

`tests/mobile-sim.mjs` 34/34 — доли ночи/сумерек/дня совпадают с прежними,
механики отмасштабировались, намаз длиннее записи азана, живой движок
проходит полный цикл за 15 минут, все тач-настройки на месте.

**Грабля:** фаза суток циклическая — 0.999 и 0.001 соседние. Проверять
замыкание цикла надо кратчайшим расстоянием по кругу, иначе успешно
замкнувшиеся сутки читаются как максимальное расхождение.

## 1.0.089 — Редизайн интерфейса: казахский орнамент как система

Сквозная переработка оформления всех экранов с усилением национального
колорита. Ключевое решение — **править CSS-классы, а не разметку**: `panel-iron`
используется 20 раз, `btn-iron` 8, `btn-gold` 6, поэтому переработка ядра
применилась ко всем экранам разом, без 34 точечных правок.

### Орнаментальное ядро

Три переиспользуемых класса в `src/index.css`:

- `.kz-border-top/bottom` — лента **кошкар-мүйіз** («бараньи рога», базовый
  мотив казахского орнамента) по кромке панели;
- `.kz-corners` — уголковые завитки для крупных модалок;
- `.kz-divider` — разделитель с золотыми нитями.

Узоры зашиты **инлайновым SVG в data-URI**, а не картинками: нет отдельных
запросов, нет мигания при старте, цвет меняется правкой одной строки.
Псевдоэлементы помечены `pointer-events: none` — иначе орнамент перехватывал бы
касания по канвасу (та же ловушка, что была с оверлеями HUD в 1.0.081).

### Фактуры вместо плоских заливок

| элемент | было | стало |
|---|---|---|
| `.panel-iron` | плоский градиент | войлок кошмы: тканый шум + золотое свечение внутри + золотая нить по кромке |
| `.btn-gold` | градиент | тиснение + пробегающий блик `gold-sheen` (4.5 с, не отвлекает в бою) |
| `.btn-iron` | градиент | фактура тиснёной кожи + отклик на наведение |

Тканая текстура сделана `repeating-linear-gradient` под углом 58°: на глаз
читается как войлок, но весит десяток байт против растровой картинки.

### Экраны

- **Меню**: фон-клетка заменён орнаментальной сеткой кошкар-мүйіз (тот же ритм
  44→88 px, но национальный мотив), под баннером — орнаментальная лента,
  подзаголовок — в рамке `kz-divider`.
- **Модалки** (8 шт.): уголковые завитки. Карточке выделения их НЕ дал — она
  узкая, уголки съели бы место под содержимое.
- **HUD**: панель ресурсов получила орнаментальную кромку.

### Проверки

`tests/ui-design.mjs` 24/24 — ядро на месте, узоры инлайновые без внешних
файлов, фактуры не потеряны, орнамент применён к экранам, офисная клетка
убрана, мобильные настройки (safe-area, альбомная ориентация, touch-action)
не пострадали.

**Про «скиллы»:** пользователь просил запустить навык `impeccable`. Такой
системы у агента нет — ни в проекте, ни в окружении; проверено поиском.
Редизайн сделан обычной работой с кодом.

## 1.0.090 — Орнаменты переделаны на ПОДЛИННЫЕ казахские мотивы

Пользователь указал: «это не казахские орнаменты». Он прав — в 1.0.089 я
нарисовал абстрактные завитки на глаз, не сверяясь с образцами.

### Что сделал иначе

1. **Посмотрел настоящие образцы** через `image_search` (ою-өрнек, қошқар
   мүйіз, сынықмүйіз). Ключевые признаки подлинного орнамента: заливные формы,
   зеркальная симметрия по обеим осям, парные встречные спирали.
2. **Построил геометрию формулой**, а не от руки — `scripts/make-ornaments.cjs`.
   Функция `horn()` задаёт спираль Архимеда с переменной толщиной полосы,
   `ramHorns()` собирает пару встречных рогов.
3. **Проверил растеризацией.** Написал временный скрипт, разбирающий `d`
   SVG-путей (M/L/H/V/C/Z) и заливающий полигоны scanline — так орнамент видно
   глазами, а не «на веру». Первая попытка с ручными кривыми Безье дала
   бесформенные кляксы, и это стало видно только на растре.

### Итоговые мотивы

| мотив | где | что это |
|---|---|---|
| **сынықмүйіз** (ломаный рог) | бордюры панелей | прямоугольный меандр зеркальными парами, ковровый мотив басқұр |
| **қошқар мүйіз** (бараньи рога) | углы модалок, фон меню | парные встречные спирали — главный элемент казахского орнамента |
| **төрт құлақ** (ромб) | разделители | четырёхлепестковая розетка |

### Проверки

`tests/ui-design.mjs` 30/30 — добавлен раздел «орнаменты подлинные»: мотивы
названы, спираль строится `Math.cos/sin` в генераторе, а не рисуется вручную,
длина углового пути >900 символов (у «клякс» была короткой).

**Урок:** декоративную графику нельзя принимать «на веру» по коду SVG —
обязательно растеризовать и посмотреть. Скрипт растеризации без библиотек
(разбор path + scanline-заливка) занимает ~60 строк и окупается сразу.

## 1.0.091 — Қошқар мүйіз: связанная лента вместо двух обрубков

Пользователь прислал скриншот: два разорванных полумесяца и отдельно лежащий
ромб. Справедливо — это не орнамент.

### Что было неверно

В 1.0.090 рог строился как заливка сектора спирали Архимеда с ПЕРЕМЕННОЙ
толщиной. На выходе — два оборванных полумесяца, ничем не связанных.

На подлинных образцах қошқар мүйіз устроен иначе: это **непрерывная лента
почти постоянной ширины**, которая выходит из ОБЩЕГО основания пары, ведёт
вверх-в сторону и туго закручивается в «глазок». Между рогами — острый листик
(жапырақ). Фигура связная, а не набор фрагментов.

### Как построено теперь

`scripts/make-ornaments.cjs`:
- `hornAxis()` — осевая линия: прямой стебель от основания + спираль внутрь
  (радиус тает на 80%, ~50 точек — спираль гладкая);
- `ribbon()` — обводка ленты вокруг осевой по нормали, ширина постоянная и
  сужается только на последней четверти (кончик);
- `ramHorns()` — два зеркальных рога из одной точки `bx,by`;
- `leaf()` — листик между ними.

Разница принципиальная: раньше заливался сектор, теперь обводится линия.

### Проверки

`tests/ui-design.mjs` 34/34. Добавлены проверки именно на признак подлинности:
лента строится через `ribbon(hornAxis(...))`, оба рога выходят из общего
основания, есть листик, контур плотный (198 узлов — спираль без углов).

**Повтор урока:** декоративную графику проверять только растеризацией. Обе
неудачные версии выглядели правдоподобно в коде SVG и разоблачались лишь на
картинке.

## 1.0.092 — «Устав кочевника» и «Зал легенд» убраны в попап

Две панели занимали весь второй экран меню и оттесняли кнопку «В ПОХОД!» —
чтобы начать игру, приходилось прокручивать страницу. Обе вынесены в модалку
с вкладками, в меню остались две компактные кнопки.

### Решения

- **Одна модалка на два раздела, а не две.** Вкладки переключаются без
  закрытия окна: игрок сравнивает устав и рекорды, не бегая по меню.
- **Enter не стартует игру, пока открыт попап.** Иначе читающий устав случайно
  улетал бы в бой — пробел и Enter висят на «В ПОХОД!».
- **Esc и клик по фону закрывают.** Обычное поведение модалки.
- На кнопке «Зал легенд» — счётчик рекордов, чтобы не открывать пустое окно.
- Содержимое в `overflow-y-auto overscroll-contain`: длинный устав
  прокручивается внутри окна, не растягивая его и не прокручивая страницу
  под ним.
- Оформление общее: `kz-corners panel-iron anim-banner`, как у прочих модалок.

### Проверки

`tests/ui-design.mjs` 45/45 — добавлен раздел: обе кнопки на месте, попап
закрыт по умолчанию, все 6 пунктов устава и таблица рекордов ВНУТРИ модалки,
из меню они убраны (проверяется отсутствие `HowRow` до модалки), Esc и клик по
фону закрывают, Enter заблокирован при открытом окне.

## 1.0.101 — Хоткеи: `Home` к ставке, звук на `N` (мёртвая ветка `M`)

Два хвоста из раздела багов `plan.md`.

### Что было неверно

- В `onKeyDown` буква `m` встречалась дважды: первая ветка ставила мечеть,
  вторая — `toggleMute()` — не достигалась никогда. Кнопка паузы обещала
  «(M)», а клавиша молча открывала фундамент мечети.
- «Камера домой» не существовала: `centerTC()` вызывался только кнопкой «Центр».

### Решения

- Звук — `N` (рус. `Т`), мечеть остаётся на `M`: перевешивать здание дороже,
  оно фигурирует в уставе и на кнопке дока.
- `Home` — `centerTC()`; клавиша не зависит от раскладки, `preventDefault`
  гасит прокрутку страницы.
- Подсказки в паузе, уставе и README обновлены.

### Проверки

`tests/hotkeys-test.mjs` — разбор веток `k === '…'` в `onKeyDown`: ни одна
буква не встречается дважды, `home` и `n` присутствуют, в HUD нет «(M)» у
звука. Тест ловит именно тот класс ошибок, который прожил с 1.0.062.

## 1.0.102 — Погода степи: дождь, туман, буран

Пункт «погодные эффекты» из старого бэклога. В отличие от событий (пункт 8),
погода **не останавливает игру** и не требует выбора — это обстоятельство,
под которое подстраиваешься: в туман — рейд, в дождь — стройка и пашни.

### Виды и эффекты

| погода | скорость | дальность | обзор | прочее |
|---|---|---|---|---|
| 🌧️ Жаңбыр | −15% | −15% | −15% | пашни +20% |
| 🌫️ Тұман | — | −25% | −45% | — |
| 🌨️ Боран | −30% | −30% | −40% | усталость шаруа ×1.5 |

Ненастье держится 70–130 с (буран 45–80 с), после него ВСЕГДА просвет
120–240 с — два ненастья подряд невозможны, иначе партия превращалась бы
в вязкую кашу. Буран чаще ночью (18% против 8%).

### Решения

- **Честно для обеих сторон.** Множители не смотрят на `owner`: джунгары
  в тумане тоже слепнут. Иначе погода была бы скрытым уровнем сложности.
- **Ближний бой не штрафуется** (`u.range > 40`): дождь мешает стреле,
  а не сабле. Это даёт пехоте окно против стрелков.
- **Согласовано с событиями:** при тикающей `droughtT` дождь не выпадает,
  а если событие «Засуха» пришло под дождём — дождь прекращается; джут
  (`case 'jut'`) принудительно ставит буран на 70 с.
- **Экранный слой, а не мировые частицы.** Капли и снег живут в
  `weatherFx` в координатах экрана с потолком 220 — лимит игровых частиц 650
  не тронут, зум камеры на плотность осадков не влияет. Туман — два слоя
  радиальных градиентов, ползущих навстречу друг другу, без текстур.
- Рисуется после ночного освещения и до HUD (`drawWeather` в `draw`).
- Сейв: `events.weather/weatherT`; старые сейвы грузятся как «ясно».
- Настройка «Погода степи» (вкл. по умолчанию); выключение снимает текущее
  ненастье сразу, а не по таймеру.

### Проверки

`tests/weather-test.mjs` 27/27: виды, все шесть точек подключения к
механикам (ход, дальность юнитов, дальность башен, обзор, пашни, усталость),
честность (нет `owner === 'player'` в множителях), совместимость с засухой и
джутом, сейв/HUD/настройка, потолок частиц. `save-sim`, `ui-design` — зелёные.

Не проверено скриншотом: в песочнице нет браузера. Слой построен на тех же
примитивах, что и `drawNightLight` (setTransform по dpr + заливка/градиент).

## 1.0.103 — Иконки: почему плыл масштаб и как починено

Пользователь заметил «баги с масштабом иконок» после перевода UI на
собственный векторный набор (1.0.093). Три независимые причины, все системные:

### 1. Иконка в 1em внутри 9-пиксельного текста
83 из 113 `<Ico>` стояли без className → `h-[1em]`. Тринадцать из них — внутри
плашек с `text-[9px]`…`[11px]` (война/мир, отдых, намаз, дни). Контур 24×24
ужимался до 9 px, обводка 1.7/24 превращалась в 0.64 px — браузер размывал её
на два субпикселя, и иконка выглядела то жирной кляксой, то серым пятном.
**Фикс:** размер по умолчанию `max(1em, 12px)` (и в `RT` — `max(1.05em, 12px)`).
Явный `h-N w-N` уважается как раньше.

### 2. Preflight Tailwind: `svg { display: block }`
`inline-block` жил только в классе по умолчанию. Любой явный `className="h-4 w-4"`
его затирал → svg становился блочным, выпадал из строки и «прыгал» по вертикали
относительно соседнего текста; во flex-контейнерах без `shrink-0` мог сжиматься.
**Фикс:** `inline-block shrink-0 align-[-0.14em]` теперь добавляется ВСЕГДА,
className дописывается к ним, а не заменяет.

### 3. Canvas: обводка в единицах viewBox
`drawIcon` делает `ctx.scale(k)` и ставит `lineWidth = 1.7` в масштабе viewBox:
на 10-px иконках (плашка ресурса, полумесяц у молящихся) это 0.7 px — тонет,
на 22-px звезде Мавзолея — 1.6 px, заметно жирнее DOM-соседей.
**Фикс:** `lineWidth = max(w, 1.1 / k)` — на экране не тоньше 1.1 px.

### Попутно
Ресурсы в шапке HUD были lucide (`TreePine/Drumstick/Coins`) — другой стиль
линии и другая оптическая масса, чем у `{i:wood}` в стоимостях под ними.
Заменены на тот же набор; lucide-импорты убраны.

### Проверки
`tests/icons-scale.mjs` 8/8. Не проверено скриншотом (нет браузера в песочнице);
причины 1–3 выведены из кода и preflight.css, каждая воспроизводится расчётом
толщины обводки в px.

## 1.0.104 — Лояльность границ (п.23): перетягивание каната вместо статики

В оседлом режиме граница была печатью построек: поставил юрту — гекс твой
навсегда. Теперь у каждого гекса территории есть лояльность −1..+1.

### Решения

- **Давление — от построек и боевых отрядов, не от шаруа.** Иначе толпа
  рабочих на лесоповале «завоёвывала» бы землю. Якоря: ставка 3, башня и
  мечеть 2, чудо 2.5, юрта 0.8, прочее 0.5, плюс 0.15 за каждого в гарнизоне.
- **Дрейф = разница давлений ×0.03/с, кап ±0.06.** Первый вариант делил на
  сумму давлений — тогда перевес 3 против 2 давал всего 0.012/с, и почти
  потерянный гекс возвращался 3 минуты. Разница без нормировки честнее: строишь
  вторую башню — видишь эффект.
- **Без чужого давления гекс сам возвращается** (0.04/с). Разовый рейд не
  оставляет шрамов, оставляет только удержанный форпост.
- **Гекс меняет владельца при |l| = 1** и остаётся у нового, пока штамп старого
  не перетянет обратно. Спорные (|l|<0.5) в победу не идут — 60 % долины надо
  именно удержать.
- **Симметрично:** джунгары теряют гексы у наших башен так же.
- Пересчёт раз в 1.5 с: ~500 гексов × ~200 объектов — дёшево.

### Проверки
`tests/loyalty-sim.mjs` 15/15 — динамика воспроизведена той же формулой:
вражеская башня забирает гекс за ~минуту, равное давление держит бесконечно,
перевес возвращает, симметрия. Плюс проверки подключения и сейва.

## 1.0.105 — Атты-мерген (п.24): конный лучник и жалған шегініс

### Решения

- **Статы между мергеном и жасауылом**: HP 130, ATK 10, R 150 (у пешего 175 —
  с седла целиться хуже), speed 205 (жасауыл 200 — тяжёлая конница его
  ДОГОНЯЕТ, иначе юнит без контры). cd 1.5 против 1.35 у пешего.
- **Броня cav, урон pierce.** Найзагеры ×1.6 — главный ответ; сам ×1.6 по
  коннице и ×1.2 по пешим стрелкам, по пехоте ×0.85. Так он не заменяет
  мергена, а дополняет: против пехоты бери пеших, против конницы — конных.
- **Кайт — пассив, а не приказ.** `kiteFrom` после залпа отводит юнита к
  0.85R, если ближний противник подошёл ближе 0.72R. От стрелков не бегает
  (иначе два лучника разбегались бы по карте), стойка «стоять» выключает.
  Лицо — к цели: парфянский выстрел читается на экране.
- **Арт без gpt image**: кадры жасауыла (все направления уже есть) +
  `tintedFrame` оливковым 0.28 + лук дугой за спиной в координатах кадра
  (масштабируется и отражается вместе со спрайтом). Ключ кэша `anKey|ha`,
  чтобы не смешаться с перекраской ступеней апгрейда.
- **Спрайт-таблицы через `sk`**: внутри `drawUnitSprite` все обращения
  `TABLE[u.key]` заменены на `TABLE[sk]`, где `sk = 'cavalry'` для
  атты-мергена — один алиас вместо девяти новых записей.
- Хоткея нет. Кнопка в доке после жасауыла.

### Проверки
`tests/horsearcher-sim.mjs` 17/17: контры, симуляция кайта против сарбаза
(отходит и стреляет), скорости, подключение. Визуально не проверен — браузер
в песочнице поставить не удалось (загрузка Chromium падает).

## 1.0.106 — Лагеря бандитов (п.25): PvE-цели и общая беда степи

### Решения

- **Юрта как здание стоянки** (`house` нейтральный с флагом `bandit`): готовый
  спрайт, затемнение `source-atop` + чёрный бунчук с черепом. Нейтральные юрты
  в `popCap` не входят — проверено.
- **Кольцо 2.2–5 км**, а не вся карта: на торе 48 000² лагеря по всей площади
  игрок бы не встретил никогда. Кольцо вокруг ОБЕИХ баз — джунгарам достаётся
  столько же.
- **Агрессивны сразу**, в отличие от племён: племя — дипломатия, бандиты —
  угроза. `acquireEnemy` считает их целью авто-обороны, как волков.
- **Набег на шаруа любой стороны**: разбойники — общая беда, а не скрытый
  бонус ИИ. Полуразбитая шайка (<2) не ходит — иначе одиночка суицидничает.
- **Лут — кому снёс**: ИИ тоже получает золото/еду при зачистке. Реликвия —
  только игроку (у ИИ нет `relicsHeld`). Пленные шаруа — редкий и самый
  ценный исход: +2 рабочих без еды и очереди.
- **Респавн 1 в 2 суток при <8**: медленный, чтобы зачистка была
  достижением, а не рутиной.

### Попутно найденный баг
Флаги `tribe` у воинов племён и `nationId` у их башен **не сохранялись**:
после загрузки племя превращалось в обычных нейтралов без лагеря — дипломатия
с ними ломалась. Теперь пишутся и читаются; старые сейвы без стоянок
разбойников получают их заново по сиду.

### Проверки
`tests/bandits-sim.mjs` 23/23, `save-sim` зелёный.

## 1.0.107 — Таран (п.26): ответ на стены

### Решения

- **Один юнит вместо трёх** (таран / лестницы / осадная башня). Задача пункта
  — «ИИ берёт укреплённый аул без катапультного тупика». Таран решает её один;
  лестницы требуют юнита НА стене (новая механика высот), башня — десанта.
  Обе — контент ради контента при текущей стене-полосе.
- **Стрелы −60 %, живые ×0.25**: контра тарана — пехота, а не башни. Иначе
  игрок отвечал бы на таран тем же, чем на всё (башни + мергены), и юнит не
  менял бы решений. Теперь стена без пехоты за ней — не стена.
- **ИИ приводит тараны реактивно**: только если у игрока ≥6 сегментов. Игрок
  без стен таранов не видит — они бы просто гибли.
- Цена 140 🪵 40 🪙 при стене 30 🪵/сегмент: таран стоит ~5 сегментов и
  ломает сегмент за 22 с — окупается, если стена длинная.

### Проверки
`tests/siege-sim.mjs` 15/15: время сноса стены/ворот, число мергенов,
нужное чтобы остановить таран стрелами (≥5 — не вариант), подключение.

## 1.0.108 — Порох (п.27): мылтықшы и фальконет

### Решения

- **Тип урона `gun` вместо новых множителей.** Смысл пороха в истории — броня
  перестала иметь значение. Поэтому пуля ×1 всем: мылтықшы — не «лучник+», а
  ответ на батыров, против которых мерген бесполезен (×0.7). Против лёгких он
  равен мергену и вдвое дороже — значит не заменяет его.
- **Фальконет — против строя, а не стен.** Сплеш 55 px по всем чужим, но ×0.6
  по постройкам, чтобы не убить катапульту как выбор. По одиночке 8 dps, по
  кучке из пяти — 27: игрок учится рассыпать строй, ИИ этого не умеет
  (честная плата за поздний тир).
- **Свои не под огнём**: friendly fire был бы «правдивее», но с click-to-move
  без формаций игрок его не контролирует — только злил бы.
- Порох-ресурс не вводился (см. plan.md).

### Проверки
`tests/gunpowder-sim.mjs` 18/18: dps мылтық/мерген по броне и лёгким,
сплеш-кривая, dps по одиночке против кучки, катапульта по-прежнему лучше по
зданиям, подключение спрайтов/волн/дока.

## 1.0.125 — Фикс вечной загрузки на GitHub Pages: шрифты больше не блокируют рендер

### Симптом
После включения Pages (workflow `deploy-pages.yml`, деплой зелёный): тайтл вкладки
есть, а страница пустая с вечным спиннером. Жёсткая перезагрузка не помогала.

### Диагностика (по шагам, что отпало)
- JS невиновен: продакшн-бандл прогнан в jsdom (скрипт-тест, позже удалён) — React
  рендерит меню целиком («КАЗАХСКОЕ ХАНСТВО», 297 КБ HTML), ноль ошибок.
- В `dist/index.html` ноль абсолютных путей: JS/CSS/спрайты вшиты (singlefile 14 МБ),
  `voices/` — относительные пути, фавикон — data-URI. Переносимость подтверждена,
  `base: './'` НЕ нужен.
- Остался единственный внешний render-blocking ресурс: `<link rel=stylesheet>` на
  `fonts.googleapis.com`. Пока он висит — браузер не рисует ни пикселя: тайтл (уже
  распарсенный `<head>`) есть, контента нет, спиннер крутится. Все симптомы сошлись.

### Фикс (только `index.html`, логика не тронута)
Классический loadCSS-паттерн: `rel=preload as=style` + `rel=stylesheet media=print
onload="this.media='all'"` + `<noscript>`-фолбэк. Шрифты грузятся в фоне: висят —
игра всё равно рисуется мгновенно на системных, подтянулись — swap без перерендера.
После фикса внешних render-blocking ресурсов в проекте не осталось вовсе.

### Проверки
Пересборка: новые link-теги на месте в `dist/index.html`, бандл стартует как раньше.
Финальная проверка — открыть Pages после автодеплоя (Ctrl+F5 от кешированной копии).

### Попутно: песочница сбрасывает .git между ходами
В этом же ходе обнаружено: указатель сессионной ветки и `--unshallow` слетели к
состоянию старта сессии (HEAD вернулся на `99f9f64`, shallow-файл восстановился),
а файлы рабочей копии сохранились. Лечение: `fetch --unshallow`, сверка дерева с
ожидаемым SHA через `git diff <SHA> --stat` (не `git status`), `git reset <SHA>`,
затем штатный `merge --ff-only origin/main`. Пуш на GitHub при этом никуда не
девается — удалённая ветка сессии уцелела. Проверять `git log --oneline -1` перед
гит-операциями в начале каждого хода.

## 1.0.126 — Чанки: сборка больше не один HTML на 14 МБ (+ запрет PR)

### Причина
В 1.0.125 убрали единственный блокирующий внешний ресурс (шрифты Google), но монолит
остался: `vite-plugin-singlefile` вшивал в `index.html` весь JS, CSS и ~9.5 МБ спрайтов
в base64 → **14 040 КБ одним файлом (10 184 КБ gzip)**. Браузер обязан скачать его
целиком до первого рендера, и на Pages это всё равно выглядит как «вечная загрузка».

### Что сделано (`vite.config.ts`)
- `viteSingleFile()` **выключен по умолчанию**; включается флагом окружения
  `SINGLE_FILE=1` (скрипт `npm run build:singlefile`) — режим «один HTML» не потерян.
- `manualChunks` делит код на 5 кешируемых чанков: `vendor-react` (React/react-dom),
  `vendor-ui` (lucide-react/clsx/tailwind-merge), `game-engine` (`engine.ts`, 8.5 тыс.
  строк — самый крупный и редко меняющийся), `game` (остальное из `src/game/`),
  `index` (`App.tsx` + точка входа). Правишь движок — игрок не перекачивает React.
- `assetsInlineLimit: 2048`: мелочь (иконки, svg-паттерн) уходит в бандл, спрайты
  остаются файлами — 241 файл, ~10 МБ, грузятся параллельно и по факту обращения.
- `base: './'` — всё ссылается относительно `index.html`: Pages-подпуть, подпапка и
  любой статик-хостинг работают без правки конфига (в singlefile-режиме путей не было).

### Цифры (прод-сборка, до → после)

| | было (singlefile) | стало (чанками) |
|---|---|---|
| `index.html` | 14 040 КБ (gzip 10 184 КБ) | **2.5 КБ** |
| критический путь до первого рендера | 14 040 КБ (gzip 10 184 КБ) | **~827 КБ (gzip ~253 КБ)**: html + CSS 81 КБ + JS 744 КБ |
| JS | один вшитый файл | `game-engine` 270 (gzip 89), `vendor-react` 192 (60), `index` 137 (37), `game` 133 (48), `vendor-ui` 11 (4) |
| спрайты | base64 внутри HTML | 241 файл по ~10 МБ, грузятся по факту |
| `voices/` | как было | без изменений (2 МБ mp3, относительные пути) |

### Проверки
- `npm run build` — 5 чанков + CSS + спрайты; `SINGLE_FILE=1 npx vite build` даёт тот же
  бандл, что и до изменения (хеши `index-kbxMqyIo.js` / `style-BEzKC3Hj.css` совпали),
  то есть старый режим не сломан.
- `npx tsc --noEmit` — чисто (правилась только конфигурация сборки).
- Тесты: `tests/regress-sim.mjs`, `gunpowder-sim.mjs` (18 ok), `kurultai-sim.mjs`
  (23 ok), `econ-sim.mjs` (13 ok) — зелёные.
- Статик-сервер на `dist/`: 200 на `index.html`, все чанки, спрайт и `voices/azan.mp3`.
- В `dist/index.html` и во всех чанках **ноль абсолютных путей**; спрайты подключаются
  как `new URL("….png", import.meta.url).href` → работают с любого подпути.

### Что НЕ сделано (следующий шаг, осознанно)
- **Ленивая загрузка чанка движка** по клику «Играть» дала бы ещё ~270 КБ экономии на
  первом рендере, но `App.tsx` использует `Game`/`DEDICATIONS`/`counterText` на уровне
  модуля (меню, `hasSave`, `setInGame`) — это переписывание 1.9 тыс. строк UI с риском
  для игровой логики, а не правка конфига.
- 241 спрайт (~10 МБ) на момент релиза всё ещё запрашивались все сразу, как только
  выполнялся чанк движка (`new Image()` в `engine.ts`/`pixelart.ts`) — это не блокировало
  первый рендер, но съедало канал. **Исправлено в 1.0.127** (ленивые спрайты).

### Запрет PR (вторая половина релиза)
- Правило 2 в разделе «Уведомления» переписано: **`main` обновляется прямым пушем,
  PR не открываются** — review-ветки расходятся с сессионной и дублируют уведомления.
- `CONTRIBUTING.md` — предупреждение наверху; раздел «Правила для PR» теперь читается
  как правила для кода.
- `ci/no-pr-workflow.yml` — готовый workflow: комментирует и закрывает любой входящий PR.
  Ставить вручную (у токена агента нет права `workflows`, `.github/` в `.gitignore`),
  как и `ci/notify-workflow.yml`.

### Гит-механика этого хода (важно для следующих сессий)
Сессионная ветка `arena/01a091a8-strategy-browser-game` стартовала от `99f9f64` (1.0.108)
и была **на 29 коммитов позади `main`** (1.0.125), а не «на 1 впереди». Коммит 1.0.126
положен уже после `git merge --ff-only origin/main`, иначе он оказался бы на базе
двухнедельной давности и при вливании отменил бы 1.0.109–1.0.125.
Проверять связку перед релизным коммитом: `git fetch --deepen=40 origin main`
(клон мелкий, без deepen истории не хватает) → `git merge --ff-only origin/main` →
`git log --oneline -1`.

## 1.0.127 — Ленивые спрайты: меню больше не качает 10 МБ графики

### Симптом
После 1.0.126 страница стала рендериться сразу (2.5 КБ HTML), но чанк движка при
загрузке создавал ~240 картинок (`new Image()` + `im.src = …`) в модульной области
видимости `engine.ts`/`pixelart.ts` — то есть **все ~10 МБ спрайтов начинали качаться
в ту же секунду, что и меню**, просто не блокируя первый кадр. На медленном канале
это по-прежнему съедало весь канал и тормозило всё остальное.

### Фикс: создаём картинку сразу, `src` — потом
Ключевая деталь: запрос в сеть делает не `new Image()`, а **назначение `src`**. Поэтому:

- `pixelart.ts`: `mk(url)` создаёт пустой `<img>` и кладёт пару `[img, url]` в
  `PENDING_SPRITES`; `warmSprites()` назначает `src` всем отложенным.
- `engine.ts`: то же для спрайтов зданий (`loadBldSprite`/`makeExtraSprite`) и
  AI-рельефа (`TERRAIN_FEATURES`) через `pendingImg()`; `warmGameSprites()` запрашивает
  свои + pixelart. Обе функции идемпотентны (список очищается).
- Прогрев вызывается в двух местах: **в конструкторе `Game`** (гарантия: партия всегда
  с графикой) и **в `App.tsx` на `requestIdleCallback`** — как только меню отрисовалось
  и браузер свободен, спрайты подгребаются в фон, поэтому к нажатию «В поход!» они
  уже в кеше.
- Побочки нет: все места рисования и так проверяли `im.complete && im.naturalWidth`
  и рисовали процедурный фолбэк — «спрайт ещё не приехал» для рендера штатное
  состояние, а не ошибка.

### Цифры

| | до 1.0.127 | после |
|---|---|---|
| запросов графики при открытии меню | ~274 (≈10 МБ) | **0** |
| когда качаются спрайты | сразу, вместе с меню | в idle после первого рендера (или на старте партии) |
| первый рендер | ~827 КБ (gzip ~253 КБ) | без изменений |

### Проверки
- `tests/lazy-sprites-test.mjs` (12 ok) — **исполнительный** тест: движок собирается
  esbuild'ом (ассеты — заглушки), `Image` подменяется на счётчик `src`; проверяется,
  что на импорте запросов 0, отложенных 274, после `warmGameSprites()` — ровно 274
  (219 уникальных файлов), повторный вызов не дублирует. Плюс проверки по исходникам.
- `tsc --noEmit` чисто, `npm run build` без предупреждений, регресс-тесты
  (`regress-sim`, `gunpowder-sim` 18 ok, `kurultai-sim` 23 ok, `prayer-test` 57/57).

### Что дальше (осознанно не сделано)
- **Ленивый чанк движка** (динамический `import('./game/engine')` по клику «Играть»)
  снял бы с первого рендера ещё ~270 КБ, но `App.tsx` использует `Game`/`DEDICATIONS`/
  `counterText` на уровне модуля. **Сделано в 1.0.128** — выигрыш оказался скромным
  (−29 КБ gzip), потому что общие модули меню всё равно нужны сразу.
- Спрайты по-прежнему запрашиваются пачкой (все 274 сразу); можно грузить по
  биомам/эпохам, но это уже дробление кеша, а не экономия трафика.

### Заметка (тесты, 2026-09-12): 4 красных теста оказались устаревшими проверками

Полный прогон `tests/*.mjs` показывал 46 зелёных и 3 падающих (`age-sim`, `azan-real`,
`ui-design` — 4 проверки). Разбор показал: **прав был код, а не тест** — проверки писались
под более ранние правила и не обновились, когда правила расширили:

| тест | что проверял | что в коде сейчас |
|---|---|---|
| `age-sim` «кнопка закрывает экран» | свиток итогов закрывается кнопкой | золотой век **держит** свиток, пока не выбрано посвящение (п.22); обычные эпохи — как раньше |
| `azan-real` «в HUD нет расписания, пока режим выключен» | `hud.realAzan === null` | расписание уходит в HUD **всегда** (справочно), выключенность — флаг `realAzan.on === false` |
| `ui-design` «фон меню — SVG-орнамент» | inline `data:image/svg+xml` | орнамент — файл-ассет `pattern.svg` (кешируется, не раздувает JS) |
| `ui-design` «Enter не стартует…» | `!showSettings && !infoTab` | добавлен `!showCamp` — попап глав кампании |

Тесты переписаны под новое поведение (не ослаблены: `age-sim` теперь проверяет оба
правила — и что свиток держится, и что после `chooseDedication` закрывается).
Итог: **49/49 зелёные**. Коммит — `tests: …` (версию игры не поднимали: это не релиз).

### Грабли песочницы (дополнение к 1.0.125)
- **`git worktree remove --force` удаляет содержимое по симлинкам.** Создали
  `/tmp/base-…/node_modules -> <репо>/node_modules`, чтобы прогнать тесты на чистом
  `HEAD`; при удалении worktree git пошёл по ссылке и вынес `node_modules` основного
  репозитория. Лечение: `npm ci` (6 с). В следующий раз — копировать, а не линковать.
- В тот же момент `HEAD` сессионной ветки again откатился на `99f9f64` (старт сессии):
  рабочие файлы уцелели, но индекс показывал кучу «изменённых» и «неотслеживаемых».
  Лечение подтвердило прежний рецепт: `git fetch --unshallow` (история вернулась, 169
  коммитов) → `git reset --hard <SHA pushes>` → `git status` чист. Спасает то, что
  каждый релизный коммит сразу пушится в обе ветки: SHA всегда есть и локально, и на
  GitHub, поэтому `reset --hard` ничего не теряет.

## 1.0.128 — Ленивый чанк движка: меню не ждёт 350 КБ игрового кода

### Что мешало
К 1.0.127 меню уже не качало спрайты, но чанк движка (`engine.ts` 8.5 тыс. строк +
pixelart + iso + audio + terrain) оставался в статическом графе `App.tsx`: три вызова
`Game.wasInGame()` / `Game.setInGame()` / `Game.hasSave()` в меню делали движок
обязательным для первого рендера.

### Что сделано
- **`src/game/session.ts`** (новый): флаги сессии в localStorage (`hasSave`,
  `wasInGame`, `setInGame`, `clearSave`). Три строчки localStorage больше не тянут
  за собой движок. В `Game` оставлены статики-обёртки — вызовы в игре и тестах не менялись.
- **`App.tsx`**: движок импортируется динамически.
  - `import type { Game as GameEngine, GameStats, HudSnapshot }` — типы стираются при
    сборке, в рантайм не попадают;
  - `let ENGINE: EngineModule | null` — ссылка на подгруженный модуль (нужна рендеру:
    `unitStats()` для подсказок дока и `DEDICATIONS` для выбора посвящения; до загрузки
    — `''` и `[]`, но эти блоки рисуются только в партии);
  - эффект создания игры стал async: `const eng = ENGINE ?? await import('./game/engine')`,
    с флагом `cancelled`, чтобы не создать партию после ухода с экрана;
  - в простое (`requestIdleCallback`) чанк подгружается заранее — клик «В поход»
    срабатывает мгновенно. Спрайты при этом не трогаем (их прогреет конструктор `Game`).
- **`vite.config.ts`**: ручное правило «всё из `src/game/` → чанк `game`» **снято**.
  Оно и было ловушкой: в один чанк попадали и статически нужные меню модули
  (`config`, `nations`, `prayer-times`, `iconset`, `session`), из-за чего чанк движка
  оказывался частью статического графа и всё равно скачивался на первом рендере
  (в `index.html` появлялся `modulepreload` на него). Границу теперь проводит Rollup сам —
  по `import()`.

### Цифры (прод-сборка)

| | было (1.0.127) | стало (1.0.128) |
|---|---|---|
| JS+CSS первого рендера | 554 КБ (gzip ~164 КБ): index 137 + game 133 + vendor-react 192 + vendor-ui 11 + CSS 81 | **475 КБ (gzip ~135 КБ)**: index 190 + vendor-react 192 + vendor-ui 11 + CSS 81 |
| чанк движка | в статическом графе | **352 КБ (gzip ~118 КБ), лениво** — по клику «В поход» |
| выигрыш первого рендера | — | **−79 КБ (−29 КБ gzip, ≈ −18 %)** |

Выигрыш скромнее, чем в 1.0.126/1.0.127, и это нормально: `config`, `nations`,
`prayer-times`, `iconset` нужны меню сразу, поэтому остались в главном чанке. Плюс
предзагрузка в простое: суммарно по сети уходит столько же байт, меняется только то,
что первый рендер не ждёт. Отдельный бонус — кеш: правки UI больше не инвалидируют
чанк движка и наоборот.

### Появился смоук-тест (`tests/smoke-dom.mjs`, 12 ok)
Первая в проекте проверка, поднимающая **всё приложение целиком**: esbuild собирает
`src/main.tsx` в IIFE, jsdom его исполняет, canvas подменён «вседозволенным» прокси
(как в `age-sim`), `Image` считает назначения `src`. Проверяет: React примонтировался,
меню отрисовалось, версия в футере, ноль ошибок в консоли, **ноль запросов спрайтов на
меню**, клик «В ПОХОД» → появился canvas, спрайты поехали (283), ошибок нет.
Раньше такую проверку делали один раз руками (1.0.125) и удаляли — теперь она постоянная,
именно поэтому правка старта игры не прошла незамеченной.
Зависимость: `jsdom` (devDependency, ^30.0.1) — только для этого теста, в бандл не попадает.

### Проверки
`tsc --noEmit` чисто; `npm run build` — 4 чанка, `index.html` не ссылается на движок;
`tests/*.mjs` — **50/50 зелёных**; смоук-тест 12 ok; прогон в jsdom показывает старт
партии без ошибок после клика.

### Грабли (важно для следующего раза)
`git worktree remove --force` на worktree, куда прилинкован `node_modules` симлинком,
**удаляет содержимое по ссылке** — то есть выносит `node_modules` основного репозитория
(лечение: `npm ci`, 6 с). В тот же момент `HEAD` сессионной ветки again откатился на
старт сессии. Спасает привычка пушить каждый релизный коммит сразу в обе ветки: SHA
есть и локально, и на GitHub, поэтому `git reset --hard <SHA>` восстанавливает всё.

## 1.0.129 — Роды-таңба (п.12 плана): Арғын, Қыпшақ, Найман, Ұйсын

Закрыт последний открытый пункт `plan.md` и первый в рекомендованном порядке
(12 → 19 → 20 → …): перед походом игрок выбирает род, и партия начинается с другими
акцентами без нового контента карт.

### Что сделано
- **`src/game/clans.ts`** (новый): таблица родов + множители. `clanMods(clan)` на
  неизвестный/пустой id возвращает нейтральные 1 — поэтому **старые сейвы грузятся без
  бонусов**, а не падают. `clan: ClanId` добавлен в `Settings` (сохраняется в localStorage).
- **Пассивки (только игроку, ±10–15%):**
  | род | бонус 1 | бонус 2 |
  |---|---|---|
  | Арғын | приплод: 15% шанс двойни в загонах | загоны −15% |
  | Қыпшақ | керуен +15% выручки | барлаушы +15% скорости |
  | Найман | конница (батыр/жасауыл/атты-мерген) +10% HP | батыр −15% |
  | Ұйсын | стройка +12% | башни +15% HP |
- **Хуки в движке:** `addUnit` (скорость барлаушы, HP конницы), `addBld` (HP башен),
  `updateBuildings` (темп стройки), керуен (`trGold`), `telBirth` (двойня),
  `bldCost`/`clanUnitCost` (цены), `demolish` (возврат по той же цене).
- **Единая цена:** `clanBldCost()`/`clanUnitCost()` в `config.ts` — движок списывает и
  док показывает одну и ту же сумму. Движок внутри использует `bldCost()`, так что
  точка ровно одна (иначе игрок увидел бы одно, а заплатил другое).
- **Таңбы:** 4 иконки в `iconset.ts` (`tamga-argyn|qypshaq|naiman|uisyn`) — бейдж с
  подсказкой в HUD, знак над ханской ставкой на карте и крупные иконки в селекторе меню.
- **ИИ род не получает**: у него упрощённая экономика, и равные сравнения силы сторон
  были бы обманом.

### Проверки
- `tests/clans-sim.mjs` (40 ok): таблица множителей executes (сборка esbuild'ом), по два
  бонуса на род, отклонения в ±15%, неизвестный род нейтрален, скидки только на свои
  ключи (загон — только Арғын, батыр — только Найман), и движок реально применяет
  бонусы в семи местах (проверки по исходникам).
- `tests/smoke-dom.mjs` дополнен: в реальном DOM меню ровно 4 карточки рода, переключение
  не ломает меню (стало 15 ok).
- `tsc --noEmit` чисто, сборка без предупреждений, **51/51 тестов зелёных**.

### Заметка на будущее
Из описания пункта не сделана только «ИИ тоже получает род (виден в дипломатии)» —
осознанно: это потребует уравновешивания экономики ИИ, а не просто таблички.

## 1.0.130 — пятый род: Керей (мудрость и послы)

Просили добавить род Керей. Уточнение, которое стоит помнить: это **племя Керей**,
а не «Керей-хан» — в описании рода и в подсказках нельзя смешивать эти вещи.

### Что сделано
- **Керей** (пятый род, таңба «ай» — полумесяц на древке): **мудрость копится на
  15% быстрее** (`wisdomRate`) и **посланники к племенам на 15% дешевле**
  (35 → 30 золота за первого, дальше 60 → 51 и т.д.). Тематически — «род биев и
  дипломатии»: мудрость и послы, а не война.
- Новые ключи множителей `wisdom` и `envoy` в `ClanMods`; у остальных родов они 1
  (проверяется тестом).
- **Побочно исправлена старая несогласованность**: цена посланника считалась в двух
  местах, и панель дипломатии НЕ показывала скидку ясы «Аралас ұлыс» (игрок видел
  35, а платил 17). Теперь есть один метод `envoyPrice(nid)` — его зовут и
  `sendEnvoy` при списании, и HUD при показе. Ровно тот же принцип, что с
  `clanBldCost`/`bldCost` в 1.0.129: одна цена, а не две.
- Сетка селектора рода: было 4 колонки, с пятью родами осталась бы сирота — стало
  `grid-cols-2 sm:grid-cols-3 lg:grid-cols-5`.

### Проверки
- `tests/clans-sim.mjs` → **49 ok**: 5 родов, по две пассивки, ±15%, Керей один
  владеет мудростью и послами, цена посланника считается в одном месте.
- `tests/smoke-dom.mjs` → **15 ok**: в DOM ровно 5 карточек, переключение на Керей
  не ломает меню.
- `tests/yasa-eureka-sim.mjs` пришлось поправить: он искал в исходнике буквальную
  строку старого вычисления цены посланника (регексп по `engine.ts`), а выражение уехало
  в `envoyPrice()`. Проверка обновлена, яса «Аралас» по-прежнему −50%.
- 51/51 тестов зелёных, `tsc` чисто.

### Заметка про иконки
Таңбу рисовал «вслепую»: ImageMagick в песочнице без SVG-делегата, PIL нет.
Написал мини-растеризатор SVG-путей на чистом Python (`/tmp/razr.py`, вне репо):
парсит M/L/h/v/a, дуги сэмплит по параметризации «концы → центр», рисует в буфер и
пишет PNG через zlib. Дальше печатал знак ASCII-артом и смотрел на него — так
поймал две ошибки: дуги с large-arc-flag=1 раздували полумесяц за край иконки, а
две дуги в 16 px сливаются в кляксу. В итоге «ай» — одна дуга с древком. Приём
годится и для других иконок: **рендер в ASCII + метрики (заливка, bbox, различие
пар)** вместо глаз.

## 1.0.131 — род джунгар: враг тоже выходит под таңбой

Последний открытый подпункт п.12 («вражеский ИИ тоже получает род, виден в
дипломатии») закрыт.

### Что сделано
- В начале партии джунгарам жеребьёвкой достаётся свой род (`rivalClan`,
  `randomClan()` в `clans.ts`). Род едет в сейв; в **старых сейвах поля нет — род
  доигрывается**, партия грузится как ни в чём не бывало (проверено тестом).
- **Врагу идут только те множители, которые у него что-то меняют**: экономика ИИ
  упрощённая (золото по формуле, очередей найма и караванов нет), поэтому «приплод»,
  «загоны», «керуен», «батыр», «мудрость» к нему неприменимы — нечего масштабировать.
  Применяются: `cavHp`, `towerHp`, `build`, `scout`, `envoy`.
- **Честное следствие:** у рода Арғын боевого множителя нет, так что партия против
  джунгар-Арғын объективно легче. Это задуманная асимметрия: враг разный от партии
  к партии. Так и записано в подсказке панели.
- Видно в двух местах: бейдж «род врага: …» с таңбой и примечанием в карточке
  джунгар в панели дипломатии, и багровая таңба над их ханской ставкой на карте
  (у игрока — золотая, чтобы не путались).

### Числа (сняты на живом движке, не расчётом)
| что | база | у «своего» рода |
|---|---|---|
| конница джунгар (Найман) | 250 HP | **275 HP** (+10%) |
| батыр джунгар (Найман) | 190 HP | **209 HP** |
| башня джунгар (Ұйсын) | 550 HP | **633 HP** (+15%) |

### Проверки
- **Новый `tests/rival-clan.mjs` (17 ok) гоняет настоящий движок**, а не ищет
  строки в исходнике: поднимает `Game` headless (паттерн из `tests/age-sim.mjs` —
  подставной canvas/DOM/localStorage), спавнит вражескую конницу и башни при разных
  родах и сравнивает HP, проверяет сейв-раундтрип, старый сейв без `rivalClan`, что
  род игрока не протекает на врага и наоборот.
- `tests/clans-sim.mjs` (49 ok) и `tests/yasa-eureka-sim.mjs` ловили буквальные
  выражения в `engine.ts`; после правок их регекспы пришлось обновить — это
  ожидаемая цена тестов «по исходнику».
- 52/52 тестов зелёных, `tsc` чисто, сборка без предупреждений.

### Урок на будущее
Тест «ищем строчку в исходнике» хрупок: любая перестановка выражения роняет его,
хотя поведение не изменилось. Там, где можно поднять движок headless (паттерн
`age-sim.mjs`), лучше проверять поведение — так и надёжнее, и понятнее, что именно
сломалось.

## 1.0.132 — шестой род: Дулат (добыча и найм)

Продолжение п.12 по своей инициативе: план закрыт, следующий осмысленный шаг —
расширить систему родов туда, где у неё ещё не было механик.

### Что сделано
- **Дулат** (шестой род, таңба «қос белгі» — два шеврона): **добыча на 12%
  быстрее** и **войска готовятся на 15% быстрее**. Это первые множители, под
  которые пришлось добавлять новые точки в движке (`workMult` и `train`), а не
  переиспользовать готовые — система родов растёт через хуки.
- Оба бонуса только игроку: у ИИ нет своей очереди найма и добычи в полном смысле
  (золото капает по формуле, вражеские крестьяне появляются отдельным кодом со
  своей константой `total: 7`). Поэтому **джунгары-Дулат в бою нейтральны** — как и
  джунгары-Арғын. Два нейтральных рода из шести — осознанная цена честности: лучше
  так, чем приписывать ИИ бонус, которого он не получит.
- Сетка селектора: 6 карточек — `grid-cols-2 sm:grid-cols-3` (3×2 на планшете),
  вариант с 5 колонками оставлял бы сироту.

### Цифры с живого движка
| | база | Дулат |
|---|---|---|
| добыча (workMult) | ×1.000 | **×1.120** |
| найм крестьянина | 7.00 с | **6.09 с** |

### Проверки
- `tests/clans-sim.mjs` → 55 ok, `tests/rival-clan.mjs` → **20 ok** (добыча и найм
  проверены на настоящем `Game`, а не по исходнику), `smoke-dom` → 15 ok (6 карточек).
- 52/52 тестов зелёных, `tsc` чисто, сборка без предупреждений.

### Что стоит помнить
1. **Тесты «по исходнику» ломаются третий раз подряд**: `yasa-eureka-sim` (1.0.130),
   `clans-sim` (1.0.131), `kurultai-sim` (1.0.132). Все три ищут буквальные
   выражения в `engine.ts`; любая перестановка роняет тест, хотя поведение не
   менялось. Правило: новый тест писать behavioural (pattern из `age-sim.mjs` —
   headless-движок), а старые регекспы при правках обновлять осознанно, проверяя,
   что смысл проверки сохранился.
2. **Сброс песочницы повторился (4-й раз)**: `node_modules` исчез, `HEAD` улетел на
   коммит начала сессии. Рабочие файлы уцелели. Рецепт восстановления:
   `git fetch --deepen=60 origin arena/01a091a8-strategy-browser-game` →
   `git reset --hard <sha последнего релиза>` → `npm ci`. Перед `reset --hard`
   обязательно копировать незакоммиченные файлы вне репо (в этот раз — `clans.ts`).
   Спасает то, что релиз запушен на GitHub: `git ls-remote` показывает актуальный
   sha даже когда локальная история откатилась.

## 1.0.133 — у каждого рода врага появился боевой эффект

Выбор пользователя из двух вариантов: «придумать боевой перевод» вместо «оставить
нейтральными» и вместо «исключить из жеребьёвки».

### Что сделано
Множители, у которых нет прямого рычага в ИИ, **переведены** на то, чем джунгары
реально управляют:

| род врага | множитель | перевод |
|---|---|---|
| **Арғын** | `tel` (приплод) | **набеги плотнее**: состав волны ×1.15 |
| **Дулат** | `train` (найм) | **стан люднее**: лимит крестьян ×1.15 (8 → 9) |

Остальные четыре рода применяются напрямую (конница, башни, скорость конных,
частота послов). В панели дипломатии у всех шести теперь осмысленная строка.

### Почему не «гарнизон больше»
Идея «Дулат-враг держит больший гарнизон» оказалась мёртвым кодом: `canGarrison()`
жёстко требует `b.owner === 'player'`, враг юнитов в здания не прячет. Проверять
существование рычага — обязательный шаг перед тем, как вешать на него бонус.

### Что показали измерения
- Арғын-враг: на **первой-второй волне разницы нет** — 15% от трёх воинов тонут в
  округлении. Эффект появляется с середины партии (6-я волна: 27 → 28 воинов).
  Это записано прямо в тест, чтобы никто не искал баг там, где его нет.
- Дулат-враг: крестьян в стане 8 → 9.

### Проверки
- `tests/rival-clan.mjs` → **26 ok**: состав волны при разных родах, крестьяне
  после 90 с симуляции, отсутствие строки «нейтрально» в `RIVAL_NOTE`.
- `tests/dynamic-ai-sim.mjs` обновлён (регексп по формуле волны) — это уже
  **четвёртый** source-regex тест, сломавшийся за три релиза (`yasa-eureka-sim`,
  `clans-sim`, `kurultai-sim`, `dynamic-ai-sim`). Вывод прежний и теперь
  обязательный: новые проверки писать behavioural на headless-движке, старые
  регекспы править осознанно, сохраняя смысл.
- 52/52 тестов зелёных, `tsc` чисто.

## 1.0.134 — балансный проход по родам (выбор пользователя: «баланс и полировка»)

### Как мерили
Новый `tests/balance-lab.mjs`. Главная ловушка, которую пришлось обойти: **карта при
каждом запуске новая**, поэтому «прогнал партию за Арғын, прогнал за Дулата» даёт
разницу из рельефа, а не из рода. Мерим в ОДНОЙ партии: переключаем
`game.settings.clan` и снимаем показания с того же крестьянина.

Второй сюрприз: **скорость юнита при рождении умножается на `rand(0.94, 1.06)`** —
разброс ±6%. Один-два юнита не доказывают ничего: замер скорости керуена давал то
+9.6%, то +19.3% при настоящих +15%. Лечение — среднее по 40 юнитам (погрешность
среднего ≈ ±0.8%, замер показал +14.3…14.7%).

### Что нашлось
Взвешенная «сила» рода (вес домена = как часто он включается в обычной партии):
Дулат был вдвое сильнее слабейшего — у него оба бонуса (добыча, найм) работают с
первой до последней минуты, а у Арғын и Керей бонусы узкие (загоны, послы).

### Что сделали
| род | было | стало |
|---|---|---|
| **Дулат** | добыча +12%, найм +15% | **+10% и +10%** (всё ещё «род темпа», но не доминант) |
| **Арғын** | приплод и загоны | + **дойка на 15% быстрее** (тот же множитель животноводства) |
| **Қыпшақ** | доход керуена, скорость разведчика | + **керуены идут на 15% быстрее** |
| **Керей** | мудрость, послы | + **скидка и на дары** (не только на послов) |

Разброс сил: **×2.12 → ×1.73** (индекс: Дулат 57.2, Ұйсын 46.6, Қыпшақ 43.5,
Найман 41.4, Арғын 40.5, Керей 33.0). Дальше подгонять под индекс не стал: веса в
нём — моя оценка, а не измерение, и подгонка под собственную модель хуже, чем
честный разброс. Все множители по-прежнему в ±15% (проверяется тестом).

### Побочная находка
Цена дара племени считалась в трёх местах, и **обработчик действия в панели вообще
не знал про скидку ясы «Аралас ұлыс»** (как и HUD с ценой посланника в 1.0.130) —
игрок видел 40, а платил 20, или наоборот. Собрал всё в один `giftPrice()` по тому
же принципу, что `envoyPrice()` и `bldCost()`.

### Проверки
- `tests/balance-lab.mjs` (2 ok) — таблица доменов + контроль ±15% и разброса.
- `tests/rival-clan.mjs` → 31 ok: дойка Арғын, скорость керуенов Қыпшақ (среднее по
  40), цены даров и послов Керея.
- `tests/yasa-eureka-sim.mjs` обновлён — **пятый** source-regex тест, сломавшийся
  из-за переноса выражения. Правило закреплено: новые проверки — behavioural.
- 53/53 тестов зелёных, `tsc` чисто.

## 1.0.135 — мобильная версия: камера пальцем и верхняя панель

Жалоба: «мобильная версия вообще не играбельна». Снимок — `errors/moblie-scr.png`
в репозитории (828×1792, то есть CSS-вьюпорт 414×896 — iPhone XR/11).

### Как читали снимок без глаз
В этой сессии у меня нет зрения, поэтому картинку разбирал кодом: написал на чистом
Python декодер PNG (zlib + расфильтровка строк) и снял две метрики — профиль яркости
по высоте и средний цвет по полосам. Получилось: сверху ~30% и снизу ~42% экрана
занято тёмными панелями, поле боя — только ~25% высоты. Дальше смотреть стало не на
что, и я пошёл в код — там причина нашлась точно.

### Причина №1 — камера не двигалась пальцем
`panMode` по умолчанию был `false`, поэтому **палец рисовал рамку выделения**, а
сдвинуть камеру можно было только двумя пальцами или стрелками у края. Переключатель
«Кам./Рамка» в HUD существует, но он спрятан среди кнопок и по умолчанию выключен —
то есть первый запуск на телефоне выглядит как «игра не слушается».

Сделано: по умолчанию палец **панорамирует**, рамка выделения — по кнопке «Рамка».
Мышь не пострадала: она панорамирует правой/средней кнопкой, а рамку ведёт левой —
ветка в `pUp` теперь проверяет тип указателя, а не только флаг.

### Причина №2 — верхняя панель съедала экран
Пилюли (день, мудрость, ислам, курултай, азан, намаз, погода…) лежали в
`flex flex-col` — **каждая на своей строке**. Десяток пилюль = ~270 px из 896.
Сделано: на узком экране элементы переносятся по строкам (`flex-wrap`, 3-4 ряда
вместо 12), на широком остаются колонкой (`sm:flex-col`). Плюс подсказка для
касаний вместо клавиатурной: «палец — камера • щипок — зум • тап — выбор и приказ».

### Что осталось под вопросом
Уверенно сказать, что именно игрок видел на снимке, я не могу — только косвенные
метрики. Если осталось неиграбельно, вероятные candidates: размер кнопок в доке
(сейчас 72×52 px), шрифты 8.5–11 px и модалки (дипломатия, древо техов) на узком
экране. Проверять надо уже глазами.

### Проверки
53/53 тестов зелёных, `tsc` чисто, сборка без предупреждений. Регрессию на мышь
страхует то, что обе ветки в `pDown`/`pUp` разделены по `pointerType`.

## 1.0.136 — мобильная вёрстка: цели для пальца и модалки

Продолжение 1.0.135 по пунктам, которые я сам же назвал вероятными.

- **Цели для пальца:** вкладки дока (Войска/Стройка) были ~28 px — стало 40; MiniBtn
  38 → 42; кнопки найма/стройки 72 → 76 px и выше (`py-2`), подпись 10 → 11 px,
  цена 8.5 → 10 px на касании. На десктопе всё оставлено как было (`sm:`-префиксы).
- **Модалки:** у семи окон (яса, курултай, той, итоги эпохи и др.) не было предела
  высоты — на 414×896 содержимое уезжало за край без прокрутки. Добавил
  `max-h-[88dvh] overflow-y-auto`; у двух окон предел уже был (92dvh).
- **Сторожевой тест `tests/mobile-layout.mjs` (13 ok).** Осознанное исключение из
  правила «не писать тесты по исходнику»: jsdom не считает layout, браузера в
  песочнице нет, а инвариант важный — именно так семь модалок жили без max-h.
  Ложные срабатывания, которые пришлось убрать: `bg-black/40` внутри карточек
  (не оверлей) и панель-колонка со внутренней прокруткой (ей хватает max-h).

Проверки: 54/54 тестов зелёных, `tsc` чисто, сборка без предупреждений.

## 1.0.137 — мобильный интерфейс: интерфейс больше не закрывает поле боя

Выбор пользователя из трёх вариантов: **док-ящик + кнопка-глаз**, **мини-карта
чипом**, **все четыре правки камеры**.

### Раскладка
- Левая колонка (задания + отряд + экономика) на узком экране больше не
  показывается: она съедала 172px из 414. Её содержимое вынесено в компоненты
  `QuestsPanel` / `SquadControls` / `EconSummary` и открывается вкладками дока
  **ОТРЯД** и **СВОДКА** (только на телефоне, `sm:hidden`).
- Док на телефоне свёрнут по умолчанию: видна одна строка вкладок, лист
  раскрывается по тапу и сам закрывается при касании поля (`onPointerDown` на
  canvas). Высота листа — не больше 45dvh. На десктопе док раскрыт всегда.
- **«Чистый экран»** (кнопка-глаз у правого края): прячет верхний HUD, док,
  левую колонку, карточку выделения, баннеры режима, ТОЙ и Көш; движок по флагу
  `uiHidden` перестаёт рисовать мини-карту и ловить её тапы.

### Камера и мини-карта
- Карта на телефоне — чип 96px вместо панели 168px. **Касание чипа больше не
  телепортирует камеру**: тап раскрывает карту (390px, не выше половины экрана),
  а протяжка от чипа просто двигает камеру. Раньше в правом нижнем углу
  панорамы не было вообще — карта перехватывала касание на `pDown`.
- Инерция: после отрыва пальца камера докатывается (~0.24 с, гаснет сама).
- Панорама касанием ×1.15 (поле узкое, 1:1 заставляет делать лишние свайпы).
- Стена и ворота: ветка протяжки встала **до** панорамирования касанием, иначе
  палец всегда двигал камеру и линию стены на телефоне провести было нельзя.
- Ручная панорама отменяет автоследование камеры (`camFollow`).

### Тесты (55 файлов, все зелёные)
- новый **`tests/mobile-camera.mjs`** (30 ok) — поведенческий: реальные
  `pDown/pMove/pUp` с `pointerType: 'touch'` на canvas 414×896. Ловит чип,
  протяжку от чипа, перелёт по карте, линию стены, инерцию и то, что мышь
  (рамка левой, панорама правой) не пострадала.
- `tests/mobile-layout.mjs` дополнен до 24 ok (инварианты новой раскладки),
  `tests/smoke-dom.mjs` — до 26 ok (клик по «глазу» и по вкладке «Отряд»
  в настоящем DOM).

### Грабли этой правки
- **Хуки после раннего `return`** → React error #310. Все `useState`/`useEffect`
  должны стоять до `if (screen === 'menu') return`.
- В Node нет `Path2D` — для `drawMinimap` в тесте нужна заглушка.
- rect мини-карты обновляется только на отрисовке: в тесте после смены
  `minimapOpen` надо звать `drawMinimap()` руками, иначе старый rect перехватывает
  касание (так и упал раздел про стену).
- `document.body.textContent` в смоук-тесте содержит текст самого бандла из
  `<script>` — проверки по строкам надо делать по `#root`.
- `frame(t)` в тесте: `t` считать от `game.last`, иначе первый dt уходит в минус
  и камера улетает.

## 1.0.138 — верхний HUD: три полосы вместо трёх-четырёх рядов

Продолжение мобильной правки: после 1.0.137 сверху оставалось ~150–190px пилюлей.

- Три полосы HUD (ресурсы • события и счёт • кнопки) на телефоне идут друг под
  другом, и каждая — **одной строкой с горизонтальной прокруткой**. Раньше они
  переносились в 3–4 ряда и занимали до 30% экрана. На широком экране раскладка
  прежняя (строка и колонка событий).
- Внутри полос пилюлям дан `shrink-0` (вариант `[&>*]:shrink-0`): без этого flex
  сжимал их и цифры обрезались.
- Убрана дублирующая полоса счёта на телефоне: те же очки, время и эпоха уже
  есть в полосе событий. На планшете (640–768px) она осталась.
- Карточка выделения и баннеры режима привязаны к высоте дока, а он в 1.0.137
  стал ниже (56px вместо ~190): `bottom-[236px] → 104px`, `132px → 76px`.
- Устав: раздел «Сенсор» переписан под новую схему (чип карты, док-ящик, глаз).

Проверки: 55 файлов тестов зелёные, `mobile-layout.mjs` — 31 ok (новый раздел
следит, что полосы не переносятся и что карточки не висят над пустотой),
`tsc` чисто, сборка 3.9 с. Отдельно проверил в собранном CSS, что Tailwind
генерирует `sm:overflow-x-visible` и `[&>*]:shrink-0`.

## 1.0.139 — качество процедурного пиксель-арта

Задача: «улучшить процедурный пиксель-арт, с наибольшим разрешением и лучшим
качеством». Речь о резервном рендере юнитов (`src/game/pixelart.ts`), который
виден, пока PNG-спрайты не загружены.

**Главная помеха была в производительности, а не в умении рисовать.** Юнит
рисовался сотнями `fillRect` прямо в боевой канвас для каждого юнита каждый
кадр — поэтому шаг сетки приходилось держать крупным (`UB = 3`), иначе 60 кадров
не вытянуть. Снято кэшем кадров:

- `procFrame()` собирает кадр один раз на комбинацию «юнит · команда · поворот ·
  фаза · зум» в оффскрин и кладёт в `PROC`. В бою остаётся один `drawImage`.
- Шаг сетки для юнитов `UB = 3` → `UUB = 1` (детализация ×3): примитивы `pxu`,
  `cxu`, `lnu`. У зданий и пропов остался прежний `UB` — их вид не тронут.
- Суперсэмплинг: черновик рисуется с запасом (`ssFor`: ×2 на отдалении, ×1.25
  при зуме ≥1.75) и уменьшается при выводе — вместо лестниц из квадратов
  получаются гладкие края.
- Контур: силуэт кадра берётся по альфе, красится в тёмный и подкладывается под
  кадр со сдвигом на 1 по восьми направлениям. Юнит читается на любом фоне.
- Память: кэш ограничен 260 кадрами, при переполнении выбрасываются самые
  старые (40%). Замер на листе арта: холодный проход 30 978 примитивов,
  повторный — 513, то есть в 60 раз меньше работы на кадр.

**Перерисованы фигуры** (объём = тень/блик через `shade()` → `D()`/`L()`):

- человек: руки (задняя в противофазе шагу, передняя выносится при ударе),
  корпус с тенью со спины и бликом по груди, пояс с пряжкой, подол, сапоги,
  лицо (`drawFace`: белки, зрачки, брови, нос, усы, борода воину), блик на
  шлеме и наплечниках; оружие — с кромкой и тенью;
- конь: вытянутый корпус с крупом и грудью, клин шеи, вытянутая морда с
  ноздрёй, глаза с белком, два уха, уздечка и повод, грива и чёлка прядями,
  хвост в две пряди, попона цвета команды, подпруга, седло с луками; ноги —
  бедро/голень/копыто, диагональные пары в противофазе;
- всадник: нога в стремени, кираса с объёмом, лицо, рука с пикой;
- скот: блик по спине, тень по брюху, глаза с белками и зрачками (в боковом и
  переднем ракурсах), ноздря;
- волк: вытянутое тело, свет по хребту, клочья шерсти по силуэту, острая морда
  с клыками, жёлтые глаза с зрачком и бликом, пушистый хвост;
- катапульта, таран, фальконет: объём дерева и бронзы, тени под рамой,
  волокна древесины, сучки на бревне, блик по стволу.

**Лист арта `/?dev=art`** (`src/ArtSheet.tsx`): все 22 юнита крупно, в четырёх
кадрах (покой • шаг • удар • разворот), переключатель команды и кнопка
«Показать PNG-арт» — подгружает настоящие спрайты для сравнения. Подключается
динамическим импортом, отдельный чанк 3 КБ, в игру не попадает.

Тест `tests/artsheet.mjs` (14 проверок): лист поднимается в jsdom, все кадры
рисуются без ошибок, кэш наполняется и переиспользуется. Всего тестов: 56,
зелёные.

## 1.0.140 — ультрареалистичный процедурный арт юнитов

Правка курса. В 1.0.139 я улучшил «качество рисунка» (контур, объём, детали),
но пользователь оценил результат как «детские рисунки» и потребовал
ультрареалистичности. Причина оказалась не в проработке, а в трёх вещах:

1. **Масштаб отрисовки.** Процедурный юнит рисовался сразу в боевой размер
   (~34 единицы), а настоящие спрайты нарисованы крупно (87×150, 127×150,
   150×128) и только потом уменьшаются до 46–60 пикселей по `UNIT_TARGET_H`.
   На 34 пикселях не помещаются ни кисть, ни черта лица — физически.
2. **Пропорции.** Голова была 14 единиц при росте 34 — **1:2.4**, это пропорции
   ребёнка. У коня голова радиусом 6 при корпусе радиусом 12 — жеребёнок.
   У скота голова радиусом 5–6 при корпусе 8–11 — те же детеныши.
3. **Примитивы вместо форм.** Тела были прямоугольниками 14×15, ноги — палками.

Что сделано:

- **Арт-масштаб** (`artFor`): кадр собирается в ×4 крупнее боевого на обычном
  обзоре (черновик ~136 пикселей — как у настоящих спрайтов), ×3 и ×2 на
  приближении, и уменьшается при выводе. Кэш кадров 1.0.139 делает это
  бесплатным по fps; лимит кэша снижен до 180 кадров (при ×4 кадр тяжелее).
  Шаг сетки примитивов `UUB = 1 / A` — примитив кладёт ровно один пиксель
  черновика, то есть детализация выросла вчетверо относительно 1.0.139.
- **Анатомия 1:6 у человека**: голова 6 единиц (было 14), шея, плечи шире
  талии, рука из плеча/локтя/кисти, нога из бедра/колена/голени/сапога с
  подошвой. Высота силуэта не изменилась — тень, попадание и посадка на тайл
  считаются по-прежнему.
- **Материал**: чешуя доспеха рядами (у каждой пластины светлый верх и тёмный
  низ), складки и запах ткани, пояс с пряжкой, ворот-ожерелье, блик по metalу
  шлема, глаза с белками и зрачками, усы/борода; у оружия — клинок с долом,
  гарда, рукоять с обмоткой, древко с обмоткой и наконечником.
- **Конь**: холка 24, корпус вытянут 24×11, голова 9 (было 12), ноги —
  бедро/голень/копыто, мышцы, шерсть, грива и чёлка прядями, уздечка с бляхой,
  седло с луками, стремя, попона цвета команды с каймой и подпруга.
- **Всадник**: бедро вперёд, голень вниз, сапог в стремени, кираса чешуёй,
  наплечники, лицо, шлем с султаном.
- **Скот**: корпус вытянут (20–24×11), голова 6–7, ноги с голенью и копытцем,
  шерсть клочьями (овца), пятна и вымя (корова), ветвистые рога (олень),
  глаза с белком во всех трёх ракурсах (бок/перёд/спина).
- **Волк**: вытянутое тело 22×10, лохматый силуэт, свет по хребту, острая
  морда с клыками, жёлтый глаз с белком и бликом, хвост в две пряди.
- **Осадные машины**: клёпаные колёса со спицами и бликом обода, брусья с
  волокнами древесины и торцами, А-образная стойка с заклёпками оси, рычаг с
  ковшом и противовесом, у тарана — стропила и швы на коже, сучки на бревне,
  окованный наконечник и подвес, у фальконета — болты лафета и бронзовый ствол
  с бликом по граням.

Техническое: суперсэмплинг (SS) из 1.0.139 заменён арт-масштабом (он же даёт
запас по разрешению, но теперь это и есть детализация, а не сглаживание);
контур стал тоньше (сдвиг на 1 пиксель черновика). `tsc` чистый, тестов 56,
все зелёные, сборка 3.87 с.

Побочная ветка, которую НЕ стали делать: генерация новых PNG-спрайтов через
`scripts/process-kz-sprites.cjs` (raw на magenta-фоне). Один пробный спрайт
(kz_villager) был пересобран и **откатан** — задача стояла про процедурный
рендер, а не про растровые спрайты.

## 1.0.141 — казахский образ и формы вместо прямоугольников

Оценка 1.0.140: «всё ещё плохие арты, и они не похожи на казахских воинов».
Две отдельные претензии, два отдельных исправления.

**1. Формы.** Прямоугольниками одежду не нарисовать: чапан — это трапеция со
скошенными плечами и расширением к подолу, қылыш — дуга, бөрік — полукруг с
мехом. Добавлены формы:
- `poly` / `polyG` — заливка полигона, в том числе градиентом по вертикали
  (свет падает сверху и чуть слева — отсюда объём, а не плоские заплатки);
- `arc` — дуга (сабля, дуга лука, обод щита, ухо);
- `ell` — эллипс с градиентом (голова, туша, круп, щит);
- `ornament` — волнистая линия «қошқар мүйіз» (бараньи рога) по кайме одежды.

**2. Образ — воин Абылай-хана, а не абстрактный человечек:**
- головные уборы: **бөрік** (меховая шапка) у ополченца, **дулыға** (купол, обод,
  наносник, прорези для глаз, бармица-сетка на шее, султан-айдар) у доспешного
  воина, **қалпақ** (войлочная шляпа с полями) у шаруа, капюшон с остриём у
  лучника, чалма у имама, **тақия** у көпеса;
- одежда: **шапан** с запахом направо, подол колоколом, кайма с орнаментом
  «қошқар мүйіз», **белбеу** (пояс) с пряжкой и бляхами, **шалбар**, **етік** с
  голенищем, мыском и подошвой;
- доспех: **сауыт** — ряды пластин со светлым верхом и тёмным низом, наплечники,
  ожерелье;
- оружие: **қылыш** (сабля дугой, с елманью, гардой и навершием), **найза**
  (копьё: наконечник-перо, красная кисть под ним, обмотка древка), **садақ**
  (сложносоставной лук — две дуги, тетива, стрела), **қалқан** (круглый щит:
  обод, умбон, шесть заклёпок, орнамент);
- лицо: смуглая кожа с тенью по скуле, разрез глаз с эпикантусом (бровь и
  белок с наклоном к виску), усы, борода;
- **конь — казахской породы (жабы/джабе)**: короткая мясистая шея, широкая
  грудь, плотный корпус, голова с широким лбом и короткой мордой, косматые
  грива и чёлка, густой хвост; сбруя казахская — седло с высокой передней
  лукой, уздечка с бляхами-нашивками, попона с орнаментом, стремя;
- **всадник**: бедро вперёд, голень вниз, сапог в стремени, сауыт чешуёй,
  шапан под ним, бармица, султан, пика с красной кистью.

Пропорции уточнены: голова 7.5 единицы при росте 34 (**1:4.5** вместо 1:6) —
столько, чтобы читались глаза, усы и брови; фигура при этом остаётся взрослой.

Скот, волк и осадные машины остались в пропорциях 1.0.140 (к ним претензий по
образу не было — это не люди); следующий шаг, если понадобится, — верблюд для
көпеса и племенных всадников.

`tsc` чистый, тестов 56 — все зелёные, сборка 3.84 с.
