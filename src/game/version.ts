// Версия игры — единый источник для показа в меню.
// Вынесено ИЗ App.tsx намеренно: файл, который экспортирует не-компонентные
// значения рядом с компонентами, ломает React Fast Refresh — Vite вместо
// горячей замены делает полную перезагрузку страницы (см. заметку
// vite-plugin-react «consistent-components-exports»). Держим App.tsx
// «только компоненты», чтобы правки UI не сбрасывали игроку страницу.
// При обновлениях поднимаем ТРЕТЬЮ цифру на 1: 1.0.008 → 1.0.009 → 1.0.010 …
export const GAME_VERSION = '1.0.103';
